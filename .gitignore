# macOS
.DS_Store
**/.DS_Store
__pycache__/

# Claude / 로컬 도구 설정
.claude/

# 악보 · 이미지 · 논문 (코드만 커밋)
*.png
*.jpg
*.jpeg
*.gif
*.pdf
정악보/
symbol_samples/

# 로컬 보관 폴더 · 참고 자료(이미지 등은 위 규칙으로 이미 제외)
_보관/

# 논문·연구 자료·세션 노트 — 공개 저장소에 올리지 않는다 (2026-07-27 히스토리 초기화)
paper/
references/
NEXT-SESSION.md

# 장구 음원 원본 wav (12MB) — js/janggu-audio.js로 가공해 쓰므로 리포에는 안 담는다
장구단음/

# 씨앗 악보의 수정 열쇠 — 서버 게시물을 고치거나 내릴 때 쓴다. 절대 커밋 금지.
server/tokens.local.json
# 국악원 OMR 게시물(69곡)의 수정 열쇠 — tools/publish-ngc-omr.mjs가 쓰고 만든다. 커밋 금지.
server/ngc-tokens.local.json
# 관리자 로그인 — tools/scan-published.mjs가 읽는다(환경변수로 줘도 된다). 절대 커밋 금지.
server/admin.local.json

# 국악원 OMR 데이터셋 — CC BY-NC-SA 원자료·변환물·그림. 리포에는 변환기 코드만 담는다.
국악원OMR데이터셋/
