# 정간보 생성기 — 작업 안내 (Claude용 코드 맵)

목적: 매 세션 파일 전체를 다시 읽지 않고 바로 해당 위치로 가기 위한 지도.
큰 리팩토링 없이 빠른 수정을 우선한다.

앱 이름(브랜드)은 **우물사이**(Umulsai) — 정간(井間)이 '우물 정(井) 자 사이 칸'이라는 데서 온 이름.
상단바 맨 왼쪽 #brandBox(로고+이름, 페이지 유일 h1)·탭 제목·meta description·환영 카드에 쓰인다.
로고 원본은 `assets/Gemini_Generated_Logo.png`(까치+井 붓글씨), 웹용 가공본(투명화·크롭·축소)은
`assets/brand/`이며 index.html에는 **데이터 URL로 인라인**(파비콘 포함) — 프리뷰 미러가
assets/를 제외해도 보이게. 상단바 로고는 **위쪽 크롭본**(umulsai-top-128: 까치+맨 윗 가로획,
전체 井은 작게 그리면 새가 안 보임), 파비콘은 전체 로고(favicon-64, 흰 배경).
상단바 로고가 128px(표시 36px의 ~3.6배)인 건 화질 때문 — 예전 64px 가공본은 dpr 2에서 딱
1:1인 데다 **잉크 알파가 최대 221(반투명)**이라 흐리고 회색으로 씻겨 보였다. 재가공은
`python3 tools/regen-top-logo.py`(원본에서 현행 프레이밍을 MSE 탐색으로 되찾아 크롭,
알파 16단계+팔레트 64색 양자화로 2KB 미만) → 출력 PNG를 base64로 index.html 데이터 URL에
교체. 다크모드 로고는 CSS invert(body.dark #brandBox img).
워드마크(#brandWord)는 **EBS훈민정음 SB를 아웃라인 패스로 뜬 인라인 SVG** — 폰트 파일은
안 싣는다. EBS 라이선스가 웹 임베딩·BI/CI는 허용하나 폰트 자체의 변형(서브셋 포함)·재배포는
사전 서면승인 대상이라, '폰트로 만든 로고'인 패스 방식이 그 조항을 안 건드린다(2.6KB, 모든
OS 동일). 다시 뜨려면 `python3 tools/gen-wordmark.py` → 출력을 index.html #brandWord 자리에
교체(fontTools + ~/Library/Fonts/EBS훈민정음SB.otf 필요, 폰트는 리포에 없음).
자간은 패스에 구워져 있어 CSS letter-spacing으로 못 벌린다. 잉크 경계로 크롭해서 SVG 아래끝이
글자 밑선이 아니므로 `transform: translateY(13.66%)`로 내려야 옆 "Umulsai" 글줄과 밑선이 맞는다
(음수 margin·position:top의 %는 안 먹는다 — styles.css 주석 참고). 색은 fill=currentColor라
다크모드 자동(로고 img의 invert와 무관).
문패는 로고 옆 **두 줄**(위=이름, 아래=작은 "정간보 편집기") — 위/아래 자리가 곧 위계라 부제가
부제로 읽힌다(한 줄로 옆에 붙이면 이름의 꼬리처럼 보였다). 크기는 로고 36px(상단바 버튼과 같은
높이)·워드마크 18px·부제 11px — 입력 방식 버튼이 상단바에서 빠져 왼쪽이 허전해지면서 한 단계
키운 값이고(원래 32/16/10), 텍스트 열(32px)이 로고 안에 들어가 상단바는 49px 그대로 —
"두 줄은 답답하다"는 궁서 20px 시절 판단이니 되살리지 말 것.
부제의 `letter-spacing:.5em` + `margin-right:-.5em`은 두 줄의 **오른쪽 끝까지**
맞춘 값이다(자간이 마지막 글자 뒤에도 붙어 잉크 폭 = 박스 폭 − 자간 1개 → 박스만 맞추면 오른쪽이
5.5px 뜬다. 음수 마진은 그 꼬리 여백을 레이아웃에서 걷는 용). svg height나 부제 문구를 바꾸면
다시 풀어야 한다 — 계산은 styles.css 주석 참고. 로마자 "Umulsai"는 문패에서 뺐다 — 이름의 로마자와
'뭐 하는 앱인지'는 성격이 달라 `·`로 묶으면 둘 다 흐려진다(탭 제목·meta·#brandBox 툴팁엔 남음).
상단바 그룹 구분은 세로 구분선 없이 #topBar gap(16px)만으로 — 구분선
(.topbar-sep)은 제거했고 되살리지 말 것. 사이드바 머리글은 '설정'(h2.side-title).

## 파일 구조

폴더 정리됨 — 루트엔 진입점 `index.html`(마크업)·`CLAUDE.md`·`.gitignore`만 두고, 나머지는
기능별 폴더로 묶었다. **index.html이 `css/styles.css`·`js/*.js`를 상대경로로 로드**하므로 파일을
옮기면 이 script/link 태그도 같이 고쳐야 한다.

- `index.html` (~950줄, 루트) — 마크업 전부. 패널·버튼마다 "왜 이렇게 뒀는지" 주석이 붙어 있음.
- `js/app.js` (8천 줄대) — 단일 IIFE. **섹션 마커로 탐색**: `grep -n "// ----------" js/app.js`
  가 목차 역할을 한다 (렌더/에디터/팔레트/재생/저장/되돌리기 등 30여 섹션).
- `js/cloud.js` + `js/cloud-config.js` — 악보 게시(서버에 올리고 `#v=<id>` 주소로 열기).
  app.js **뒤에** 로드되며 `window.jgbDoc`만 통해 문서를 만진다. 설정이 비면 스스로 물러난다.
  서버 쪽은 `server/schema.sql`(Supabase에 붙여넣어 실행). 규칙은 아래 '악보 게시' 절.
- `policy.html` + `notices.html` + `css/doc.css` + `js/doc-brand.js` — **공적 문서 두 장**.
  `policy.html`은 **이용약관 및 저작권 정책**(금지 대상·신고 절차·개인정보),
  `notices.html`은 **중단 내역**(신고로 내린 악보의 기록).
  **조문 형식의 공적 문서**다 — 조(條)·항(①)·호(1.)의 층을 지키고, 앱의 다른 글처럼 설명하거나
  설득하지 말 것('오히려 그럴수록…' 같은 논증조 문장은 여기 안 쓴다). 근거는 조문 번호로만 적는다.
  꼴은 `css/doc.css`, 문패·테마는 `js/doc-brand.js`가 함께 맡는다(index.html에서 로고를 가져와 꽂아
  로고를 두 번 박지 않는다).
  **운영자가 남의 악보를 고칠 수 있는 근거는 제5조 제4항부터 제7항까지**다(2026-08-21 개정).
  범위를 '명백한 표기 오류'로 좁히고(입력 형식에 안 맞아 **안 그려지거나 뜻과 다르게 그려지는**
  부분·오기가 분명한 기호·게시자가 요청한 것), 교정 전후 판을 보존하며, 게시자가 요구하면 되돌린다 —
  `score_versions`와 `admin_log`가 그 조항의 이행 수단이다. **이 셋 밖의 내용 변경은 약관
  위반이니 `admin_save_score`를 그 밖에 쓰지 말 것.** 관리자 계정이 생기며 '회원 계정을 두지
  않는다'는 제2조 제2항·제8조 제1항은 '**이용자를 위한** 회원 계정'으로 좁혔다.
  **중단 내역이 게시자 통지를 갈음한다** — 계정이 없어 올린 사람의 연락처를 모르므로, 개별 통지
  대신 '왜 내려갔는지 공개적으로 확인할 수 있는 자리'를 둔 것이다(약관 제6조 제4항). 삭제 기록을
  남기는 자리이기도 하다. 손으로 `<tr>`을 더하는 정적 표이고 서버에서 가져오지 않는다 — 신고가
  들어와야 쓰이는 페이지라 자동화할 양이 아니고, 사유는 사람이 적어야 하는 말이라서.
  신고 받는 주소는 `policy.html`·`notices.html`·`js/cloud.js`의 `REPORT_TO`·`browse.html` 꼬리말
  **네 곳이 같아야** 한다. 저작권법 제102·103조가
  요구하는 '신고 수령인 공지'가 여기 있으므로, **신고 받는 주소를 바꿀 땐 js/cloud.js의
  `REPORT_TO`와 browse.html 꼬리말까지 세 곳을 함께** 고쳐야 한다(두 군데서 다르면 안 된다).
  문패는 index.html에서 fetch로 가져와 꽂는다 — 로고를 두 번 박지 않으려고.
- `browse.html` + `css/browse.css` + `js/browse.js` — **모아보기**(공개 악보 목록).
  **UI 표기는 '모아보기'** — 도움말의 기능 안내 투어가 '둘러보기'라 말이 겹쳐 갈랐다(2026-08-04,
  사용자 확정). 파일 이름·식별자(browse·btnBrowse·sc-*)는 그대로 — 이미 배포된 주소를 바꾸지
  않으려는 것이고, 이 리포는 코드 이름과 표시 이름을 분리하는 관례다(cellStyle*↔'정간 서식'처럼). index.html과
  **별개 문서**라 app.js·기호 데이터(600KB)를 안 싣는다 — 여기선 악보를 그리지 않으므로.
  카드의 그림은 올린 사람 브라우저가 게시할 때 떠 둔 것이다. 규칙은 아래 '모아보기' 절.
- `admin.html` + `css/admin.css` + `js/admin.js` — **관리 화면**(운영자용). browse.html과 같은
  '별개 문서'라 app.js·기호 데이터를 안 싣는다. 앱 어디에도 이리로 오는 링크가 없고
  `robots noindex`다 — 다만 **숨기는 것은 보안이 아니라 정리**다. 진짜 빗장은 서버에 있다
  (로그인 + `admins` 명단). 규칙은 아래 '관리 화면' 절.
- `js/staff-glyphs.js` — 오선보의 악보 글리프(자리표·임시표·쉼표·꼬리·박자표) 윤곽선.
  **생성 파일**(`tools/gen-staff-glyphs.py`) — 직접 수정 금지. 자세한 것은 아래 '오선보 보기'.
- `js/staff-core.js` + `js/staff-view.js` + `js/musicxml.js` — **오선보 세 짝**.
  app.js가 `buildStaffScores()`로 만든 재료를 **화면(staff-view)과 파일(musicxml)이 나눠 쓰고**,
  음이름 적기·조표 고르기·음표꼴 같은 밑감은 staff-core가 준다. 셋 다 **app.js를 한 줄도 안
  본다** — 받는 것은 그릇 하나뿐이라 악기가 늘어도 그대로 쓴다. index.html에서 app.js보다
  **먼저**, 그리고 staff-core가 셋 중 **맨 앞**에 실려야 한다(staff-glyphs는 staff-view보다 앞).
  예전엔 app.js(mxlPitch)와 staff-view(pitchAt)에 같은 셈이 두 벌이라 1155자리 대조 검사로
  겨우 맞춰 두고 있었다 — staff-core로 합치며 그 대조가 필요 없어졌다. **새 오선보 기능은
  app.js가 아니라 이 셋 중 하나에 붙일 것.**
- `js/vendor/verovio-toolkit-wasm.js` — 오선보 **화면 조판기**(Verovio 6.2, LGPL, wasm 내장
  단일 파일 6.7MB). index.html에 안 실리고 오선보 칸을 열 때 app.js가 지연 로드한다
  (janggu-audio와 같은 수법). 규칙은 아래 '오선보 보기' 절.
- `js/jangdan-presets.js` (window.JGB_JANGDAN) — **정악 장단 모음**. 국립국악원 『양금정악보』
  (2021)의 곡별 기본 장단을 그 정간보 그대로 옮겨 적은 표라 **사람이 직접 고친다**(생성 파일
  아님). 항목: `{ name, beats, daegang, jangdan }` — 장단 줄 문법은 선율과 같다(`|`로 나눈
  칸 하나 = 정간 하나). app.js보다 먼저 로드. 규칙은 아래 '장단 프리셋' 절.
- `js/analytics.js` — 익명 사용 통계 래퍼(쿠키·식별자 없음). app.js는 `track(name, {v})` 안전
  호출만 하고, 전송은 이 파일의 GoatCounter 어댑터가 담당(GOATCOUNTER_CODE 비면 대기 모드,
  로컬/DNT 제외). 검증은 `window.jgbTrack.recent`(메모리 링 20건). app.js보다 먼저 로드.
- `css/styles.css` (~940줄) — 위→아래 순서가 화면 구성 순서와 대체로 일치
  (상단바 → 사이드바 → 리본 → 독/팔레트 → 툴팁 → 인쇄).
- `js/notes-data.js`·`js/symbols-data.js`·`js/janggu-data.js` — 생성된 데이터(데이터 URL 포함).
  직접 수정 금지. `assets/symbol_svgs/` 원본을 바꿔도 앱은 `js/symbols-data.js`를 읽으므로 재생성 필요.
  `js/symbols-data.js`(window.SYM_DATA)는 `tools/gen-symbols-data.mjs`로 생성 —
  `assets/symbol_svgs/{symbols,tempo,special}` 세 폴더를 스캔한다(폴더 간 파일명 중복 금지).
  재생성: `node tools/gen-symbols-data.mjs` (리포 루트). 키 = 확장자 없는 파일명.
- `js/janggu-audio.js` (window.JANGGU_AUDIO) — 장구 구음 **소리**(mp3 데이터 URL). 위 그림
  데이터와 달리 **index.html이 미리 싣지 않는다** — [재생]을 눌러야 쓰이므로 app.js의
  `loadJangguAudio()`가 첫 재생 때 `<script>`로 붙인다(그래도 데이터 URL이라 경로·CORS를 안 탄다).
  원본은 `장구단음/<구음>_<세기>.wav`(git 미추적, 12MB) — 그대로는 못 쓴다. **앞에 무음이
  0.2초** 붙어 있어 박에 맞춰 울리면 그만큼 늦고, 4초짜리 파일이 스무 개다. 재생성은
  `python3 tools/gen-janggu-audio.py`(ffmpeg 필요)가 앞뒤 무음을 떼고 모노 96k mp3로 줄여
  다 합쳐 85KB로 만든다. 키 = 장단 줄에 적히는 구음 이름이고 `궁←쿵_중`·`작은덩←덩_약`
  대응은 생성기 주석 참고(나머지는 '중' 세기만 쓴다 — 구음 기호는 세기를 적지 않으므로).
- `js/symbols-registry.js` (window.JGB_SYM) — **기호 사전**. 위 그림 데이터에 이름과 '놓일 자리'를
  붙이는 표라 **사람이 직접 고친다**(생성 파일 아님). 시김새·가사 기호·장구 구음이 여기 한 곳에
  모여 있다 — 예전엔 팔레트마다 목록이 따로라(ORN_LIST / LYRIC_SYMS+별칭표 / JANGGU_NAMES)
  '같은 기호를 저 줄에도'가 생길 때마다 표와 별칭이 한 겹씩 늘었다. **기호를 더하거나 크기를
  손볼 땐 app.js가 아니라 이 파일을 고칠 것.**
  - 항목: `{ id, ko, at:{자리:크기}, attKeep?, snd? }`. `id`=그림 키(SYM_DATA 또는 JANGGU_DATA),
    `ko`=표시 이름 겸 토큰 이름.
  - **`snd`** — 이 기호가 어떻게 울리나(`{seq,pre,post}`). 값 하나는 **음계 칸수**(정수)
    또는 율명(문자열)이고, 기준음은 자리가 정한다: `cell`이면 **앞 음**, `att`면 **제가 붙은
    본음**. 그래서 붙임에 `seq`가 있으면(나니나) '본음 자리를 이 꼴로 가른다'가 된다.
    표(表)류(흘림표·미는표·요성표·추성·퇴성…)엔 일부러 안 달았다 — 음높이를 옮기는 게
    아니라 주법을 적는 표라 사인파로 흉내 낼 것이 없다. **없으면 소리가 없다**(앞 음이 이어짐).
    자세한 형식·'추정' 표시는 사전 머리말 참고. 재생(app.js)과 앞으로의 오선보 내보내기가
    **같은 이 표 하나**를 본다 — 소리를 고칠 땐 app.js가 아니라 여기.
  - 자리 다섯: `att`(음표 옆 작게) · `cell`(정간 한 칸) · `tempo`(정간 밖 세로) ·
    `lyric`(가사줄 한 분박) · `jd`(장단줄 한 분박). att+cell = 옛 `c:"both"`.
  - `cat`은 팔레트에서 묶음을 가르는 꼬리표(지금은 `"장구"` 하나). `chip`은 **팔레트
    아이콘만** 줄이는 비율 — 장구 '다'처럼 실물이 작은 점이라 상자를 꽉 채우면 큰
    덩어리로 보이는 기호용. 예전엔 `#jangdanPalette` 전용 CSS였는데 같은 기호가 가사
    팔레트에도 나오면서 거기선 안 걸렸다. **창이 아니라 기호에 딸린 사실은 사전에 둘 것.**
  - 장구 구음 7종은 `jd`와 `lyric` 두 자리를 갖는다(장단 줄이 없는 악보에서 구음을 정간
    옆에 적는 쓰임). 가사줄에선 0.65, '다'는 0.23. **'다'는 비율이 아니라 눈에 보이는 크기로
    잡은 값**이다 — 가사줄은 장단 줄보다 좁아 같은 0.15배를 쓰면 점이 실선 굵기까지 내려가
    사라진다(실측 0.30 대 장단 0.74). 자리마다 값이 따로인 이유가 이것이니, 한쪽을 고칠 때
    다른 쪽에 비율로 옮기지 말고 그 자리에서 눈으로 확인할 것.
  - **`box: "square"`** — 가사줄 그리기 상자는 폭의 2.5배쯤 세로로 길다(폭은 가사줄 너비,
    높이는 정간 높이 기준). 그대로 두면 세로로 긴 기호만 높이를 꽉 채워 둥근 기호보다 2.5배
    커진다(기덕·덕·더러러러가 덩·궁보다 컸다). 구음처럼 **한 글자로 읽히는 기호는 서로 키가
    같아야** 하므로 장구 7종에 이 표시를 달아 장단 줄과 같은 정사각 상자에 넣는다.
    가로표·세로표 같은 '표'류는 칸을 가로지르는 게 제 노릇이라 **달면 안 된다**.
  - 팔레트 차례는 사전 순서가 아니라 `LYRIC_ORDER`·`JANGGU_ORDER`가 정한다(무엇이 있나 ≠
    어떤 차례로 보여주나). `LEGACY_ALIAS`는 옛 토큰 호환 **읽기 전용** — 새로 늘리지 말 것.
  - app.js는 옛 이름(`ORN_LIST`·`ATT_SYM_SCALE`·`LYRIC_SYMS`·`JANGGU_NAMES` 등)으로 이 사전을
    받아 쓰는 얇은 층만 남았다(`SYM_REG`). 그래서 index.html에서 **app.js보다 먼저 로드**해야 한다.
  - 이름이 같은 짝이 둘 있는데 성격이 다르다: 선율 `fermata`↔가사줄 `늘임표`는 **같은
    그림**이 두 키로 등록된 것(바이트까지 동일 — 전체 103항목 중 유일한 그림 공유,
    2026-07-27 실측)이고, `sigimsae-00`↔`뜰`은 이름만 같고 **그림이 다르다**.
    사전이 하나가 되며 드러난 충돌이라 주석으로 표시만 해뒀다 — 정리는 이름 일원화 단계에서.
- `assets/symbol_svgs/`·`assets/janggu_svgs/` — 데이터 JS 생성용 SVG 원본(빌드 입력, 브라우저는 안 읽음).
- `tools/gen-symbols-data.mjs` — 위 생성기. `docs/기능-정리.md` — 사용자용 기능 설명서.
- `docs/관리자-운영.md` — **올라온 악보를 고치고·숨기고·되돌리는 법**(운영자용 절차서).
  관리자 페이지가 아직 없어 지금은 전부 Supabase SQL Editor에서 하므로, 손으로 칠 SQL이
  거기 있다. 급소는 `push_version`을 함께 부르지 않으면 판이 안 남는다는 것.
- `tools/gen-help-doc.mjs` → `docs/도움말-문구.md` — **앱에 나오는 안내 문구를 한 문서로 모은다**
  (둘러보기·도움말 4탭·도구창 ? 안내 7개·환영/새 문서·팔레트 한 줄 안내·툴팁·호버 설명).
  문구를 다듬을 때 '어디에 무슨 말이 있나'를 여기서 찾는다. 쓰는 길이 둘이다:
  - **소스를 고치고 문서를 새로 뽑기** — 절마다 적힌 '고치는 곳'을 고친 뒤 생성기를 다시 돌린다.
  - **문서를 직접 고치고 그대로 적용 요청하기** — 사용자가 이 문서에서 문구를 다듬어 두면
    그걸 보고 소스에 옮긴다. 이 쓰임 때문에 **문서를 손으로 고치는 것을 막지 않는다.**
  손댄 내용이 재생성으로 날아가지 않게 문서 끝에 만들 때의 지문(해시)을 적어 두고, 생성기는
  **문서가 손대진 상태면 덮어쓰지 않고 멈춘다**(버리고 새로 만들려면 `--force`).
  지문은 끝의 빈 줄을 떨군 본문으로 뜬다 — 쓸 때와 읽을 때 손질이 다르면 손 안 댄 문서도
  '고쳐졌다'로 걸린다(실제로 한 번 그랬다).
- `tools/scan-published.mjs` — **게시된 악보의 문법을 훑어 결과를 서버에 적는다**
  (`scores.lint_bad` → 관리 화면의 '문법 오류' 탭). 서버는 악보를 모르므로(schema.sql 뼈대 ①)
  재는 일은 여기서 하고 서버엔 **숫자만** 간다. 재는 자는 app.js의 `melodyBadFlags`를
  app-sandbox로 떼어 온 것 — 편집기에서 빨간 바탕을 깔아 주는 바로 그 함수라, 화면과 검사가
  어긋날 수가 없다. **선율만** 본다(곁줄·장단은 글자를 적는 자리라 틀렸다고 말할 잣대가 없다).
  · 관리자 로그인으로 든다(`JGB_ADMIN_EMAIL`/`JGB_ADMIN_PW` 또는 `server/admin.local.json`,
    gitignore). service_role 키는 안 쓴다.
  · **BASESET 빗장**을 먼저 지난다: app-sandbox는 BASESET(성립하는 율명 낱자)을 손으로 박아
    둔 대역으로 건네는데, app.js의 `YUL`과 어긋나면 멀쩡한 율명이 죄다 '틀림'이 되어 **모든
    악보가 통째로 오류로 보고된다**. 그래서 YUL을 소스에서 읽어 대조하고 어긋나면 멈춘다.
  · 값이 달라진 악보만 적는다(여러 번 돌려도 `lint_at`이 헛되이 갱신되지 않는다).
  · **`--fix-safe`는 답이 하나로 정해지는 것만 고친다**: 한자 율명 → 한글(`黃`→`황`,
    `潢`→`청황`) · 괄호 안 빈칸 걷기(`{ 흘림표 }`→`{흘림표}`) · **붙여 쓴 것을 분박으로
    나누기**(`청고청남`→`청고 청남`). 시김새 이름 오타·어긋난 괄호 짝·문장부호는 **손대지
    않는다** — 무엇을 쓰려던 것인지 짐작해야 하는 갈래라 약관 제5조 제4항의 '명백한 표기
    오류'가 아니다. 기본은 꺼져 있다.
    · 분박 나누기가 짐작이 아닌 까닭 둘: **어디에 넣을지**는 그리기가 쓰는 칸 경계
      (`groupRowTokens`)를 되찾을 뿐이고, **소리가 안 바뀐다** — `realizeMelody`가 한 칸을
      `1 / (분박 수 × 좌우 칸 수)`로 나누므로 1행×N칸과 N행×1칸이 같은 값이다(차례도 같다).
    · **시김새가 낀 칸은 손대지 않는다**(`{흘림표}황`) — 붙이려던 것인지 위 분박에 독립으로
      두려던 것인지 갈린다. 쉼표·이음은 시김새가 아니라 그대로 다룬다.
    · **곁줄 맞추기는 선율 교정과 떼어 둔다**(`fitLyrics`). 선율을 나누면 곁줄도 따라 나뉘어야
      하는데 ③은 선율만 본다 — 떼어 두어야 **이미 나뉜 뒤의 악보도** 고칠 수 있다(2026-08-24:
      자동 교정을 돌린 뒤 가사가 안 맞는다는 제보로 붙였다. 자동 교정에 곁줄을 안 본 것이 탈이었다).
      나누는 조건이 좁다 — **한 분박짜리 곁줄 칸이 딱 그 수만큼의 한글 낱글자일 때만**
      (`청고 청고` + `작은` → `작 은`). 글자가 모자라면(`물` 한 자에 음 둘) 한 음절을 두 음에
      걸쳐 부르는 자리라 그대로 두고, 기호·한자가 섞여도 손대지 않는다.
    · 한자 대응표는 app.js의 `YUL`·`OCT_HANJA`를 **그 자리에서 읽어 뒤집은 것**이고,
      **같은 한자가 두 뜻을 갖는 것은 뺀다** — 57자 중 `侇`가 배유·배이로 갈린다
      (2026-08-24 실측 · app.js의 OCT_HANJA `-1`층에 같은 글자가 두 번 적혀 있다).
    · **고친 뒤 반드시 다시 잰다**(`fixSafely`). 틀린 자리가 줄지 않았거나 없던 자리가
      새로 생겼으면 그 교정을 버린다 — 짐작으로 남의 악보를 바꾸지 않겠다는 약속을
      코드로 지키는 자리다.
    · 되쓰기는 `admin_save_score`라 판이 쌓이고 메모가 남는다(되돌릴 수 있다).
- `tools/check-symbols-registry.mjs` — 사전이 만들어 내는 표 9개가 **기준 커밋의 app.js에 손으로
  적혀 있던 값과 똑같은지** 대조한다. 사전을 손본 뒤 `node tools/check-symbols-registry.mjs`가
  깨지면 악보 모양이 달라졌다는 뜻. 기호를 **새로 들이는 건 통과**시키고 몇 개 늘었는지만
  알려 준다 — 막는 건 옛 항목이 바뀌거나 사라지는 것뿐이라, 사전이 자라도 검사는 계속 쓸모 있다.
- `tools/lib/app-sandbox.mjs` — **app.js의 함수를 이름으로 떼어 와 Node에서 그대로 돌리는 상자**.
  app.js는 DOM에 붙은 큰 IIFE라 통째로는 못 부르는데, 검사 쪽에 같은 셈을 한 벌 더 적으면
  그건 '두 번째 구현'이라 app.js를 고쳐도 옛 답을 계속 맞다 한다. DOM은 그 함수들이 실제로
  읽는 칸 몇 개만 흉내 낸다. 아래 두 검사가 이걸 쓴다.
- `tools/check-sigimsae-sound.mjs` — 시김새가 소리로 제대로 풀리는지(사전의 `snd`) 검사한다.
- `tools/check-play-parts.mjs` — **재생이 보기를 따르는가**: 총보면 합주, 파트보면 그 악기만
  울리나 · 음소거는 두 보기 모두에서 사나 · 하이라이트 표식은 늘 활성 파트뿐인가.
- `tools/check-play-mix.mjs` — **합주가 최대치를 넘지 않는가**: 소리 나는 파트 수를 옳게 세나 ·
  그 값으로 나눈 마스터로 합성한 봉우리가 파트 수와 무관하게 1 안인가(표본으로 실제 재현) ·
  소프트 클립이 평소 소리엔 손을 안 대고 넘치는 자리만 누르나.
- `tools/check-gak-beats.mjs` — **각별 정간 수**(#gakBeats)가 정간 번호·재생 시간·오선보 마디
  길이 셋에서 같은 답을 내는지 검사한다.
- `tools/import-ngc-omr.mjs` — **국립국악원 OMR 데이터셋(.txt) → .jgb.json**(69곡, 악기 4~6종을
  총보 파트로). 저쪽 인코딩(`:위치 율명 시김새 | …`, Han et al. TISMIR 2026 §6)과 우리 것이
  **밑에 깔린 모델이 같아서**(정간을 행으로 나누고 그 행을 좌우 칸으로) 자리만 옮겨 적으면 된다.
  위치 0~15의 뜻은 논문 그림이라 글로 없어 **짝으로 든 MusicXML에서 실측해 확정**했다(파일 머리말
  참고). `--verify`가 변환 결과를 app.js의 `realizeMelody`에 먹여 칸 나눔을 대조한다(92,586 정간
  전부 일치). 데이터셋은 CC BY-NC-SA 4.0 — 변환물을 내보낼 땐 출처·조건을 함께 적을 것.
- `tools/check-jangdan-presets.mjs` — **장단 모음이 말이 되는가**: 칸 수 = 정간 수인가 ·
  적힌 구음이 사전에 있는 것뿐인가 · 대강 합이 맞나 · 이름이 겹치지 않나 · 그 장단이 실제로
  오선보(1선보)로 풀려 마디를 채우나. 손으로 옮겨 적는 표라 오타가 나기 쉬워 둔 검사다.
- `tools/check-janggu-staff.mjs` — **장단이 1선보로 옮겨지는가**: 두 성부가 저마다 마디를
  채우나 · 손이 기둥 방향과 맞나 · 타점 길이가 한 박에서 끊기나 · 겹채·굴림이 제 꼴인가 ·
  세 모드가 각각 몇 마디를 내나.
- `tools/check-musicxml.mjs` — 오선보 내보내기가 말이 되는 악보를 뱉는지: 마디 길이가 딱 맞나 ·
  음높이가 시김새 규칙과 맞나 · 붙임줄·조표·음이름이 옳은가.
- `tools/check-staff-view.mjs` — 오선보 그리기: 밑감이 음이름·조표를 옳게 푸나 · 그림 밖으로
  삐져나간 음표가 없나 · 빽빽한 정간에서 머리가 겹치지 않나 · 총보가 악기 수만큼 오선을 그리나.
- `references/` — 참고 자료(가야금주법·장구 시김새·악보 예시·정악보·legacies, git 미추적/이미지 무시).
  `_보관/`은 gitignore. `paper/`는 논문(코드와 별개).

## 핵심 구조 요약

- **문서 = 공유 뼈대 + 파트 목록**(총보 기반, app.js '파트 (총보 성부)' 절). 파트 = 악기 하나의
  내용(선율·곁줄·정간 서식·시김새 우선순위 악기), 공유 = 레이아웃 controls·장단(곡에 하나)·
  각 이름·자유 텍스트. 전역 melodyFull·lyricsFull·cellStyles·ornInstrument는 **활성 파트의 작업
  사본**이라 기존 코드가 그대로 돈다 — parts를 읽기 전엔 반드시 `stashActivePart()`, 파트를
  갈아탄 뒤엔 `hydrateActivePart()`. 저장은 v:2(`parts[]`·`activePart`)이고 applyState가 옛
  v1(루트 melody·lyrics·cellStyles·ornInstrument)을 파트 1개로 승계한다 — 박제된 링크·옛 파일
  때문에 **v1 읽기를 지우지 말 것**.
  - **총보 보기**(#scoreView, 악기 관리 창 머리줄 토글 · CTRL_IDS에 저장): 파트가 둘 이상이고
    켜져 있으면 render()가 **한 각 = 파트 열 묶음**으로 그린다(목록 위 파트가 오른쪽 — 읽는
    방향). 묶음 안 열 사이는 가는 선(T_THIN), 묶음 양쪽 벽만 T_THICK, 묶음 사이는 gakGap —
    사용자 확정(2026-08-08). 파트보(끔·파트 1개)는 P=1로 **똑같은 코드가 예전 경로 그대로**다.
    - 셈의 핵심은 `gakW = P*cell + lyOnCount*lyExtraFull`(묶음 폭)와 `pgSlot = gakW + pgGap` —
      곁줄은 **내용 있는 파트만** 제 열 오른쪽에 붙고(복사 없음), 간격을 파고드는 건 맨
      오른쪽 파트(partLyOn[0])의 곁줄뿐이다. 장단 자리는 묶음과 같은 폭으로 패딩해 밴드끼리
      열이 맞는다(bandJdExtra = jdExtraFull + (gakW - cell)).
    - 각 번호·각/장 이름·클릭존은 묶음에 한 번(가운데 gakCx), 악기 이름은 밴드마다 맨 오른쪽
      각 위에 — 곡 머리는 파트 `name`(비면 악기→파트N), 둘째 줄부터는 `abbr`(비면 생략,
      피날레 Full/Abbreviated 구분).
    - 드래그 선택·정간 서식·시김새 수정(ornCollect 빗장)은 **활성 파트 열에만** 산다.
      **남의 파트 열을 누르면 그 파트로 갈아타고 누른 칸이 곧장 열린다**(정간=클릭,
      곁줄=더블클릭 — 피날레 감각). cellBoundaryNibbled·collectCellBorderSegs는 styles
      인자로 파트별 서식표를 받는다(안 주면 활성 파트).
    - **들리는 것은 보이는 것과 같다** — **총보면 합주, 파트보면 펴 놓은 그 악기만** 울린다
      (2026-08-14 사용자 요청). 화면엔 한 악기만 있는데 소리는 온 악기가 나면 무엇을 듣고
      있는지 알 수가 없다 — 인쇄·PNG가 '보이는 그대로'인 것과 같은 규칙이다. 갈림길은
      buildAudioEvents의 `inView`이고, 총보 여부는 **`scoreViewOn()` 한 곳**이 답한다
      (그리기·오선보·레이아웃 프로필·재생이 같은 답을 봐야 한다 — 예전엔 같은 식이 네 군데
      복사돼 있었다). 장단은 곡에 하나뿐이라 보기와 무관하게 그대로 울린다(악보에도 늘 그려진다).
      검사는 `node tools/check-play-parts.mjs`.
    - 합주는 addPart가 파트마다 같은 0초에서 출발해 한 시간축에 섞는다(realizeMelody가
      melodyText 인자를 받게 됨). 관리 창의 🔊/🔇(파트 `muted`)는 **두 보기 모두에서**
      소리를 빼고, **하이라이트(marks)는 보이는 악기마다 한 벌**이다(2026-08-16 사용자 제보 —
      총보에서 한 악기에만 표시가 떴다). 세 짝이 같은 '열 번호'로 맞물린다:
      `built.marks[열]`(표식) · `playGeom[열]`(정간 좌표) · `playHi[쪽][열]`(사각형).
      열 번호는 총보면 파트 번호 그대로, 파트보면 0 하나다.
      · **`playGeom`은 `cellGeom`과 다른 물음에 답한다.** cellGeom은 '지금 편집하는 칸이
        어디냐'라 활성 파트 열만 담고(클릭·에디터·둘러보기가 그 뜻으로 쓴다), playGeom은
        '그려진 칸이 어디냐'라 총보면 모든 악기 열을 담는다. 겹쳐 보여도 합치지 말 것.
      · 재생 하이라이트 사각형은 **쪽마다 열 수만큼** 만든다(예전엔 쪽당 하나였다).
      · 음소거된 악기도 표식은 나온다 — 화면엔 있으므로 어디를 지나는지 보여야 한다.
      · 파트마다 각 수가 다를 수 있어 표식을 한 벌로 못 담는다(그래서 벌을 나눈다).
        먼저 끝난 악기는 예전처럼 마지막 정간에 머문다.
      장단 타점은 각 수가 가장 많은 파트(host)에 얹는다. startPlayback의 총
      길이는 built.total(가장 긴 파트 끝) 기준 — 이벤트가 파트 순서로 섞여 마지막 원소가
      끝이 아니다.
    - **인쇄·PNG는 보이는 그대로** — 보기 전환이 곧 '총보로 뽑을지 파트보로 뽑을지'다.
      별도 선택 UI를 만들지 말 것(같은 일을 하는 두 번째 스위치 — 중복 금지 원칙).
    - **보기 전환은 상단바 #viewToggle 메뉴**(배율 왼쪽, `.topbar-cmd`+`.tb-menu`) — 악기가
      둘 이상일 때만 보이고(updateViewMenu), 버튼 글씨가 지금 보기(총보/활성 파트 이름).
      항목: 총보 ── 파트들(누르면 그 파트의 파트보 + 편집 대상). 총보 여부의 정본은 숨은
      `#scoreView` 체크박스(CTRL_IDS라 **DOM에서 지우지 말 것**) — 예전 악기 관리 창 머리줄
      체크박스는 상단바로 일원화하며 뺐다(도로 두 군데 만들지 말 것).
    - **레이아웃은 보기별 두 벌**(app.js '보기별 레이아웃' 절, `viewLayouts`·`syncViewLayout`).
      경계는 탭: **레이아웃 탭(VIEW_LAYOUT_IDS: 배치·크기·간격·머리단·테두리) + 종이 크기·방향
      = 보기별**, 문서 탭(정간 수·총 각 수·대강·제목)·왼쪽 퀵 패널 = 공유(사용자 확정,
      2026-08-08). 스왑은 render() **첫머리**의 syncViewLayout — 컨트롤 읽기보다 앞서야 한다.
      총보 첫 진입 시 한 줄 각 수를 파트 수로 나눠 시딩. 저장은 v2의 `viewLayouts`(옛 문서는
      두 벌이 같은 값에서 출발).
    - **새 문서 마법사에 악기 편성**(#ndParts, 행 = 악기 select + 이름) — 비우면 파트 1개
      무지정(예전 그대로), 둘 이상이면 총보 보기로 시작(applyNewDocAnswers).
      partLabel은 이름→악기→"파트 N" 순서로 부른다(관리 창·보기 메뉴·총보 열 이름 공용).
  - **악기 관리 창(#partsWin, .mgr-win)** — 피날레 Score Manager 자리. 사이드바 '문서' 탭
    [악기 관리…]가 열고, 파트 추가·삭제·이름·악기·순서·'지금 편집할 악기(◉)'를 여기서 한다
    (app.js '악기 관리 창' 절). **도구창(.direct-win)이 아니라 독립 창** — 입력 방식과 무관하게
    떠야 해서 activateDirectPanel·레일 탭·win-toggle 기계에 얽지 않았다(여닫이는 .open 클래스
    하나). 파트 전환은 switchPart → loadActivePartIntoView — 악기를 setOrnInstrument로 태워
    숫자 단축키 번들·팔레트 정렬이 같이 갈아타고, 정간 선택·시김새 선택·에디터 조각을 비운다.
    마지막 파트는 못 지운다(문서는 늘 파트 1개 이상). 목록 순서 = 나중 총보에서 열이 서는 순서.
- **설정 패널은 기본이 닫힘이고 마지막 상태를 기억한다**(`jgb_sidebar_v1`, 2026-08-25 사용자
  요청). 문서를 열었을 때 먼저 보여야 하는 건 설정 폼이 아니라 악보다 — 주소로 받은 남의
  악보(`#v=`)라면 더욱. 기억이 localStorage(다크·테마·오선보 배율과 같은 자리)인 건 그것이
  '이 악보가 어떤 악보인가'가 아니라 '내가 이 브라우저에서 어떻게 보는가'라서다.
  여닫이는 `setSidebarOpen(on, remember)` 한 곳을 지난다.
  - **둘러보기의 '설정' 단계는 닫혀 있어도 잠깐 연다**(`tourEnsureLayoutTab`의 prep) —
    예전엔 닫혀 있으면 단계를 통째로 건너뛰었는데(skipIf), 기본이 닫힘이 되면서 첫 방문자가
    '악보의 짜임을 어디서 바꾸나'를 영영 못 보게 된다. 투어가 끝나면 도로 닫고 **기억은 안
    건드린다**(탭을 되돌리는 것과 같은 자리에서).
- **명령은 상단바, 설정·관리는 사이드바** — 출력 명령의 집을 정한 선. 예전엔 사이드바 '출력'
  탭에 진짜 버튼이 있고 상단바 '더보기'의 m* 항목이 그걸 `.click()`으로 대신 눌러주는 위임
  구조라 같은 명령이 두 군데 있었는데, 상단바로 일원화했다(위임·중복 없음, 배선은 app.js 한 곳).
  - #btnPrint(인쇄)는 **1급 버튼**. 빈도(곡 하나에 한 번)만 보면 아래 규칙상 메뉴행이지만,
    이 앱의 최종 목적지고 위험하지도 않아 '찾기 쉬움'을 빈도보다 앞에 뒀다 — **의도된 예외**니
    규칙대로 메뉴에 도로 넣지 말 것.
  - **파일 메뉴(⋯)는 없앴다**(2026-08-25 사용자 요청) — 이름을 '더보기'에서 '파일'로 바꿔도
    열기 전엔 뭐가 든지 모르는 서랍인 건 그대로였다. 들었던 것은 전부 **오른쪽 세로 레일**로
    풀어 놓았다(아래 '오른쪽 세로 레일' 참고). 되살리지 말 것.
    - 하나만 예외로 **전체 초기화(#btnResetContent)는 설정 › 문서 탭 맨 아래**로 갔다 —
      늘 보이는 자리에 둘 명령이 아니어서다(빈도×위험도). 안전장치는 그대로 둘이다
      (resetAllContent의 confirm + ⌘Z).
  - **링크 공유**(#btnShareLink, app.js '링크 공유' 절): 문서 전체를 브라우저 내장
    CompressionStream("deflate-raw")로 압축해 `#s=1.<base64url>`에 싣는다(우락 전곡 ≈
    2,000자 실측; 외부 라이브러리 금지 원칙 유지). 받는 쪽은 init의 `consumeShareHash`가
    처리 — **?first=1과 같은 규칙**으로 해시를 쓰자마자 replaceState로 떼고(북마크
    새로고침이 편집을 덮지 않게), 작업 중(restored)이면 confirm 후 현재 작업을
    보관함(jgb_snapshots_v1)에 자동 저장하고 교체한다. `1.`은 버전 접두어 — 링크는 글에
    박제되므로 형식을 바꿔도 v1 읽기는 남길 것. `window.jgbShareLink()`는 검증·임베드용.
  - **악보 게시**(#btnPublish, js/cloud.js · 서버는 server/schema.sql): 링크 공유와 같은
    '공유' 짝이되 악보가 주소가 아니라 **서버**에 얹힌다 — 주소가 짧아 메신저에서 안 잘리고,
    올린 뒤에도 같은 주소로 고칠 수 있다(링크 공유의 두 한계). 주소는 `#v=<8자>`.
    경로(/s/…)가 아니라 해시인 것은 GitHub Pages가 경로 리라이트를 못 해서다 — 경로 주소는
    카톡·트위터 공유 카드(OG 태그)가 필요해질 때 호스팅과 함께 옮긴다.
    - **수정 권한은 문서가 아니라 브라우저에 있다.** 게시하면 서버가 토큰을 딱 한 번 주고,
      그 토큰은 localStorage(`jgb_published_v1`)에만 남는다. 문서에 실리는 것은 게시물
      **id뿐**(`pubId`, collectState/applyState) — 그래서 .jgb.json·공유 링크를 남에게 줘도
      수정 권한이 안 넘어가고, 남의 악보를 고쳐 게시하면 새 게시물이 되며 원래 id가
      `fork_of`로 기록된다(4단계 포크 계보가 여기서부터 쌓인다). **토큰을 문서에 넣지 말 것.**
    - id를 문서 상태에 둔 까닭: 새 문서·불러오기·되돌리기·링크 열기가 저마다 따로 챙기지
      않아도 정체성이 문서를 따라다녀 저절로 맞는다. 전체 초기화는 `pubId`를 지운다
      (백지가 된 악보로 남들이 보고 있는 게시물을 덮지 않게).
    - cloud.js는 **app.js를 한 줄도 안 고친다** — `window.jgbDoc`(state·adopt·hasSavedWork·
      title·pubId·setPubId) 여섯 개가 창구이고 그 밖으로 손을 뻗지 말 것. index.html에서
      app.js **뒤에** 싣는다.
    - `js/cloud-config.js`가 비면(또는 file://) 게시 기능이 통째로 꺼지고 버튼도 숨는다 —
      analytics.js의 GOATCOUNTER_CODE와 같은 대기 모드. anonKey는 공개 키라 정적 파일에
      박히는 게 정상이고, 진짜 빗장은 서버에 있다: 테이블 직접 접근을 막고(권한 회수 + RLS
      정책 0개) RPC 넷(publish/update/delete/fetch_score)만 연다. 크기 제한·게시 빈도 제한·
      권한 확인이 전부 그 안에 있다. **없는 id와 틀린 토큰은 같은 오류를 준다**(id를 훑어
      게시물 존재를 알아내지 못하게). IP는 저장하지 않고 소금 섞은 해시만 남긴다.
    - **판(版)은 쌓이고 지워지지 않는다**(`score_versions`, 2026-08-21). 게시·갱신할 때마다
      그때의 문서를 통째로 한 줄 뜬다 — 담는 것은 인자로 받은 문서가 아니라 **표에 실제로
      남은 그것**(`push_version`이 scores를 다시 읽는다)이라, 살아 있는 사본과 판이 어긋날
      길이 없다. 그래서 판 뜨기는 늘 **쓴 다음에** 부른다. 상한은 `version_keep()`(20)이고
      **v1은 프루닝에서 뺀다** — 최초 게시본은 게시자의 것이고 신고 처리의 증거라서.
      내용이 그대로면(jsonb 비교) 판을 안 올린다(공개 설정만 토글할 때 쌓이지 않게).
      thumb은 판에 안 담는다(그림 하나가 200KB).
    - **게시자의 뜻과 운영자의 처분은 다른 칸이다**: `visibility`(공개/주소만)는 올린 사람이
      고른 것이고 `hidden_at`은 운영자가 내린 것이다. 한 칸에 섞으면 숨김을 풀 때 원래 공개
      설정을 잃는다. `fetch_score`·`list_scores`가 `hidden_at`을 뺀다.
      내려간 악보를 `#v=`로 열면 **없는 것과 다르게** 답한다(사유까지 붙여서) — 그 주소는
      이미 나눠 준 것이라 새로 새어 나갈 게 없고, 약관 제6조 ④가 '왜 내려갔는지 확인할 수
      있는 자리'를 약속하고 있어서다.
    - **관리자의 빗장은 둘**: Supabase Auth 로그인 + `admins` 명단(`is_admin()`). 대시보드에서
      신규 가입을 꺼도 명단을 함께 두는 건 빗장 하나는 언젠가 풀린다고 보기 때문이다.
      `admins`는 **로그인한 사람에게도 닫혀 있고**(누가 관리자인지 떠보는 창구가 된다),
      `is_admin`·`push_version`도 anon/authenticated에 안 연다.
      운영 기록 `admin_log`의 `score_id`엔 **외래키를 안 건다** — 게시물을 지운 기록이야말로
      남아야 하는데 외래키가 있으면 함께 끌려간다.
    - **관리자 RPC는 익명 RPC 옆에 따로 선다**(`admin_*` 11개 + 안에서만 쓰는 `require_admin`·
      `admin_note`). `update_score`에 '관리자면 토큰 없이도'를 끼워 넣지 말 것 — 그 함수의
      계약이 둘이 되어 읽는 사람이 매번 어느 쪽 이야기인지 가려야 한다. 익명 RPC의 열쇠는
      **토큰**이고 관리자 RPC의 열쇠는 **로그인+명단**이다.
      `authenticated`에만 grant하고, 그래도 함수마다 첫 줄에서 `require_admin()`을 다시
      묻는다(여는 자리와 확인하는 자리가 떨어져 있으면 언젠가 한쪽만 고쳐진다).
      · `admin_save_score`·`admin_set_hidden`·`admin_delete_score`는 **사유/메모를 안 적으면
        막는다** — 되돌릴 근거이자 약관이 약속하는 기록이라서.
      · 판 번호는 '몇 번 눌렀나'가 아니라 '내용이 몇 번 달라졌나'다(jsonb 비교로 같으면 안 올림).
      · `admin_get_score`는 조회수를 **안 올린다**(운영자가 들여다본 것은 사람들이 본 것이 아니다).
      · **SQL Editor에는 `auth.uid()`가 없어** 관리자 RPC가 그냥은 막힌다. 한 트랜잭션 안에서
        `set_config('request.jwt.claims', …, true)`로 신분을 빌린다 — 절차는
        `docs/관리자-운영.md` 4절.
    - **운영자가 고친 판이면 여는 순간 배너로 알린다**(`fetch_score`의 `admin_edit` →
      cloud.js `showBanner`). 약관 제5조 제6항이 게시자에게 '교정 기록의 열람과 원상회복을
      요구할 권리'를 주는데 **고쳐진 줄을 모르면 쓸 수 없는 권리**라, 제 악보를 여는 순간
      보이게 하는 것이 가장 빠른 알림이다.
      · 서버는 **마지막 판이 운영자 것일 때만** 보낸다 — 그 뒤 게시자가 스스로 갱신했으면
        굳이 알리지 않는다.
      · 메모가 스스로 무엇을 고쳤는지 말하므로 배너에서 되풀이하지 않는다('표기를 교정한 판'
        + '표기 자동 교정 —'이 겹쳐 읽혔다). 메모 끝의 도구 이름(`(tools/…)`)은 게시자에게
        쓸모가 없어 뗀다.
      · 이때는 **내 악보라도 [문의] 링크를 살려 둔다** — 원상회복을 요구할 길이 있어야 한다.
    - **약관 개정 공지는 게시 창의 권리 확인 안내에 한 줄**이다(제10조 제2항의 '서비스 화면을
      통한 공지'). 약관이 실제로 매이는 행위가 게시이고 거기 이미 약관으로 가는 길이 있어서다.
      **조항이 또 바뀌면 index.html의 그 날짜와 문구를 함께 고칠 것.**
    - 받는 쪽(`consumeScoreHash`)은 링크 열기와 **같은 절차**를 탄다 — app.js의
      `adoptState()`(작업 중이면 confirm + 보관함 자동 저장 후 교체)를 함께 쓴다. 출처가
      늘어도 '남의 악보가 내 작업을 조용히 덮지 않는다'는 약속은 한 곳에만 있어야 한다.
    - 주소에 악보가 실려 오면(`#s=`·`#v=`) init의 `incomingDoc`이 **새 문서 마법사와 환영
      카드를 막는다** — 악보가 들어오는 건 한 박자 뒤라, 그 사이 마법사에서 [만들기]를
      누르면 방금 받은 악보의 제목·정간 수·각 수가 덮인다.
  - **관리 화면**(admin.html, js/admin.js): 올라온 악보를 훑고·내리고·문서를 받아 오는 자리.
    - **로그인은 Supabase Auth를 fetch로 직접 부른다**(supabase-js를 안 들이는 무의존 원칙):
      `/auth/v1/token?grant_type=password` → access_token을 `Authorization: Bearer`로 실어
      admin_* RPC를 부른다. 만료 60초 전에 refresh_token으로 갈아 끼운다.
    - 세션은 **sessionStorage**(`jgb_admin_v1`)다 — localStorage가 아니다. 관리 열쇠는 오래
      두지 않는다(창을 닫으면 풀린다). 게시 토큰(`jgb_published_v1`)이 localStorage인 것과
      일부러 다르다: 그건 내 악보의 열쇠고 이건 남의 악보에까지 닿는 열쇠다.
    - **화면을 열어 준다고 권한이 늘지 않는다.** 로그인 직후 `admin_me()`로 서버에 먼저 묻고
      나서 목록을 띄운다 — 먼저 띄우고 물으면 관리자가 아닌 사람에게 잠깐 관리 화면이 보인다.
    - **내려간 악보는 [열기]가 꺼진다**(fetch_score가 막으므로). 대신 **[문서 받기]**가
      admin_get_score로 .jgb.json을 내려 준다 — 편집기에서 불러오면 문법 오류가 빨간 바탕으로
      보이므로, 고치는 길의 첫 걸음이 이것이다(⑤ 관리자 열기가 붙으면 [열기]가 살아난다).
    - **판과 운영 기록은 목록의 판 번호(v3)를 눌러 연다** — 할 일 열에 버튼을 더 붙이지 않은
      것은 거기가 이미 셋이라 좁은 창에서 가장 먼저 잘리는 자리이고, 판을 보고 싶을 때 눈이
      가는 곳이 마침 그 숫자여서다. 판 하나는 표의 한 줄이 아니라 **두 줄짜리 덩이**다:
      길이를 모르는 값(메모)에는 제 줄을 준다 — 5칸 표로 짰더니 500px 창에서 그 칸만 53px로
      뭉개졌다(2026-08-21 실측). **되돌리기는 두 걸음**이다(사유 줄이 펼쳐지고 거기서 한 번 더)
      — 지금 판을 덮는 일이라 [내리기]·[지우기]와 같은 무게로 다룬다.
    - `v2로` / `v3으로` — **조사는 숫자를 읽은 소리의 받침이 정한다**(3·6과 0으로 끝나는 수만
      '으로'). js/admin.js의 `roFor`와 schema.sql의 `admin_restore`가 **같은 셈**을 쓴다:
      화면에 보이는 말과 기록에 적히는 말이 어긋나면 안 된다.
    - **관리자로 편집기 열기는 `index.html#va=<id>`**(목록의 [열기] → cloud.js의
      `consumeAdminHash`). `#v=`와 다른 점 셋: admin_get_score를 타므로 **내려간 악보도 열리고**
      조회수가 안 오르며, 게시자 토큰 없이 같은 자리에 되쓸 수 있다. 그 창의 게시 창은
      **[관리자 갱신] 하나만** 남기고 지은이·이용 조건·[게시]를 숨긴다 — 남의 악보를 열어 둔
      채 [게시]를 누르면 베낀 새 게시물이 하나 더 생긴다. 되쓰기(`admin_save_score`)는 **메모가
      필수**다(약관 제5조 제5·6항).
      · 갈림길은 `adminMode()` 한 곳이고, `adminEditing`만 보지 않고 **`pubId`가 그대로인지
        함께 본다** — 새 문서·다른 악보를 열면 엉뚱한 악보를 남의 자리에 되쓸 수 있다.
      · 관리자 모드에서 **숨긴 것은 여느 모드에서 되살려야 한다**(창은 한 번 만들어 두고
        여닫으므로). 열쇠 안내(#pubKeyNote)가 그래서 한 번 영영 사라졌었다.
      · 새 형식을 더할 땐 app.js `incomingDoc`의 `/^#(s|va?)=/`도 함께 — 안 늘리면 새 문서
        마법사가 떠서 방금 받은 악보를 덮는다.
    - **세션·토큰 갱신·RPC는 `js/admin-session.js`(window.jgbAdmin)에 한 벌만** 있다.
      admin.html과 index.html이 **같은 파일**을 싣는다(index.html에서는 cloud.js보다 먼저) —
      두 벌 적으면 한쪽만 고쳐지는 날이 온다. 로그인한 적이 없으면 아무 일도 안 하는 대기 모드.
      · **sessionStorage는 새 탭에 저절로 안 따라간다.** 2026-08-21 크롬 실측:
        `<a target=_blank>`는 rel 유무와 무관하게 **안 넘어가고**(요즘 크롬은 암묵적 noopener),
        `window.open(url,"_blank")`만 넘어간다. admin.html의 [열기]가 링크가 아니라
        window.open인 까닭이 이것이니 `<a>`로 되돌리지 말 것.
    - **페이지가 뜨자마자 하는 말은 `alert`가 아니라 배너로.** 알림창은 눌러 없애기 전까지
      렌더러를 통째로 막아 악보가 그려지는 것조차 못 본다(`#va=`로 왔는데 세션이 없을 때가
      그 경우다). 버튼을 눌러 생긴 일은 alert 그대로 둔다.
    - 내린 뒤 **중단 내역에 붙일 `<tr>`을 만들어 준다**(약관 제6조 제4항). notices.html은 손으로
      적는 정적 표라는 판단은 그대로고, 적을 글자만 여기서 만든다 — **서버는 HTML을 모른다.**
    - 꼴은 **온보딩 네 화면의 어휘**(각진 모서리·1px 선·강조색 한 점·mono 메타)를 쓴다. 새로
      만드는 문이라 되돌릴 옛 어휘가 없고, 표는 각진 모서리에서 줄이 맞으며, 위험한 버튼이
      [내리기] 하나뿐이라 '강조색 한 점'이 곧 안전장치가 된다. 이걸 근거로 상단바·팔레트까지
      넓히지 말 것.
    - **입력칸의 테두리는 admin.css가 직접 준다.** styles.css는 폼 컨트롤의 바탕·글자색만
      전역으로 주고 테두리는 `.field input[type=…]`(사이드바 맥락)에서만 준다 — 안 주면
      브라우저 기본 테두리가 남는다(browse.css의 `.sc-search`도 같은 까닭).
    - **할 일 열은 오른쪽 붙박이**(sticky)이고, 가른 선은 `border`가 아니라 **inset box-shadow**다.
      `border-collapse` 표에서 셀 테두리는 셀 배경보다 **아래에** 그려져, 배경이 불투명한
      붙박이 칸은 제 테두리를 스스로 덮는다(반투명한 줄에서만 비쳐 보였다 — 2026-08-21 실측).
  - **문법 검사 결과는 올릴 때 브라우저가 재서 함께 보낸다**(`publish_score`·`update_score`·
    `admin_save_score`의 `p_lint`). 서버는 악보를 못 읽으므로(schema.sql 뼈대 ①) 재는 일은
    브라우저가 하고 숫자만 간다 — 게시 창이 어차피 그 값을 띄우고 있어서 공짜다. 세는 자는
    `window.jgbDoc.badCount()`이고 그 안은 선율 에디터가 빨간 바탕을 깔 때 쓰는 함수 그대로다.
    · **믿을 수 있는 값은 아니다** — 마음먹으면 0을 보낼 수 있다. 운영자가 볼 힌트지 빗장이
      아니고, `tools/scan-published.mjs`가 언제든 덮어쓴다. 안 보내면(null) 서버가 안 적는다.
    · **규칙이 늘면 예전 숫자는 낡는다**(칸 나눔 규칙을 들이며 실제로 그랬다) — 그때는 스캔을
      한 번 다시 돌린다.
  - **모아보기**(#btnBrowse → browse.html, js/browse.js): 공개로 올라온 악보를 카드 격자로
    보여주는 **별개 페이지**. 카드를 누르면 `index.html#v=<id>`로 넘어가고 거기서 cloud.js가
    받아 연다 — 목록과 편집기 사이의 약속은 그 주소 하나뿐이다.
    - **탭 둘**(.sc-tabs): '올라온 악보'(사람들 것) | '국악원 정악보'(국악원 OMR 데이터셋 69곡,
      tools/publish-ngc-omr.mjs로 게시). 가르는 열쇠는 **지은이 한 값**("국립국악원 (OMR)" —
      browse.js `NGC_AUTHOR` = 게시 스크립트 `AUTHOR`, 글자까지 같아야 한다)이고 서버
      list_scores의 `p_author`/`p_author_not`(정확 일치)이 거른다 — 69곡을 최신순에 섞으면
      사람들 악보가 묻혀서 선반을 갈랐다. 데이터셋이 CC BY-NC-SA 4.0이라 라이선스 값
      `cc-by-nc-sa`를 서버 제약·LICENSE_KO(browse.js·cloud.js)·#pubLicense에 함께 열었고,
      출처(논문 인용)는 카드마다가 아니라 **탭 안내문(#scLeadNgc)에 한 번** 적는다.
      수정 열쇠는 `server/ngc-tokens.local.json`(커밋 금지) — 일괄 비공개/공개 전환은
      `node tools/publish-ngc-omr.mjs visibility unlisted|public`.
    - 게시 창의 **[모아보기 목록에 올리기]가 기본 켬**(visibility=public). 끄면 예전처럼
      주소를 아는 사람만 보는 unlisted가 된다. 여기는 나눠 보는 곳이라 기본값이 공개다.
    - **미리보기 그림은 브라우저가 만든다**(`makeThumb`, 게시할 때 첫 장 SVG → 300px 캔버스
      → PNG data URI). 서버는 악보를 그릴 줄 모르는데 브라우저는 이미 그려 놓았으므로,
      서버 렌더러가 통째로 필요 없다. 못 떠도 게시는 그대로 간다(카드에 그림만 안 나온다).
      그림은 `scores.thumb`에 data URI로 그냥 담는다 — Storage 버킷·업로드 정책을 안 들이려는
      선택이고, 이 앱이 기호·소리를 전부 data URI로 다루는 것과도 결이 같다. 양이 부담스러워
      지면 `thumb_path`(비워 둔 칸)로 옮기면 된다.
    - 목록 RPC `list_scores(sort, q, limit, offset)`는 **doc을 빼고** 보낸다 — 악보 본문까지
      실으면 한 쪽이 수 MB인데 목록에선 쓰지도 않는다. 정렬은 최신순·인기순 둘뿐이고 인덱스도
      그 둘만 뒀다. 검색은 제목·지은이만 본다(선율까지 뒤지면 율명 한 글자가 온 곡에 다 들어
      있어 뜻이 없다).
    - browse.html은 **css/styles.css를 함께 싣는다** — 색 변수(:root·body.dark·body.theme-*)를
      한 곳에 두려는 것. browse.css에서 새 색을 만들지 말 것. 대신 편집기용 전면 판짜임
      (body가 세로 flex·overflow:hidden)은 `body.browse`에서 되돌린다.
    - **편집기로 돌아가는 버튼은 하나(#scBack)이고 이름만 상황에 따라 바뀐다** — 편집 중이던
      악보가 있으면(`jgb_state_v1`) `← 편집기로 돌아가기`, 없으면 `악보 만들기`. 가는 곳은 늘
      index.html로 같다. 처음엔 '악보 만들기' 하나였는데 그건 '새로 만들기'로 읽혀서, 하던
      작업을 이어가려는 사람이 누를 생각을 못 했다(문패를 눌러야 돌아가는 걸 알 길이 없다).
      같은 곳으로 가는 버튼을 둘로 늘리지 말 것 — 중복 금지 원칙은 여기도 그대로다.
    - 문패(#brandBox) 마크업은 index.html에서 **복사해 온 것**이다(로고 PNG·워드마크 SVG 인라인).
      로고를 바꾸면 두 곳을 함께 고쳐야 한다. 베타 배지는 뺐다(app.js가 없어 팝오버가 안 열리고,
      `<a>` 안에 `<button>`은 잘못된 마크업이라).
  - 새 문서(#btnNewDoc)도 **1급 버튼**(#outBox 맨 앞, 새 문서→인쇄→파일 순) — 파일 메뉴
    안(File > New 자리)에 뒀더니 사람들이 못 찾았다(2026-07-18). 인쇄와 같은 '찾기 쉬움'
    예외이고, 중복 금지 원칙대로 메뉴에서는 뺐다. 도로 메뉴에 넣지 말 것.
  - 사이드바 '보관' 탭(**data-tab은 "out"인 채로 둘 것** — 저장된 상태의 activeTab에 이 문자열이
    들어 있어 바꾸면 그 탭 보던 사람이 '문서'로 튕긴다)엔 임시 저장만 남는다. 목록을 띄워놓고
    고르는 관리 UI라 드롭다운에 못 넣어서. #readout(각 너비·페이지 수·A4 맞춤 배율)은
    레이아웃 설정의 결과 보고라 '레이아웃' 탭 맨 위로 옮겼다.
- **사이드바 설명문(.hint)은 항목 이름 옆 ? 안에 접힌다**(app.js `foldSidebarHints`,
  css `.hint-btn`/`.hint.folded`). 값보다 설명이 길어 무엇을 정하는 자리인지가 안 보였다.
  마크업은 그대로 두고(**문구는 index.html의 제자리에서 고친다**) JS가 손잡이만 붙이므로,
  사이드바에 `.hint`를 새로 더하면 배선 없이 같이 접힌다.
  - 붙일 자리는 '이 설명이 누구 것인가'로 정한다: 제 `.field`의 이름표(`:scope > label`) →
    이름표 없는 칸은 바로 위 묶음 머리글(`.sec`). 둘 다 없으면 **접지 않고 그대로 보인다**
    (펼 손잡이가 없으면 글이 영영 안 보인다) — 보관 탭 맨 위 안내가 그 경우다.
  - 그래서 **한 `.hint`에 두 항목 이야기를 함께 적지 말 것**(어느 ? 에 넣어도 나머지가 딴 데
    숨는다). 각 사이·열 사이 간격 안내를 각자의 칸으로 가른 게 그 때문이고, 제 `.field`가
    없던 '인쇄영역 초과 시 자동 축소' 줄은 각 너비 안내에 합쳤다.
  - 도구창의 ?(`makeGuideToggle`/`.melody-guide`)와 달리 **띄우지 않고 제자리에서 편다** —
    #sidebar가 `overflow-y:auto`라 가로도 auto가 되어 떠 있는 패널이 잘리고 스크롤하면 버튼과
    따로 논다. 버튼만 같은 부품(`.icon-btn`)이라 '같은 ?'로 읽힌다.
- **도구창의 ? 안내(`.melody-guide`)는 `position: fixed` + JS 자리잡기**(app.js `placeGuide`).
  도구창들이 `overflow: auto`라(#lyricsArea·#jangdanArea…) absolute로 두면 **창 경계에서
  잘린다** — 폭 240px 상자가 창보다 넓어 오른쪽이 뭉텅 날아갔다(2026-08-11 사용자 제보).
  리본 툴팁(#ribbonTipFloat)이 같은 이유로 이미 쓰던 수법이다.
  - **자리 규칙은 `placeGuide` 한 곳에만 둘 것.** 예전엔 CSS에 `#dockRail .melody-guide`·
    `#shortcutsGuide`의 top/bottom/left 예외가 따로 있어, 자리 규칙이 두 군데로 갈려
    있었다(둘 다 지웠다). `#shortcutsGuide`에 남은 건 높이 제한(`max-height:74vh`)뿐이다.
  - 규칙은 '버튼 아래 왼쪽 맞춤'이 기본이고 ① 화면 오른쪽을 넘으면 안으로 당김 ② 아래가
    모자라면 버튼 위로 뒤집음 ③ 위아래 다 모자라면 넓은 쪽 + `max-height`로 눌러 스크롤.
  - **도구창 끌기가 `left/top`이라서 fixed의 기준이 어긋나지 않는다.** 끌기를 `transform`으로
    바꾸면 그 창이 fixed의 기준 상자가 되어 안내가 엉뚱한 데로 간다 — 함께 볼 것.
  - 열려 있는 동안 `resize`·`scroll`(캡처 단계)로 따라다닌다. fixed라 가만두면 버튼만
    움직인다.
  - 체크박스 항목은 이름표가 `<label class="chk">`라 그 안의 클릭이 체크박스를 뒤집는다 —
    토글 핸들러의 `preventDefault()`+`stopPropagation()`을 빼지 말 것.
  - **버튼이 이름줄을 밀면 안 된다** — 상자(18px)가 글줄(13px ≈ 15.5px)보다 커서 그냥 두면
    ?가 붙은 항목만 3px 내려앉아 옆 항목과 안 맞는다. 위아래 음수 마진(-4px)으로 줄 높이
    셈에서 걷고 `vertical-align:-2px`로 글자 가운데에 앉힌다(실측값). 크기를 바꾸면 둘 다
    다시 잴 것 — 마진 박스가 글줄보다 낮아야 한다.
  - 같은 이유로 **이름줄이 폭을 꽉 채우면 ? 혼자 다음 줄로 떨어진다**. 이름 옆 곁말(`<small>`)이
    길면 줄여야 한다(페이지 채움의 곁말을 줄인 게 그 경우 — 자세한 말은 어차피 ? 안에 있다).
- **저장된 작업이 없는 첫 화면은 8각 한 줄**이다(init의 `if (!restored)`, 새 문서 마법사도
  비워두면 8각). 예전엔 페이지를 꽉 채워(20각·두 줄) 열었는데, 아직 아무것도 정하지 않은
  사람에게 빈 격자가 두 줄이나 깔리면 부담스럽다. 이때 `gakUserSet`을 켜 두는 건 페이지
  채움(capacity)이 이 값을 덮지 않게 하려는 것 — 사용자가 총 각 수를 직접 적은 것과 같은 상태다.
  한 줄에 놓을 각 수 기본이 10이라 8각이면 장단을 켜도(한 각 자리 차지) 한 줄에 들어간다.
- **종이 크기는 고를 수 있다**(사이드바 문서 탭 #paperSize, app.js `PAPERS`·`paperSize()`).
  A4·A5·B5·레터·정사각 + 사용자 지정(#paperW·#paperH, mm). 방향(#orientation)은 그 위에 얹혀
  폭·높이를 뒤집는다. 정간보에 A4가 당연하지 않아서 연 것이다 — 전통 악보는 가로로 긴 첩(帖)이
  많고, 교재·논문 삽화는 A5나 정사각이 알맞다. 짧은 곡이 A4에서 비어 보이는 것도 여기서 풀린다.
  - **인쇄 크기는 app.js가 #pageStyle에 @page와 함께 써 넣는다.** styles.css의 `@media print`에
    210/297mm를 박아 두지 말 것 — 그러면 종이 크기를 바꿔도 인쇄만 A4로 나온다. 용지(@page)와
    종이 요소의 크기가 **한 곳에서** 나와야 어긋나지 않는다.
  - 표준 규격은 @page에 **이름 그대로** 넘긴다(`size: B5 portrait`) — 인쇄 대화상자가 그 용지를
    고르게 하려는 것. mm로 넘기면 브라우저가 맞는 용지를 못 찾아 A4에 축소해 앉히는 일이 생긴다.
    이름이 없는 것(정사각·사용자 지정)만 `size: 210mm 210mm` 꼴로 넘긴다.
  - 폭·높이 칸은 `wireLive`(눈으로 보며 맞추는 값) + 60~600mm. 범위 밖의 '치는 도중 값'은
    wireLive가 한 번, `paperSize()`가 또 한 번 걸러 낸다 — 종이가 5mm가 되면 배치가 터진다.
  - PNG 저장·모아보기 미리보기는 viewBox를 읽으므로 **손댈 것이 없다**(이미 종이 크기와 무관).
- **각마다 정간 수가 다를 수 있다**(#gakBeats, app.js '각별 정간 수' 절 · `beatsAt(gi)`).
  가곡은 곡이 각의 한가운데에서 시작해 한 바퀴 돌아 끝나 첫·끝 각이 짧고(5 + 16×14 + 11),
  수제천·취타는 장이 바뀌는 자리에 짧은 각이 낀다. 기본은 #beats 하나이고 **다른 각만**
  #gakBeats에 `1:5, 16:11` 꼴로 적는다(각 번호는 사람이 세는 1부터) — 각마다 칸을 만들면
  50각짜리 곡에서 목록만 50줄이 된다(사용자 확정, 2026-08-14).
  - **그리기·재생·오선보가 `beatsAt(gi)` 하나를 본다.** 새 코드에서 `$("beats")`를 직접 읽지
    말 것 — 기본값이 필요하면 `defBeats()`다. 정간을 한 줄로 편 번호(복사·붙여넣기)도
    `gi * beats`가 아니라 `gakCellOffset(gi)` 누적으로 센다.
  - **밴드 높이는 그 밴드에서 가장 긴 각이 정한다**(`bandBeatsOf`) — 정간 크기를 고정하고 각
    길이를 다르게 두는 쪽이 실제 악보의 모습이다(밴드 아래끝이 들쭉날쭉해진다). 세로 예산
    (hNeed)도 페이지마다 밴드 높이를 더해 잡는다. 예전의 `bandH = headH + beats*cell` 한 줄이
    이 기능의 급소였다.
  - **길이가 같은 밴드는 예전 그림 그대로** — 밴드 아래 통줄·대강선을 밴드 폭 전체에 한 줄로
    긋는다(`bandUniform`). 섞인 밴드에서만 각별로 끊어 긋는다. 이 갈래를 없애면 기존 악보의
    선이 각 사이 간격에서 끊긴다.
  - **장단 줄은 늘 표준 정간 수**(`defBeats()`) — 곡의 장단이라 첫 각이 짧아도 온전한 한 각이다.
    m 루프의 장단 블록이 `gBeats`·`gBottom`·`gDg`를 제 값으로 가려 쓴다(shadowing).
  - **대강은 '각 번호'가 아니라 '각 길이'가 정한다**(`daegangTextFor(n)`) — 같은 길이의 각은
    저절로 같은 대강을 쓴다. #daegang 문법: 앞머리가 기본 각의 분절이고 `5: 3 2`처럼 길이를
    붙이면 그 길이의 각에만 걸린다. '숫자:'로 안 읽히는 조각은 전부 기본 분절로 본다 —
    옛 문서의 `3,3,3,3`(쉼표로 나눈 숫자)이 그대로 읽혀야 해서다. 안 적은 길이는 DAEGANG_PRESET.
  - **오선보는 각이 곧 마디라 마디마다 박자표가 바뀐다**(각을 더 나눌 수도 있다 — '오선보
    보기' 절 참고) — score에 `measBeats[]`가 실리고,
    MusicXML은 길이가 바뀌는 마디에 `<attributes><time>`을 다시 적는다. 화면 조판(Verovio)은
    그 XML을 그대로 받으므로 저절로 따라온다. staff-view.js(대체 경로)도 마디별 폭·박자표를 그린다.
  - 저장은 controls의 `gakBeats` 한 칸(CTRL_IDS) — 없는 옛 문서는 빈 문자열이라 전부 기본값이다.
    각을 넣고 뺄 때 `shiftGakNames`가 각 이름과 함께 예외 번호도 민다.
  - 검사는 `node tools/check-gak-beats.mjs` — 한 줄로 편 번호·재생 시간·오선보 마디 길이
    **셋이 같은 답을 내는지**를 본다(어긋나면 악보와 소리와 파일이 서로 다른 곡이 된다).
- **제목 칸 폭은 '각 자리 수'로 정한다**(`titleGakFor`, #titleGakWidth: 자동/1~4각).
  세로 칸 제목이 첫 줄에서 먹는 각 자리를 사용자가 정하면 첫 페이지 용량
  `(gakPerRow − titleGak) × stack − jdSlot`이 따라 정해지고, 각 너비는 종이에 맞춰 자동으로
  다시 잡힌다 — 그래서 폭을 mm로 재지 않고 **각 단위로만** 움직인다(격자 정렬 유지).
  `formStructure`(용량)와 `render`(그리기)가 **같은 값**을 써야 페이지가 어긋나지 않으므로
  한 함수로 모았다(예전엔 같은 식이 두 곳에 복사돼 있었다) — 새 조건을 붙일 땐 거기만 고칠 것.
  - **총보에서 '각 한 자리'는 파트 수(+곁줄)만큼 넓은데 제목 칸·장단 칸은 정간 한 칸 폭이다**
    (`titleSlotPlan`, 2026-08-24). 예전엔 제목이 각 한 자리를 밀어내 놓고 그 자리의 1/P만 써서
    **첫 페이지만 그만큼 좁아지고 빈 폭이 왼쪽 끝에 띠로 남았다**(영산회상 군악 5파트 실측:
    15정간칸 중 4칸 = 폭의 27%가 왼쪽 공백). 지금은 그 자리를 셋이 나눠 쓴다:
    ① 제목 칸은 각 한 자리를 **예약**하되 칸 폭은 '정간 한 칸'과 '각 한 자리'의 **가운데**
      (`TITLE_W_MID` 0.5 — 눈으로 고른 값. 각 자리를 다 쓰면 제목 칸이 너무 커 보이고,
      정간 한 칸이면 남는 폭이 간격으로만 몰려 첫 페이지 간격이 뒷페이지의 세 배가 된다).
    ② 그 여유에 장단 줄(정간 한 칸 + 간격)이 들어가면 **장단은 각 한 자리를 따로 안 먹는다** —
      첫 밴드가 각을 하나 잃지 않는다(군악 첫 페이지 3각 → 4각). 조건은
      `titleGak × (gakW − cell) ≥ cell + gap` 하나이고, 여유가 모자라면(2파트 총보·제목 없음)
      예전처럼 각 한 자리를 먹는다. 아래 밴드들은 그 띠를 비워 두는데, 장단 자리가 첫 줄에만
      쓰이는 실제 정악보의 모습 그대로다.
    ③ 그러고도 남는 폭은 **그 밴드의 각 사이 간격들이 고루 나눠 갖는다**(`gap0`, 제목 칸이
      있는 페이지에만). 그래서 첫 페이지 밴드 폭이 뒷페이지와 **정확히** 같아진다.
  - **파트보(P=1·곁줄 없음)는 한 획도 안 바뀐다** — `titleMinW === titleMaxW`라 ①②③이 전부 0이
    된다(옛 문서 전체 SVG 대조로 확인). 제목이 없거나 가로(맨 위) 제목이면 titleGak이 0이라
    역시 그대로다. **총보는 첫 페이지 각 수가 늘 수 있어 쪽 나눔이 달라진다.**
  - 자리 셈이 `formStructure`(용량)와 `render`(그리기) 두 곳에 걸려 있어 ②의 판정만
    `titleSlotPlan`에 두고 ①③은 배율이 정해진 뒤 render가 같은 식으로 잇는다 — 배율은 모두에
    똑같이 곱해져 ②를 안 바꾼다. 파트 열·곁줄 켜짐도 `partColumns` 한 곳에서 받는다.
- 배율 숫자(#zoomVal ▾ → #zoomPop: 100%·세로/가로 맞춤)와 위 파일 메뉴는 `.tb-menu` +
  `wireTopMenu()`(app.js, 재생 설정 팝오버 아래)로 열고닫는다.
  자주 안 쓰는·위험한 명령은 상단바에 늘어놓지 말고 메뉴로 접을 것(빈도×위험도).
  #zoomBar 버튼 규칙은 직계(>)만 — 메뉴 항목은 .tb-menu 문법을 따라야 해서.
- 브랜드(#brandBox)는 margin-right 28px로 도구 무리와 떨어뜨린다(+#topBar gap 16 = 44px).
  바짝 붙으면 문패가 버튼 무리의 첫 항목처럼 읽힌다.
- 상단바 순서는 **같은 일끼리 짝지어** 왼→오른쪽으로 성격이 옮겨간다. 새 버튼을 끼울 땐
  이 짝 안으로 넣을 것(짝 사이를 가르지 말 것 — 예전엔 도움말이 다크와 인쇄 사이에 끼어
  '보기'와 '내보내기'를 갈라놨다):
  문패 │ 입력 방식+? │ 재생·정지·재생설정(듣기) │ 배율·오선보 보기·보기 전환(악보를 어떻게 볼지)
  │ #outBox = 새 문서·인쇄(문서 생명주기)
  - **상단바에는 '이 악보'를 다루는 것만 남는다**(2026-08-25). 문서가 드나드는 길(저장·
    불러오기·PNG·MusicXML·링크·게시)과 앱 자체(모아보기·색상·도움말·설정)는 오른쪽 세로
    레일로 갔다 — 아래 절 참고.
- **오른쪽 세로 레일(#appRail)** — 상단바가 가로로 꽉 차서 성격이 다른 것끼리 섞여 있던 것을
  갈랐다(2026-08-25 사용자 요청). 가르는 잣대는 **'이 악보'인가 '악보 바깥'인가**다.
  위에서 아래로 두 무리이고 사이를 가는 선(.rail-sep)이 가른다:
  ① 문서가 드나드는 길 — 불러오기·저장·**출력**·링크·게시
  ② 앱 자체 — 모아보기·색상·도움말·설정
  - 자리는 `#appRow`의 **맨 오른쪽(order:3)** 이라 설정 사이드바가 열리면 그 **바깥**에 남는다 —
    설정을 여닫는 버튼이 레일에 있으므로 버튼이 패널과 함께 사라지면 안 된다.
  - 버튼 꼴은 상단바와 같은 `.topbar-cmd`(기호 + 아래 작은 글씨) 그대로 — 같은 물건이 자리만
    옮긴 것이라 새 어휘를 만들지 않는다.
  - **레일의 드롭다운(색상)은 `position: fixed`로 떠서** 버튼 왼쪽에 붙는다(자리는
    `wireTopMenu`가 잰다 — 메뉴가 fixed면 그 가지로 간다). **absolute로 두면 안 된다**:
    #appRail은 창이 낮을 때를 위해 `overflow-y: auto`인데, 한 축이 visible이 아니면 다른
    축도 auto가 되어 **상자 밖으로 나간 메뉴가 통째로 잘린다** — 열어도 아무것도 안 보인다
    (2026-08-25 사용자 제보). 상자 좌표(getBoundingClientRect)는 멀쩡해서 **좌표만 재는
    검증으로는 못 잡는다** — `document.elementFromPoint`로 그 자리에 정말 있는지 볼 것.
    도구창 ? 안내(placeGuide)·리본 툴팁(#ribbonTipFloat)이 같은 이유로 이미 쓰던 수법이다.
  - **세로는 가로와 달리 자리가 남아 접을 까닭이 없다** — 상단바의 '자주 안 쓰는 건 메뉴로
    접는다'(빈도×위험도)는 가로 자리를 아끼려는 규칙이라 레일에는 그대로 옮겨오지 않는다.
    다만 **위험한 명령은 레일에도 안 둔다**(전체 초기화가 설정 안쪽으로 간 까닭).
  - **[출력](#outToggle → #outPop)은 종이·그림·오선보 파일로 나가는 길을 한 자리에 모은
    메뉴다**(2026-08-26 사용자 요청). 예전엔 정간보 인쇄(상단바)·PNG·MusicXML(레일)·오선보
    인쇄/PNG/MusicXML과 나란히 인쇄/PNG(오선보 창)로 **10개가 세 자리**에 흩어져 있었고
    MusicXML은 두 자리에 있었다. 묶는 잣대는 **무엇을 뽑나**다: 정간보 / 오선보 / 나란히.
    - **나란히의 좌우·위아래는 이름에서 빼 고르는 칸(#staffSideDir)으로** 옮겼다 — 방향이
      이름에 들어가 명령이 넷이던 것이 둘이 됐다(오선 크기가 이미 그런 칸이라 문법이 있다).
      이 값은 '이번에 어떻게 뽑을까'라 **문서에 저장하지 않는다**(오선 크기는 CTRL_IDS다).
    - **상단바 [인쇄]는 그대로 두고 메뉴에도 [정간보 인쇄]를 둔다** — 이 하나만 일부러 두
      자리에 산다. 가장 흔한 길의 지름길이고 1급 버튼 예외라서다(위 '명령은 상단바' 절).
    - 아이콘은 **저장(↓)과 반대인 ↑** — 둘 다 아래 화살표면 레일에서 구별이 안 된다.
  - `.no-print` — 종이에는 악보만.
  - **색상 메뉴(#screenBox)는 이 레일 ② 무리에 있다** — 예전엔 배율 옆이었다가(둘 다 '화면을
    어떻게 볼지'라고 봐서) 상단바 오른쪽 끝으로, 이제 레일로 왔다. 까닭은 그대로다: 배율·
    오선보 보기가 **이 악보를 보는 방식**인 데 비해 색상은 **앱 전체의 겉모습**이라
    모아보기·도움말·설정과 같은 무리다(2026-08-13 사용자 확정). 배율 옆으로 되돌리지 말 것.
- **문패 옆은 비워 둔다** — 브랜드가 살아야 해서. 예전엔 새 문서·실행취소·다시실행이 문패
  바로 옆에 줄지어 있었는데 다 치웠다. 왼쪽에 남는 건 곡을 쓰기 전에 정하는 '입력 방식'뿐.
  새 명령을 문패 옆에 새로 놓지 말 것.
  - 실행 취소/다시 실행은 **버튼이 없다**. ⌘/Ctrl+Z·⇧Z 단축키뿐(단축키 배선은 '되돌리기'
    절에 그대로 있고 도움말 '단축키' 탭이 안내한다). 빈도로만 보면 1급 버튼감이지만
    문패 옆을 비우는 쪽을 골랐다 — 되살리려면 문패 옆이 아닌 오른쪽에 붙일 것.
  - 새 문서(#btnNewDoc)는 #outBox 1급 버튼(위 '명령은 상단바' 절 참고).
- 상단바 컨트롤은 **예외 없이 .topbar-cmd 한 꼴**(기호 + 아래 작은 글씨). 값이 있는 것(배율,
  입력 방식)은 글씨 자리에 **지금 값**을 보여주고 .tb-menu로 고른다. `<select>`나 세그먼트
  버튼을 새로 들이지 말 것 — 입력 방식이 <select>였을 때 혼자 딴 물건처럼 튀었다.
  기호가 상태를 말해야 하면 다크 토글(.sun/.moon)처럼 CSS로 바꿔치기(#modeToggle의
  .ic-direct/.ic-editor ← body.input-direct). 켜짐/눌림은 `.on` 클래스 + var(--hover) 배경.
- 입력 방식 2가지: 에디터 / 직접 입력. `applyInputMode()`(app.js)가 전환하며
  `body.input-direct` 클래스 하나로 CSS가 갈라진다.
  - 직접 입력: 기능바(#melodyRibbon)가 #main 최상단으로 이동, 도구창(.direct-win)들이
    악보 위에 떠서 한 번에 하나만 열림(`activateDirectPanel`).
  - 에디터: 기능바가 #editorDock 맨 위로 이동, 레일 탭(선율/장단/가사/텍스트/셀 서식)으로 전환.
- **'입력' 그룹은 다섯 버튼이고 차례는 '정간에서 멀어지는 순서'다**:
  `井 율명/시김새`(정간 안) → `傍 곁줄`(정간 바로 옆, 각마다) → `長 장단`(곡에 하나뿐인 별도 줄)
  → `章 빠르기/각/장`(각 위) → `文 텍스트`(종이 위 절대좌표). 예전엔 長이 시김새와 곁줄
  사이에 끼어 '정간 안'과 '정간 옆'을 갈라놨다 — **이 짝 사이에 새 버튼을 끼우지 말 것.**
  - **율명·시김새는 한 버튼·한 창(#paletteCol)**이고 창 머리의 `.pal-views`('율명 | 시김새')로
    갈아 끼운다(`palView`). 둘 다 정간 안에 들어가고 PANEL_DOMAIN에서도 같은 `"mel"`이라
    버튼을 가르면 같은 자리를 두 번 여는 꼴이었다. 에디터 모드는 원래부터 이 꼴이었고
    직접 입력만 `#ornWinWrap`이라는 두 번째 창으로 갈려 있던 것을 없앤 것 — **되살리지 말 것.**
  - `buildPalette()`가 내용을 **갈아 끼운다**(`if (palView === "orn") { buildOrnPalette(wrap); return; }`)
    — 두 팔레트를 이어 붙이는 게 아니라서 창이 길어지지 않는다.
  - `.pal-views`는 **#paletteCol 안**에 있어야 한다. 직접 입력에서 `.melody-head`가 통째로
    숨으므로(`#melodyArea.mel-direct .melody-head{display:none}`) 거기 두면 토글이 사라진다.
  - 시김새 보기 전용 도구는 `.orn-only-tool`(악기·시김새 편집), 그중 **직접 입력에서만** 뜻이
    있는 것은 `.orn-direct-tool`(단축키 변환·#ornAddMapBar)을 한 겹 더 단다. 가두는 규칙은
    `#paletteCol.orn-active .orn-only-tool`(0,1,2,0)을 이겨야 하므로 명시도를 맞출 것.
  - 팔레트 크기는 **보기마다 따로**(`palZoom` / `ornPalZoom`) — 율명 표와 시김새 칩은 적정
    크기가 다르다. 창은 하나뿐이라 `palZoomCur()`가 지금 보기의 값을 골라 입히고,
    **보기를 바꿀 때 `applyPalZoom()`을 다시 불러야** 한다(안 부르면 앞 보기의 배율이 남는다).
- 기능바(.ribbon)는 `flex-wrap: wrap` — 좁으면 줄바꿈, 스크롤 금지(항상 전부 보여야 함).
  - **두 줄이 될 때 어디서 갈릴지는 못 박혀 있다**(`.rb-break`, 2026-08-25 사용자 요청).
    그냥 두면 flex-wrap이 오른쪽 그룹부터 하나씩 떨궈, 폭에 따라 무리가 다르게 갈렸다 —
    폭이 정한 것이지 뜻이 정한 것이 아니다. 지금은 뜻으로 가른다:
    **위 줄 = 무엇을 어디에 적나**(마우스·입력) / **아래 줄 = 악보의 틀과 꼴**(각 삽입/삭제·
    정간 편집·크기). 줄 끝에 홀로 남는 구분선은 함께 숨긴다(`.rb-break-sep`).
  - **기준은 창 폭이 아니라 기능바가 실제로 받는 폭**이라 `@media`로는 못 잡는다 — 기능바는
    #main 안에 있어 설정 패널을 열면 330px가 빠지고 왼쪽 도킹이면 또 다르다. 그래서
    **컨테이너 쿼리**(`container-type: inline-size` + `@container`)를 쓴다. 이 리포에서
    컨테이너 쿼리를 쓰는 유일한 자리다.
  - 문턱 950px은 실측값이다 — 한 줄로 서는 데 드는 폭이 **직접 입력 951px · 에디터 545px**
    (2026-08-25). 에디터는 '입력' 그룹이 없어 이 문턱까지 갈 일이 거의 없고 가더라도 줄을
    가를 짝이 없으므로 **직접 입력에서만** 자리를 못 박는다. 버튼 폭·그룹을 손보면 이 값을
    다시 잴 것.
  - **왼쪽 세로 도킹에는 `container-type`을 주지 않는다** — 거기서는 기능바 폭이 내용에서
    나오므로 인라인 크기 담기를 걸면 폭이 무너진다.
  - 둘째 줄은 `margin-left: 26px`(topctl 24 + gap 2)로 들여쓴다 — 첫 줄만 도킹 버튼이 자리를
    먹어 그냥 두면 아래 줄이 왼쪽으로 튀어나온다(실측: 첫 줄 35px, 둘째 줄 9px).
  - 두 줄로도 안 되는 아주 좁은 폭(≈500px 미만)에서는 예전처럼 자연 줄바꿈이 이어받는다.
  - **가로로 눕힌 큰 버튼은 '문구가 정말 긴 것'에만** 준다(`#ornEditToggle`류). 예전엔
    [내용 지우기](5자)·[정간 서식](4자)도 그 예외 목록에 끼어 있었는데, 둘 다
    [율명/시김새](6자)보다 짧아 예외를 받을 까닭이 없었다 — 선택자 목록에 딸려 들어간
    것으로 보인다. 그 둘만 가로로 길고(94·83px 대 58px) 강조색 상자라 기능바에서 혼자
    튀었고, 강조색으로 채워진 상자가 상시 둘이나 떠 있어 '지금 눌러야 할 것'이라는 강조색의
    뜻도 흐려졌다(2026-08-25 뺐다). 새 버튼을 그 예외에 더할 땐 라벨이 58px 안에 정말
    안 들어가는지 먼저 재 볼 것.
- **기능바 글자·버튼은 한 단계 크게 잡았다**(2026-07-26, 눈이 편치 않아도 읽히게):
  아이콘 17→19px · 라벨 9.5→11px · 그룹 이름표 9→10.5px · 버튼 폭 52→58px · 숫자칸 11→12.5px.
  줄이지 말 것. 버튼 폭을 바꾸면 **왼쪽 도킹의 줄바꿈 지점**(`body.ribbon-left
  .win-toggle-group .rb-items`의 max-width = 3*폭 + 2*gap)도 같이 고쳐야 한 줄 3개가 유지된다.
  `.rb-cap` 크기는 **한 곳(.rb-cap)에서만** 정한다 — 왼쪽 도킹 규칙이 크기를 다시 적고 있어
  기능바를 키워도 거기만 9.5px에 발이 묶여 있었다(그 규칙은 색·자간만 맡는다).
- 기능바 도킹(직접 입력 전용): #ribbonPosToggle이 위쪽 가로 ↔ 왼쪽 세로 전환
  (`ribbonPos` 상태 + `body.ribbon-left` 클래스, `applyRibbonPos()`). #leftDock 래퍼가
  위쪽 배치에선 display:contents(없는 셈), 왼쪽 도킹에선 세로 열이 된다. 왼쪽 도킹일 때
  `dockDirectWins()`가 열린 도구창(팔레트)을 #melodyRibbon **안**으로 넣고, CSS flex
  `order`(입력 그룹 뒤 형제 order:3, 도구창 order:2)로 '입력' 그룹 바로 아래에 오게 한다.
  도킹된 창은 카드 스타일을 벗고(배경·테두리 없음) 기능바와 한 몸처럼 보인다. 닫히거나
  위쪽 배치로 돌아가면 placeholder 주석 노드로 원위치 복원.
- 셀 서식(#cellStyleWin — **UI 표기는 '정간 서식'**으로 통일했다. 리본 버튼·레일 탭·창 제목·
  툴팁·confirm 문구가 대상이고 코드 식별자 cellStyle*은 그대로)은 섹션 **셋**: ① **배경색** — '색상'
  라벨 + 색판(#cellStyleColorPicker로 색만 고르고) + [칠하기](#cellFillPaintToggle이 칠함). 방향 줄과
  같은 .cs-row(flex)라 한 줄에 선다. ② **정간** — 합치기/나누기(#cellMergeBtn/#cellUnmergeBtn) ·
  없애기(#cellEraseBtn). ③ **가로줄** — 모양(#cellBorderShapeThick/Dashed/Double)을 방향
  토글(#cellBorderSideTop/Bottom)이 가리키는 줄에. \+ 맨 아래 #cellStyleResetBtn(초기화)은 배경색과
  **네 변 전부**를 함께 지운다(위/아래 토글 무시) — 버튼 아래 .cs-foot-note('배경색·가로줄을 함께
  지웁니다')로 그걸 상시 적어 둔다(툴팁에만 있으면 놓쳐서). 예전엔 배경색이 '정간' 섹션 안에
  합치기·없애기와 한 덩어리였는데(2026-07-20 분리), '칠하는 일'과 '합치고 지우는 일'은 성격이 달라
  제목부터 갈랐다. 라벨은 '배경색' 버튼→'칠하기', 색판 앞 '색상'.
  - 넷 다 sidesForCellInRange의 mode 하나로 갈린다: 합치기/나누기="inner"(구간 안쪽 가로줄만),
    없애기="erase"(좌우 벽 + 안쪽 가로줄), 초기화="all"(네 변), 모양="sides"(방향 토글).
  - **없애기**는 합치기에 좌우까지 더한 것 — 구간이 각에서 도려내진 빈 자리가 된다. 구간의 바깥
    가로줄(첫 칸의 위·끝 칸의 아래)은 **일부러 남긴다**: 그건 위아래 이웃 정간과 공유하는 선이라
    같이 지우면 남의 칸이 열린다. 뼈대(통줄·대강선)는 structuralSegs가 마스크 **뒤에** 다시 그어
    살아남는다(합치기와 같음).
  - 그 '남기는 가로줄'은 **cellBoundaryNibbled()가 골라 structuralSegs에 얹어야** 온전히 남는다.
    없애기의 좌우 마스크는 구간 바깥 경계 y에서 butt cap으로 끝나는데, 선은 그 y가 *가운데*라
    반쪽이 마스크에 물린다 → 양 끝만 반 굵기로 남아 '가는 줄 + 가운데만 굵은 줄' 두 줄로 보였다.
    대강선·통줄은 이미 structuralSegs라 멀쩡했고 평범한 가로줄(T_THIN)만 한 번 긋고 말아서 난 문제.
    마스크를 y로 들여 피하려 들지 말 것 — 경계마다 선 굵기가 달라(T_THIN/T_DAEGANG/T_FRAME)
    들일 양이 하나로 안 정해지고, 각 세로선(T_THICK)이 가로줄 캡보다 넓어 밑에 안 숨는 혹이 남는다.
  - 예전엔 프리셋 3(전체/바깥쪽/안쪽) + 변 4 + 굵기 3 + 종류 4 = 16개 노브였다. 정간보는
    '열이 하나뿐인 표'라 좌우는 각의 벽이고 칸마다 따로 손볼 일이 없어서 UI에서 뺐다 —
    **되살리지 말 것**(없애기는 좌우를 '지우기'만 하지 만들지 않는다). 굵기×종류 조합도
    CELL_BORDER_SHAPES 3개로 고정했다.
  - CELL_BORDER_WIDTH_PX의 굵기는 **T_THICK(0.32)을 넘기지 말 것**. line()이 square cap이라 선은
    양 끝에서 굵기의 절반만큼 더 나가는데 각 세로선(T_THICK)의 바깥 모서리도 딱 T_THICK/2라,
    굵기 = T_THICK이면 캡 끝이 세로선 바깥 모서리와 정확히 맞고 더 굵으면 그만큼 삐져나온다
    (thick이 0.6이던 시절 0.14가 튀어나와 '세로선을 살짝 벗어난다'는 말이 나왔다).
    thick = T_THICK인 건 밴드 위/아래 **통줄과 같은 두께**여서이기도 하다 — [굵게]가 악보에 원래
    있는 굵은 가로줄처럼 보이게 한 것이니, 눈에 띄게 하겠다고 도로 올리지 말 것.
  - **데이터 모델(border[변]={width,style})은 그대로**라 옛 파일은 그대로 열리고 그려진다.
    UI가 좌우를 못 만들 뿐이고, 옛 좌우 테두리를 걷을 길이 [초기화](mode "all")이다.
  - 지우개는 **초기화 하나뿐**이다. 예전엔 '색 지우기'와 '기본'(테두리 지우기)이 따로였는데,
    '이 칸에 해둔 걸 물리고 싶다'는 한 가지 마음에 버튼이 둘이라 매번 어느 쪽인지 생각해야 했다
    — 도로 가르지 말 것.
  - 버튼은 전부 즉시 실행 — 악보에서 정간을 드래그로 먼저 고른 뒤 누른다(칠하기 모드 아님).
    선택이 없으면 MEL_SEL_BTN_IDS로 비활성화된다(방향 토글은 선택과 무관하니 그 목록에서 뺌).
  - 창 레이아웃은 **폭 하나(--cs-w)로 묶인 3칸 그리드**(.cs-grid) — 버튼이 제 글자 길이대로
    늘어나던 flex-wrap을 바꾼 것이라, 줄이 달라도 버튼 모서리가 같은 세로선에 선다. 새 버튼을
    끼울 때 이 그리드 밖에 놓지 말 것. 아이콘의 `.ghost`(var(--muted))는 '사라지는/안 건드리는
    선', currentColor는 '남는 선' — #cdc5b4로 박아두면 다크에서 둘의 밝기가 붙어 뭉갠다.
- **정간 오른쪽 줄의 이름은 '곁줄'(리본 傍)** — 예전엔 '가사/활'이었다. 내용으로 이름을 지으면
  들어오는 것이 늘 때마다 이름을 고쳐야 한다(가사 → 가사/활 → 가사/활/구음 → …). **위치로
  지으면 다시 안 고쳐도 된다** — 정간 안이 '무엇을 소리내나'라면 곁줄은 '그 소리에 곁들이는
  것'(부르는 말·손이 하는 일·함께 치는 북)이다. 코드 식별자(lyrics*·lyricsArea·CELL_EDIT.ly)는
  그대로 두었다. 창 머리엔 이름(곁줄) 옆에 작은 부제 '가사·활·구음'(.mh-sub)을 달아 찾을 수 있게 했다.
- **곁줄엔 켜고 끄는 스위치가 없다** — `lyricsLaneOn()`이 상태를 계산한다: 내용이 있으면 늘
  나오고(인쇄·PNG 포함), 내용이 없어도 **곁줄 창이 열려 있으면 화면에 빈 줄이 보인다**(더블클릭해
  첫 글자를 넣을 자리가 생기게 — 章 이름이 章 창 열림에만 반응하는 것과 같은 규칙).
  예전 `#wantLyrics` 체크박스는 '쓸까 말까'를 미리 정하게 하는 물음이라 없앴고, CTRL_IDS에서도
  뺐다(옛 저장분의 `controls.wantLyrics`는 조용히 무시된다). **각 하나를 통째로 차지하는 장단만
  스위치를 그대로 둔다** — 그건 정말로 정할 일이 있어서.
  - 창 여닫이가 레이아웃을 바꾸므로 `activateDirectPanel`(직접 입력)과 레일 탭(에디터)에서
    **열림 상태가 바뀌면 render()** 를 부른다. 안 부르면 빈 곁줄이 안 나타난다.
  - 초록 점(레이어 사용 표시)은 `body.has-lyrics` = **내용 있음만** 본다. `want-lyrics`(=줄이
    나와 있음)로 켜면 창만 열어도 점이 켜져 뜻이 흐려진다.
  - 곁줄이 **아직 없을 때도**(내용 없음 + 창 닫힘) 정간 오른쪽 틈에 더블클릭 진입로를 둔다
    (drawCell 옆 `wantLyrics` else 가지). 정간은 격자가 늘 그려져 있어 아무 데나 눌러도 열리는데
    곁줄만 없으면 누를 데조차 없어, 처음 쓰는 사람은 리본에서 켜야 한다는 걸 알 길이 없었다.
    켜는 길은 모드마다 다르다(`lyricsWinOpen`이 둘 다 본다): 에디터는 레일 탭 `.active`
    (setCursor(focusTa=true)가 눌러 준다), 직접 입력은 뜬 창 `.win-open`(activateDirectPanel).
    **에디터에서 activateDirectPanel을 부르지 말 것** — `.win-open`이 새어 든다.
  - 곁줄은 **더블클릭**으로 편집한다(정간 드래그 선택과 안 헷갈리게) — 예전부터 그랬고 그대로다.
    그래서 **시김새 수정 모드에서도 곁줄 더블클릭은 살아 있다**: 모드를 끄고(exitOrnEditMode)
    그대로 칸을 연다. 선율·장단은 한 번 누르는 것이 곧 '고른 걸 놓기'라 그 자리에서 멈추는 게
    맞지만(그쪽 mousedown의 `if (ornEditMode) … return`은 그대로 둘 것), 곁줄은 더블클릭이라야
    열리므로 '여기에 글자를 넣겠다'는 뜻 말고 될 수가 없다. 예전엔 곁줄도 똑같이 멈춰서 아무
    반응이 없었고, 모드를 켜둔 걸 잊은 사람에겐 곁줄이 고장 난 것처럼 보였다.
- **장단 프리셋**(장단 창 머리줄 `#jangdanPreset` · 목록은 `js/jangdan-presets.js` ·
  app.js '장단 프리셋' 절): 정악 장단을 골라 넣는다. 목록은 국립국악원 『양금정악보』(2021)의
  곡별 기본 장단을 **그 정간보 그대로** 옮긴 것이고(2026-08-21 판독), 그 책 13쪽 「장구
  악상기호」의 구음 일곱이 이 앱의 사전과 같은 체계라 이름을 바꿔 붙일 것이 없었다.
  - **고르면 정간 수·대강·장단 줄 셋이 함께 바뀐다**(사용자 확정) — 장단은 곧 각의 틀이라
    (20정간 여민락 · 16정간 가곡 · 12정간 취타 · 6정간 도드리) 장단만 갈아 끼우면 칸이 안
    맞아 잘리거나 남는다. 대강도 그 악보에 굵은 세로선으로 그어진 그대로다(여민락 6·4·4·6,
    취타 3·3·3·3, 가곡 11·5 — DAEGANG_PRESET과 같은 값이 나오는 것은 둘 다 같은 관행을
    적었기 때문이다).
  - **선율이 이미 있고 정간 수가 달라지면 한 번 묻는다.** 빈 악보에서는 안 묻는다 —
    물어야 할 것이 없다.
  - **칸은 고른 뒤 '선택'으로 되돌아간다.** 명령을 내리는 칸이지 상태를 보여주는 칸이
    아니다 — 고른 뒤 손으로 고칠 수 있으므로 이름이 남아 있으면 거짓말이 된다.
  - **고르는 자리는 여기 하나뿐이다**(사용자 확정) — 새 문서 마법사에 또 두지 말 것(중복 금지).
  - 출처는 칸 아래 한 줄(`.jd-source`)에 적는다 — 장단이라는 리듬형 자체는 전통 음악의
    공유 재산이지만 어느 자료를 보고 옮겼는지는 밝히는 것이 맞다.
  - **곡마다 한 항목씩 둔다 — 장단이 서로 같아도 그렇다**(상령산·중령산·보허사가 같은 꼴이고
    타령·군악·계면가락도드리도 그렇다). 사람은 '이 장단'이 아니라 **'이 곡'** 을 찾아 고른다.
    같은 꼴은 파일 위쪽 상수(`령산`·`잔령산`·`도드리`·`타령`) 하나를 함께 보므로, 옮겨 적다
    한쪽만 틀리는 일은 안 생긴다.
  - 모음곡은 `group`으로 묶어 보여준다(HTML optgroup) — 현악영산회상 아홉 곡처럼 각이
    20 → 10 → 6 → 12정간으로 오가는 모음곡을 한 줄로 늘어놓으면 어느 악장인지 안 보인다.
    `group`이 없는 것은 묶지 않고 목록 맨 아래에 선다(배열 순서가 곧 칸의 차례다).
  - **도드리류(상현·하현·염불)는 곡 중간에 장단이 바뀐다** — 악보에 각마다 장단이 다시
    그려져 있고 꼴이 달라진다. 여기 담은 것은 그 곡의 **첫 각** 장단이다.
  - 늘리거나 고칠 땐 `js/jangdan-presets.js` 배열만 손대면 된다.
    검사는 `node tools/check-jangdan-presets.mjs`.
- 곁줄 팔레트는 묶음 둘 — `기호`(활·시김새에서 빌려온 것)와 `장구`(구음 7종). 사전의
  `cat`으로 갈린다. 성격이 달라(가락 기호 vs 북 소리) 한 격자에 안 섞는다.
- **기호 팔레트는 세 줄이 빌더 하나를 쓴다** — `buildSymPalette(wrap, lane)` (app.js '기호 팔레트'
  절). `buildOrnPalette`(선율)·`buildLyricSymPal`(가사)·`buildJangdanPalette`(장단)은 이 함수를
  부르는 한 줄짜리 껍데기다. 예전엔 셋이 제각기 다른 함수·다른 칩 클래스(.ornchip / .lsp-btn)여서
  같은 '전성'이 창마다 다르게 생겼고, 기능 하나를 붙이려면 세 번 붙여야 했다. **새 기호 UI는
  이 빌더에 붙일 것** — 한 줄에만 필요한 것도 lane 설정으로 갈리게 두면 나머지 둘이 공짜로 따라온다.
  - 줄마다 다른 건 `SYM_LANES`에 적힌 넷뿐: `domain`(정간 입력 카드의 이름 mel/ly/jd) ·
    `chips()`(무엇을 보여주나) · `groups`(어떤 묶음으로) · `insert()`(누르면 무엇을 넣나).
  - **검색·최근은 칩이 `SYM_RICH_MIN`(12)개를 넘는 줄에만** 붙는다. 그래서 장단(7개)은 예전 그대로
    칩만 한 격자로 나온다 — 한눈에 들어오는 목록에 검색칸을 얹으면 자리만 먹어서. 이 문턱을
    내려 장단에까지 붙이지 말 것.
  - 검색은 표시 이름·파일 이름에 더해 **한글 초성**('ㅁㅈ'→모지)과 **악기 이름**('가야금')으로도
    걸린다(`symSearchHay`). 89개를 눈으로 훑는 게 한계라 넣은 길이니 좁히지 말 것.
  - 머리(`.sym-head` = 문법 안내 + 검색줄)는 한 번 만들면 **다시 만들지 않는다**(`.sym-body`만 새로 그림) —
    다시 만들면 타이핑 중 포커스와 커서가 날아간다. 같은 이유로 '최근'을 쌓을 땐 `.sym-recent`만
    다시 그린다(팔레트를 통째로 새로 그리면 연달아 고를 때 칩이 발밑에서 출렁인다).
  - **검색칸을 누르면 열려 있던 정간 입력 카드가 닫힌다**(포커스가 옮겨가서). 그래서 검색칸
    mousedown 때 편집 중이던 칸을 `symSearchReturn`에 적어 두고, 기호를 고르면 그 칸을 다시 열어
    넣는다. 안 그러면 텍스트 에디터의 엉뚱한 커서 자리에 들어간다. 이 기억은 **한 번만** 쓰이고
    (쓰는 즉시 비움) 다른 정간을 열면 `openCellEditor`가 지운다. 줄이 다르면(장단 칸을 편집하다
    시김새를 고른 경우) 되돌리지 않는다 — 그 칸에 못 넣는 기호라 되살리면 더 엉뚱해진다.
  - 검색칸 바탕·글자·포커스 테두리는 **파일 앞쪽 폼 컨트롤 공통 규칙**에 맡긴다(styles.css).
    `.palette .sym-search-input`에서 다시 칠하면 그 규칙이 더 구체적이라 어차피 지고, 다크모드
    값만 어긋난다(흰 바탕에 밝은 글자가 됐었다).
  - 숫자 단축키 배지는 **`data-stem`이 붙은 칩(=선율 줄)만** 대상(`refreshOrnAddBadges`).
  - **정간 입력 카드가 열려 있으면 칩 클릭은 '골라두기'가 아니라 그 칸 삽입**이다
    (SYM_LANES.mel.insert). 직접 입력은 ornAddMode가 늘 켜져 있어, 예전엔 숫자가 배정된
    칩만 클릭이 골라두기가 되어 편집 중인 칸에 아무것도 안 들어갔다(배정 없는 칩과 동작이
    갈려 고장처럼 보였다, 2026-07-27 수정). 카드가 닫혀 있을 때만 골라두기로 흐른다.
  - 최근 목록(`symRecent`)은 줄별로 상태에 저장된다(`collectState`/`applyState`). 옛 저장분엔
    없으니 없으면 빈 목록으로 시작한다.
- 가사 기호 팔레트를 감싼 `.lyric-sym-pal`에 **max-height/overflow를 주지 말 것** — 창(#lyricsArea)이
  이미 스크롤돼 스크롤 상자가 둘로 겹치면 안쪽 목록이 잘린 채 바깥이 또 밀린다.
- **괄호 문법은 줄마다 다르고, 그 차이를 팔레트가 스스로 말한다.**
  - 기호 토큰은 `{}`·`[]`·`()` **셋 다** 받는다(`SYM_TOKEN_RE`, 짝이 맞는 것만). 넣을 땐 `{}`로
    통일. 자판에 따라 치기 쉬운 괄호가 달라 셋을 다 받는 것이니 하나로 줄이지 말 것.
  - **곁줄은 괄호가 필수** — 글자를 적는 자리라 괄호 없는 `덩`은 그냥 '덩' 글자다.
    **장단 줄은 괄호가 선택** — 전부 구음이라 이름만 써도 되고, 다른 줄 버릇대로 씌워도
    `stripSymBracket`이 벗겨 읽는다. 이 차이는 우연이 아니라 줄의 성격에서 나온 것.
  - 그 차이를 `SYM_LANES[*].syntax`(문구)와 `token(ch)`(실제로 들어가는 글자)로 적어 두고,
    `makeSymHint`가 팔레트 맨 위 한 줄로, `symChip`이 툴팁 `→ {덩}`으로 보여 준다.
    `insert()`도 같은 `token()`을 쓰므로 **툴팁에 보이는 글자와 실제 들어가는 글자가 어긋날 수 없다.**
  - 이 안내는 예전엔 index.html에 하드코딩된 `.orn-syntax-hint` 두 벌이었다(에디터·직접 입력).
    줄마다 문구가 달라야 해서 빌더가 그리게 옮겼으니 **마크업에 도로 박지 말 것.**
- **정간 칸을 편집하기 시작하면 그 줄의 팔레트로 바꿘다**(`focusDomainPanel`, openCellEditor 맨 위).
  곁줄 칸을 여는데 시김새 창이 열려 있으면 거기서 고른 기호가 그 칸에 안 들어가 헛클릭이 된다
  (insert 계열이 cellEditDomain을 본다). 규칙은 '지금 열린 팔레트가 이 칸에 넣을 수 없는 것이면
  바꾼다' — 율명·시김새는 둘 다 선율 줄에 넣으므로(`PANEL_DOMAIN`) **선율을 치면서 시김새 창을
  열어두는 흐름은 안 깨진다**. 章·텍스트·정간 서식 창은 기호를 넣는 창이 아니라 열려 있어도 안 건드린다.
  팔레트 전환이 render()를 부를 수 있으므로 **좌표를 재기 전에** 호출해야 한다.
- 도구창 헤더는 창마다 클래스가 다름: 율명/시김새=`.pal-head`+`.pal-top`, 셀서식=`.pal-top`,
  장단·가사·텍스트=`.melody-head`. 닫기(X) 겹침 방지 padding-right는 이 세 클래스 모두 대상.
- 장단·가사 창의 초기화(+가사 글씨체)는 예전 상단 별도 리본 박스에 있었으나 X와 겹쳐
  머리줄(.melody-head) 오른쪽 끝(`.mh-right`/`.mh-reset`)으로 이전. 끌기 그립도 머리줄 안
  `.bar-grip.dock-panel-grip`으로 옮겨 플로팅 끌기 유지(attachBarDrag가 첫 .bar-grip 사용).
- 각/장(#gakNameArea, 입력 그룹 버튼 라벨은 '章 빠르기/각/장' — 템포 표기가 이 창에 있음을
  이름에 드러낸 것. 가사 버튼도 활 기호 팔레트 때문에 '詞 가사/활'로 같은 취지. UI에서
  '템포'라는 말은 '빠르기'로 통일(재생 설정의 BPM 제외). 코드 식별자는 gakName*·wantTempo* 그대로):
  특정 각 '위'에 붙는 라벨(대여음·1장 등). 한자 표시는 #gakNameHanja 체크(기본 켬).
  데이터는 gakNames(각 번호 0부터 → 원문), 표기만 한자 변환(gakNameDisplay: N장→N章 +
  GAK_NAME_HANJA 사전). 각 삽입/삭제 시 shiftGakNames로 같이 밀림(가사 밀기와 같은 규칙).
  악보에서 각 위 빈 곳 클릭 → 입력 카드(openGakNameCard) — **章 창이 열려 있을 때만** 그 클릭
  영역(cell*1.4 높이의 넓은 띠)을 만든다. 늘 살아 있으면 위쪽 여백·제목 근처를 누를 때마다
  이름 카드가 불쑥 열린다. 이름 글자 자체를 끌거나 누르는 길은 창과 무관하게 살아 있고,
  하이라이트·드래그가 章 창 열림에만 반응하는 규칙과 이제 결이 같다. 도구창엔 번호+이름 목록.
  위 공간이 좁으면 글자 자동 축소(ascent 포함). 인쇄·PNG에 포함(악보 내용, no-print 아님).
  - **빠르기 표기는 최소~최대 범위를 받는다**(`tempoRange`/`tempoLabelText`). 표기의 정본은 章 창의
    두 칸(#tempoBpmGak = 최소, #tempoBpmGakMax = 최대). 최대를 비우면 예전처럼 한 값(`一分・六十井`),
    채우면 `一分・六十～八十井`. 물결표는 **전각(～)** — 반각 ~는 세로쓰기에서 가는 획으로 묻힌다.
    최대가 최소보다 작으면 범위로 안 친다(타이핑 도중에 안 깨지게).
    한자 수사는 **빠르기 전용(`numToHanjaTempo`)** — 국악보 관행대로 백의 자리에서 `百`을 뺀다
    (160 → `一六十`, 180 → `一八十`). 딱 떨어지는 백(100·200)만 `一百`·`二百`으로 남긴다 —
    안 그러면 `一`·`二`만 남아 1·2로 읽힌다. 각/장 이름의 한자 변환(N장→N章)은 **표준 표기**를
    써야 하므로 `numToHanja`는 건드리지 않았다 — 둘을 도로 합치지 말 것.
  - **재생 BPM(#tempoBpm)은 표기와 따로 논다** — 표기의 최소값에서 시작하고, 재생 설정(⚙)에서
    한 번이라도 직접 바꾸면(`tempoBpmUserSet`) 그때부터 표기를 고쳐도 안 끌려간다. 예전엔 둘이 늘
    같은 값이었는데, 표기를 범위로 적는 순간 '재생은 어느 쪽 값인가'가 답이 없어져 끊은 것.
    옛 저장분엔 표기용 칸이 없으므로 applyState가 `tempoBpm`을 표기 최소값으로 승계한다.
  - 템포 표기(一分・N井, #wantTempo)는 같은 자리에 붙지만 각에 소속된 이름이 아니라 곡에
    하나뿐이라 목록 맨 위 **#tempoItem**에 따로 세운다(body.want-tempo로 보임/숨김).
    **크기·자간 모두 각/장 이름과 따로 논다** — 이름은 여럿을 머리줄
    '크기'·'간격'으로 한꺼번에 맞추고 템포는 제 항목(#tempoSize·#tempoSpacing)에서 조절.
    (머리줄의 '간격'은 #gakNameGap = 각/장 이름 것이다. 빠르기와 헷갈리지 말 것.)
  - **빠르기 표기의 위치는 칸이 아니라 드래그로 정한다.** 좌우 #tempoOffX, 상하 #tempoGap
    **둘 다 `hidden`**이고 악보에서 표기를 끌면 값이 써진다. #tempoGap은 이름이 '간격'이어도
    하는 일이 상하 위치라, 칸으로 내놓으면 끌기와 하는 일이 겹치면서 이름까지 헷갈렸다
    (시김새 패널에서 좌우·상하를 뺀 것과 같은 이유). **DOM에서는 지우지 말 것** — 드래그가
    여기에 쓰고, collectState가 CTRL_IDS를 돌며 `$(id).value`를 널 검사 없이 읽는다.
    #tempoGap은 높이 예약(tempoH)과 그리기(drawTempoLabel)가 **같이 쓴다** — 예전엔 예약이
    tempoFont*0.45, 그리기가 gakNameGap이라 서로 어긋나 있었다.
  - **빠르기 표기는 세로를 먹으므로 배율 예산에 든다**(`dTempoH`, 2026-08-24). 예전엔
    `hNeed`가 밴드 높이만 세서, 배율이 정해진 **뒤에** 표기 높이만큼 격자가 아래로 밀렸고
    밀린 만큼 종이 밖으로 나갔다 — 군악 실측 **아래 15.8mm 잘림**(표기를 짧게 써도 5.7mm),
    가로 제목까지 켜면 위로 17.7mm·아래로 22.3mm. 지금은 셋으로 막는다:
    ① **글자 자리를 1.12에서 `TEMPO_LEAD`(1.00)로** 좁혔다(글자 크기 = 글자 자리). 그보다
      촘촘하면 획 많은 글자(八·六·百)가 닿는다(실측). 사용자 자간은 그 위에 얹힌다.
    ② **높이 예약과 그리기가 같은 수**를 쓴다(`TEMPO_F`) — 예전엔 예약 0.42, 그리기 0.38로
      예약이 10% 컸다. 크기도 두 단계 줄였다(0.38 → 0.34 → **0.30**, 2026-08-25 사용자 확정).
      더 줄일 몫은 글자 크기 쪽에 있다 — 자리(`TEMPO_LEAD`)는 1.00이 하한이다.
    ③ 그래도 모자라면 **위 여백을 먼저 빌려** 테두리째 위로 올리고(`TOP_BORROW`, 종이 끝에서
      `TOP_SAFE` 5mm는 남긴다), 여백을 다 빌려도 모자라는 몫만 배율이 격자를 줄인다.
    군악 실측: 정간 10.68 → 9.47mm(−11%)에서 잘림이 사라진다(셋을 안 쓰면 −17%).
    **빠르기·가로 제목을 안 쓰는 문서는 한 획도 안 바뀐다**(전체 SVG 대조로 확인).
  - **테두리 위끝이 빠르기 표기를 가로지르지 않는다**(2026-08-25 사용자 제보). '페이지 채움'
    중간값에서 테두리 선이 표기 글자 한가운데를 지나갔다(채움 50% 실측: 표기 13.4~45.6mm에
    테두리가 26.9mm). 표기도 종이에 찍히는 내용이라 상자 **안**에 들도록 위끝을 올린다 —
    채움 100%가 이미 그렇게 그려지고 있었고 이제 어느 채움값에서나 같다.
    **글자를 줄여서는 못 막는다**: 표기가 짧아지면 테두리도 따라 내려와 여전히 가로지른다(실측).
  - **`verticalText`는 한 글자 자리를 `font*1.12`로 잡는다** — 빠르기 표기는 그보다 촘촘히
    세우므로 그 차이를 자간에 실어 넘긴다(`spV`). verticalText를 고치면 제목·부제·각/장
    이름까지 함께 움직이니 그쪽을 건드리지 말 것.
  - **자간(#tempoSpacing, mm)** 은 그 위에서 더 벌리거나 좁힌다. 글자 크기에 비례하지 않는
    고정 mm라 **남는 높이까지만** 벌어지게 막아 뒀다(spMax) — 안 막으면 표기가 각 위 여백을
    넘어 종이 밖으로 올라간다. 좁히는 쪽(음수)은 넘칠 일이 없어 그대로 통과시킨다.
    **기본값을 음수로 두지 말 것**: 고정 mm라 배율·#tempoSize가 달라지면 촘촘한 정도가
    딴판이 된다 — 조판을 좁히는 일은 비례값인 `TEMPO_LEAD`가 맡는다.
    #tempoItem은 꺼져 있어도 **마크업에서 지우지 말 것** — collectState가 CTRL_IDS를 돌며
    `$(id).value`를 널 검사 없이 읽어 #tempoSize가 없으면 저장이 통째로 터진다.
    높이 예약(tempoFont = cell*TEMPO_F*tempoMul)과 그리기(drawTempoLabel)가 **같은 배율**을
    써야 키운 만큼 진짜 커진다(예약을 안 늘리면 avail에 걸려 잘림). 템포를 키우면 위
    공간을 먹어 cell이 줄어드는 되먹임이 있어 배율만큼 정비례로 커지진 않는다(정상).
- 텍스트(#textArea, 리본 '文 텍스트')는 **章 창과 같은 짜임**: 전역 설정은 머리줄, 항목은
  목록 — 이 두 자리만 쓴다. 본문 #textRow = `.tx-add`(추가 줄) + 안내 한 줄 + #txTitleItem ·
  #txSubItem · #textItemList(자유 텍스트). 제목·부제는 곡에 하나뿐이라 #tempoItem처럼 목록
  **맨 위에 마크업 고정**이고 내용이 비면 render()가 display로 숨긴다 — **지우지 말 것**
  (collectState가 CTRL_IDS의 `$(id).value`를 널 검사 없이 읽어 #titleSize 등이 없으면 저장이
  통째로 터진다). 예전엔 제목·부제·글씨체가 위쪽 `.field` 고정 블록 셋이었는데 블록과 목록의
  시각 언어가 달라 '항목이 늘어나는 흐름'이 위에서 끊겼다(2026-07-25 통합) — 도로 `.field`
  블록으로 빼지 말 것. #textRow에 **높이를 주지 말 것**(옛 `height:140px; overflow-y:auto`는
  목록이 세 줄만 넘어도 제목 줄만 보이게 했다 — #gakNameRow처럼 내용대로 자라야 한다).
  - 제목·부제 **내용의 정본은 설정 › 문서 탭의 #title·#subtitle**이고(render·저장이 읽는 곳),
    목록 항목의 #titleMirror·#subMirror는 양방향 미러다(#tempoBpmGak ↔ #tempoBpm과 같은 규칙).
    미러는 `wireTextMirror`(칸→정본, 정본에 input 되쏘기)·`syncTextMirror`(정본→칸, 포커스 중인
    칸은 건너뜀 — 덮으면 커서가 튀고 조합 중인 한글이 끊긴다) 두 함수가 담당. 정본을 미러 쪽으로
    옮기지 말 것.
- 이름 미상 시김새 sigimsae-00~25는 팔레트에 s00~s08·s10~s18·s20~s25(임시 이름=파일번호)로 등록
  — 정식 이름이 정해지면 사전(js/symbols-registry.js)의 `ko`만 바꾸면 됨(토큰 `{s01}` 꼴도
  같이 바뀜에 유의). 파일 stem(=`id`)은 그대로, 표시 이름·순서만 조정 가능. (파일 09·19는 없음.
  sigimsae-01은 표시명 s01 그대로 — 예전 s11 별칭은 폐기, s11이라는 별도 SVG는 존재한 적 없음.)
  이 SVG들은 원래 viewBox 안 여백이 커 잉크가 박스의 ~45%만 채워 작게 보였음 → assets/symbol_svgs
  원본의 viewBox를 잉크 경계로 크롭(여백 3%)해 다른 기호처럼 꽉 채우게 만듦(다시 다듬으려면
  같은 방식으로 재크롭 후 js/symbols-data.js 재생성). 그래서 크기 보정 코드는 없음.
- 빠르기(tempo) 시김새 5종(assets/symbol_svgs/tempo, 한글 stem)은 사전에 `at:{tempo:1}`로 등록되고
  buildOrnPalette의 세 번째 그룹 "빠르기"에 나온다. **토큰에 공백 금지**(drawCell이 content를 공백으로
  분박 분할하므로 ko=stem 그대로 공백 없이). 렌더는 숨표(<)처럼 칸 배치에서 빼고(rowToks 필터·
  buildAudioEvents 필터) 정간 **바깥 오른쪽**에 세로로 그린다 — `drawSymImageRect`(세로 박스, meet).
  빠르기는 붙임표 시김새 아니라 숫자단축키(ORN_ADD_ALL=wo/both)에서 자연 제외.
  - 자리는 **정간 바로 옆**(cell*0.08)이다. 예전엔 곁줄 폭만큼 더 바깥에 놓았는데, 각 사이
    간격이 각 폭의 2/3뿐이라 곁줄을 켜면 옆 각을 침범했다. 곁줄 글자와 겹칠 수 있지만 그건
    **끌어서 옮겨** 푼다 — 자리를 벌리려고 옆 각을 침범하는 쪽으로 되돌리지 말 것.
  - 다른 시김새와 똑같이 **끌어서 옮기고 크기를 고칠 수 있다**(토큰 `{점점느리게@120,5,-5}`).
    배선을 따로 하지 않았다 — drawCell의 빠르기 블록이 `ornInstances`에 상자를 담기만 하면
    시김새 수정 모드의 점선 하이라이트·클릭·드래그가 전부 딸려온다. **새로 배선하지 말 것.**
    adj의 단위(크기 %, 좌우·상하는 cell 대비 %)는 `drawAdjSym`과 같아야 한다 —
    드래그가 `setOrnPositionAbsolute`로 cell 기준 %를 쓰므로 어긋나면 손끝과 그림이 따로 논다.
- **기호 순번(k)은 그리는 차례가 아니라 원문 등장 차례다.** 시김새를 클릭해 고르면
  `getOrnToken`이 그 정간의 원문에서 괄호 토큰을 앞에서부터 세어 k번째를 찾으므로, 화면
  좌표에서 매기면 어긋난다. 그래서 `drawCell` 맨 위에서 토큰마다 `k`를 미리 박아 두고
  (`tk.sym != null`인 것만 셈) 그리는 쪽은 그 값을 쓰기만 한다. 예전엔 그리는 루프에서
  세었는데, **배치에서 빠지는 빠르기 기호가 번호를 안 먹어** 같은 정간의 뒤 기호들이 한 칸씩
  밀렸다(빠르기를 고르려 하면 옆 시김새가 잡혔다). 칸 배치에서 빼는 기호를 새로 만들 땐
  번호는 그대로 먹어야 한다는 걸 기억할 것.
- 시김새 수정 모드가 그리는 상자(점선 하이라이트·투명 히트)는 **둘 다 `no-print`**. 출력할 때
  모드가 저절로 꺼지지 않으므로, 빠뜨리면 빨간 네모가 인쇄물·PNG에 그대로 찍힌다.
- 시김새 패널(#ornPanel)엔 **크기만 있고 좌우·상하 버튼은 없다** — 위치는 악보에서 기호를
  직접 끌어 옮긴다. 끌기와 하는 일이 똑같은 버튼이라 뺐으니 되살리지 말 것(핸들러의
  `x±`·`y±` 가지도 함께 없앴다. `updateOrnParams`의 dDx·dDy 인자는 '위치·크기 초기화'가 쓴다).
- 그 패널은 **고른 기호 옆**에 뜬다(`placeOrnPanelNearSel`). 예전엔 늘 악보 오른쪽 위 구석이라
  눈은 기호를, 손은 화면 끝을 봐야 했다. 규칙 셋:
  - 기본은 기호 **왼쪽** — 정간보는 오른쪽→왼쪽으로 읽으니 왼쪽이 아직 안 읽은 쪽이라 덜 가린다.
    왼쪽이 좁으면 오른쪽으로 넘기고, 그래도 안 되면 보이는 범위 안으로 당긴다.
  - 자리는 **고를 때 한 번만** 잡는다. 크기 조절·끌기마다 다시 잡으면 손 밑에서 상자가 달아난다.
  - 좌표는 #sheetArea 안 좌표(스크롤량 포함) — 패널의 기준점이 #sheetArea라 그래야 종이에
    붙어 함께 스크롤된다. `hideOrnPanel`이 인라인 left/top/right를 지워 CSS 기본 자리로 되돌린다.
- 가사 특수기호: assets/symbol_svgs/special 8종(가로표·세로표·늘임표·뜰·모지·장지·튕김·연튕김)을
  가사 도구창(#lyricsArea)의 팔레트 #lyricsSymRow(buildLyricSymPal → buildSymPalette의 "lyric" 줄.
  목록·차례는 사전의 `at.lyric`·LYRIC_ORDER)에서 클릭 →
  `insertLyricToken`이 편집 중인 가사 칸/커서에 `{stem}` 토큰 삽입. drawLyricCell이 행 문자열이
  `{stem}`(공백 없어야 한 분박)이고 symURL 있으면 글자 대신 이미지로 그린다. setLyricText는 `{}`를
  안 지워서 토큰이 보존됨. special의 늘임표 stem은 시김새 늘임표(k)→fermata(s)와 별개 키.
- `symURL(key)`는 **SYM_DATA → NOTE_DATA → JANGGU_DATA** 순으로 찾는다. 그림이 세 파일에
  나뉜 건 만들어지는 경로가 달라서일 뿐이라, 쓰는 쪽에선 하나로 보여야 한다 — 장구 구음을
  가사줄에 넣는 것처럼 기호가 제 본디 자리를 벗어나는 순간 여기서 못 찾으면 그림이 사라진다.
  덕분에 선율 정간에 `{덩}`을 쳐도 그려진다(문법 검사도 symURL을 보므로 빨간 표시 안 뜸).
- **드래그해도 글자가 잡히지 않는다** — styles.css 맨 위에서 `body { user-select: none }`.
  이 앱에서 마우스를 끄는 일은 거의 다 '정간 고르기'·'창 옮기기'·'기호 위치 조정'이라, 그때마다
  라벨이나 악보 글자가 파랗게 함께 선택되면 방해만 된다. 되살리는 곳은 **입력칸(input·textarea)과
  읽고 옮길 만한 글(.melody-guide·.hint·.help-pane·#tourBody·.mg-sec·경고문)뿐**. 새 UI를 만들 때
  그 목록에 없으면 선택이 안 되는 게 맞으니, 개별 요소에 `user-select:none`을 또 달지 말 것.
  그림을 끌 때 유령 미리보기가 따라오는 것도 `img, svg image { user-drag: none }`으로 함께 막았다.
- 팔레트 묶음 제목(`.orn-sec`)의 부제(`sub`)는 **선율 줄에만** 쓴다(붙임표=음표 오른쪽에 작게 등
  '어디에 놓이는지'를 알려야 해서). 곁줄의 `기호`·`장구`는 제목만으로 충분해 부제를 뺐다 —
  바로 위 한 줄 안내와 말이 겹쳤다.
- 색은 전부 styles.css `:root`의 역할별 변수(`--bg/--panel/--soft/--hover/--track/--line/
  --ink/--muted/--accent/--accent-soft/--accent-tint/--overlay/--danger*` 등)를 통한다.
  새 색을 하드코딩하지 말고 이 변수를 쓸 것 — 다크모드(`body.dark`, 파일 끝)가 변수 값만
  갈아끼워 동작한다. 예외: 피아노 건반·강조색 위 흰 글자(`color:#fff`)·말풍선은 테마 무관
  고정색. 악보(종이)는 별도 흰색 SVG라 다크에서도 흰 종이(인쇄·PNG 안 바뀜).
  - **밝은 테마 프리셋 3종**: 미색(`:root` 기본, 클래스 없음)·은청색(`body.theme-crystal`)·
    옥색(`body.theme-celadon`, 둘 다 :root 바로 아래). 상단바 **색상 메뉴**(#screenToggle ▾
    → #screenPop, 예전 다크 단독 토글 #darkToggle 자리)에서 테마 3종 + 다크 전환을 고른다 —
    아이콘(해/달)은 지금 모드, 버튼 글씨는 **'색상' 고정**, 항목의 `.sel`이 지금 테마 표시.
    표시 이름은 **한국어 색상 이름**(사용자 확정 — 글씨에 지금 테마 이름을 보여주는 안과
    물건 이름(한지·수정·청자)은 물렸다, 되돌리지 말 것). 코드 식별자(hanji/crystal/celadon,
    theme-crystal 등)는 옛 이름 그대로 — 저장값 호환 때문이니 표시 이름에 맞춰 갈지 말 것.
    테마(`jgb_theme_v1`)·다크(`jgb_dark_v1`)는 앱 설정(localStorage)이라 문서 상태에 안 들어가고,
    `?first=1`도 안 지운다. 다크 규칙이 CSS에서 테마보다 뒤라 늘 이긴다(테마는 라이트 색조만).
    테마를 늘릴 땐 styles.css `body.theme-*` + index.html #screenPop 항목 + app.js `THEMES`
    세 곳을 함께. danger 계열은 세 테마 공통이라 테마 블록에 다시 안 적는다.
  - `@media print`는 항상 라이트로 강제 — 그 블록의 선택자에 `body.theme-*`도 함께 적어야
    한다(:root보다 명시도가 높아 빼먹으면 인쇄에 테마 색이 샌다).
- **온보딩 네 화면의 생김새는 한 벌이다**(환영 카드 · 둘러보기 · 도움말 센터 · 새 문서
  마법사, + 같은 뼈대를 쓰는 악보 게시 창). 규칙은 styles.css의 `.modal-overlay` 앞
  머리말에 적혀 있고 **네 가지**다:
  ① 모서리는 각지게(radius 0) — 인라인 조각(`.key`·`.tf-in`)만 2px까지.
  ② 깊이는 그림자가 아니라 선이 만든다 — 1px `--line` 테두리 + 옅은 `--ui-shadow`.
  ③ **강조색은 화면당 한 점.** 채워진 버튼은 '지금 눌러야 할 것' 하나뿐이고 나머지는
    테두리(`.secondary`)나 글자만(`.ghost`).
  ④ 번호·절 머리 같은 메타는 `--mono` + 넓은 자간으로 본문과 성격을 가른다.
  - 이 넷은 2026-08-11에 "디자인이 너무 AI 기본값 같다"는 지적으로 잡은 것이다. 그전엔
    둥근 모서리(radius 9종) + 크고 흐린 그림자 + 채운 버튼 여럿 + 46px 이모지 흔들기 +
    스프링 팝인이었는데, 그 조합이 정확히 '어느 웹 앱에나 있는 카드'라 이 앱의 얼굴이
    못 됐다. **하나만 어겨도 그리로 돌아가니 새 온보딩 UI에도 그대로 지킬 것.**
  - `--ui-shadow`·`--mono`는 `:root`에 있고 다크에선 `--ui-shadow`만 갈아끼운다.
  - 자간은 두 단이다: 짧은 라벨(단계 번호·머리 라벨) `.14em`, **한글이 길게 들어오는
    라벨**(`.help-pane h4`·`.tour-sub`) `.09em` — 긴 한글에 `.14em`을 주면 괄호 앞뒤가
    벌어져 낱글자로 흩어진다.
  - 범위는 이 네 화면까지다. `.at-add`처럼 사이드바·도구창과 **공유하는 클래스는
    `#newDocModal .at-add` 꼴로 가둬** 손봤다 — 상단바·사이드바·도구창·팔레트는 아직
    옛 어휘(둥근 모서리)이니, 거기까지 넓힐 땐 별도 작업으로 할 것.
    - 예외 하나: 팔레트의 **골라둔 칩**(`.ornchip.orn-armed`)만 아래 '강조 상자' 수법을
      빌려왔다(꽉 찬 `--accent-tint` → 옅게 번지는 그러데이션). 모서리(6px)는 팔레트의
      기존 어휘 그대로 두었으니, 이걸 근거로 팔레트 전체를 각지게 만들지 말 것.
- **둘러보기 말풍선은 대상을 따라다니지 않는다**(app.js `placeTourCard`·`TOUR_SLOTS`).
  예전엔 단계마다 '대상 아래→위→옆'을 새로 계산해 카드가 화면을 가로질러 날아갔고,
  사람은 [다음] 버튼을 마우스로 쫓아다녀야 했다(2026-08-11 사용자 지적). 규칙 넷:
  - **자리는 아홉 곳으로 고정**(가로 좌·가운데·우 × 세로 아래·가운데·위줄). 화면 크기와
    카드 크기로만 정해지므로 대상이 어디로 가든 자리 자체는 제자리에 있다.
  - **머무는 것이 기본, 옮기는 것이 예외.** 지금 앉은 자리가 짚는 상자(`also`)를 안 가리고
    주 대상(`hole`)도 카드 넓이의 15%(`TOUR_STAY_TOL`) 이하로만 가리면 그냥 둔다. 옮겨야
    할 때도 **지금 자리에서 가장 가까운** 쓸 만한 자리로 간다.
  - **세로는 카드 밑변 기준**(`tourSlotBox`의 `foot`)이라 카드가 늘 **위로 자란다**. 본문이
    209px이든 753px이든 발(버튼줄)이 그 줄의 밑선에 남아 [다음]의 높이가 안 변한다.
    윗변 기준이던 때는 위줄에 앉는 순간 카드가 아래로 자라 [다음]이 254~780px을 오르내렸다.
    같은 이유로 크기는 `getBoundingClientRect()`로 **실수 그대로** 잰다(offsetHeight는
    정수 반올림이라 1px씩 떤다). `vy` 값은 '줄 이름'이지 붙는 변이 아니다.
  - **아래줄이 아닌 자리엔 카드 넓이만큼 벌점**(`pen`). 주 대상 가림은 아무리 커도 카드
    넓이를 못 넘으므로, 위줄·가운뎃줄은 **아래줄이 짚는 상자를 가릴 때만** 쓰인다.
    '주 대상을 좀 가리더라도 버튼은 제자리'를 고른 것이다(사용자 확정).
  - 실측(뷰포트 611×829, 16단계): [다음] 버튼 이동 15/15 → **7/15(세로는 3)**, 자리 5종.
  - **검증할 땐 `#tourCard`의 transition을 꺼야 한다** — 백그라운드 탭은 CSS 전환이
    진행되지 않아 `getComputedStyle`이 이전 값에 멈춘 채로 읽힌다(인라인 스타일은 정상).
    이걸 모르면 '자리 계산이 고장 났다'로 오진한다(실제로 한참 헤맸다).
  - **강조 상자의 색은 두 갈래다**(styles.css `.tour-ring`·`#tourHole`):
    · 보조 상자(`.tour-ring`, 정간·대강·각)는 **앱의 세 테마 강조색을 종이용으로 짙게 한
      형제 셋**을 고정값으로 쓴다(`--rc`에 rgb 세 숫자만; tone-b=은청, tone-c=옥). 다크에서도
      흰 종이인 악보 위에 놓이므로 테마를 따라가면 안 된다 — 예전엔 첫 상자만 `var(--accent)`라
      다크에서 형광 파랑이 얹혔다. 꼴(테두리·번짐·이름표)은 `.tour-ring` 한 곳에서 정하므로
      색을 늘릴 땐 `--rc` 한 줄만 더하면 된다.
    · 주 대상(`#tourHole`)만 `var(--accent)`를 쓴다 — 종이가 아니라 상단바·사이드바·도구창
      위에도 얹히므로 테마·다크를 따라가야 파묻히지 않는다.
    · 둘 다 **실선 하나가 아니라 두 겹**이다(안쪽 1.5px 선 + 바깥으로 옅게 번짐). 2px 실선만
      그으면 테이프를 붙인 것처럼 딱딱하다. `opacity`로 통째로 흐리지 말 것 — 이름표 글자까지
      옅어져 색을 따로 보정해야 했다(지금은 투명도가 색 안에 rgba로 들어 있다).
- **궁서 계열 서체(`Kaiti SC`…`AppleMyungjo`)는 '종이에 실릴 글자'의 표시다.** 아무 데나
  쓰지 말 것 — 이 스택을 쓰는 곳은 **누르거나 적으면 그 글씨 그대로 악보에 찍히는 자리**뿐이다:
  율명 표(`.palette .phead`/`.pch`/`.pch-cap`) · 건반 율명(`.pk-yul`) · 빠르기 미리보기
  (`#tempoItem .gn-preview`) · 악보 본문(app.js `NOTE_FONT`). 팔레트에서 黃을 눌렀을 때
  종이에 찍히는 글씨와 팔레트의 글씨가 어긋나면 안 되므로 **이 넷은 같은 목록을 써야 한다.**
  - 반대로 **분류 이름·설명 같은 UI 라벨엔 쓰지 않는다.** '붙임표'·'운지'라는 글자가 악보에
    나오는 일은 없다. 게다가 그 스택은 OS마다 다른 글씨로 떨어지는데(맥 Kaiti / 윈도 바탕 /
    그 밖 generic serif), 악보 글자라면 치를 값어치가 있는 대가지만 라벨엔 손해뿐이다.
    팔레트 그룹 제목(`.orn-sec b`)이 그래서 2026-08-11에 메타 라벨 어휘(`--mono`+자간)로
    옮겨졌다 — 도움말 절 머리(`.help-pane h4`)와 같은 꼴이라 '절 머리는 앱 어디서나 한 꼴'.
  - 새 서체를 들이는 선택지는 사실상 없다 — 이 리포는 **폰트 파일을 싣지 않는다**(EBS 워드마크·
    Bravura 악보 글리프 모두 윤곽선 패스로 구워 쓴다). 악보 서체를 바꾸려면 그 방식이어야 한다.
- 리본/도구창 버튼 툴팁은 CSS ::after가 아니라 #ribbonTipFloat(JS 위치 계산) 하나를 공유.
- 세로 flex 도구창 안의 머리줄(.pal-top 등)은 `flex: 0 0 auto` 필수 — 없으면 max-height에
  눌려 짜부라지며 자식 버튼이 삐져나온다.
- **문법 검사가 보는 것은 둘이다**(app.js `melodyBadFlags`) — 편집기의 빨간 바탕·게시 창의
  한 줄·검사기(tools/scan-published.mjs)가 **이 함수 하나**를 나눠 쓴다.
  - **① 글자 단위** — 앱이 못 읽는 글자(한자 율명·시김새 이름 오타·짝 없는 괄호·로마자…).
  - **② 칸 단위 — 좌우로 나누려면 상하로 먼저 나뉘어 있어야 한다**(`cellSplitBad`,
    2026-08-24 사용자 확정). 정간보는 세로로 먼저 쪼개는 기보라, 공백이 위아래 분박을 가르고
    붙여 쓴 글자가 그 분박 안에서 좌우로 갈린다. 그러니 **분박 하나짜리가 좌우로 갈리는 꼴은
    없다**: `황 태`○ · `황태 황`○ · **`황태`✗**. 좌우로 놓으려면 스페이스가 한 번은 있어야 한다.
    · 세는 자는 그리기가 쓰는 그것 그대로다(rowToks 필터 + `groupRowTokens`) — 따로 세면
      화면에 그려진 칸 수와 검사가 어긋난다. 숨표·빠르기는 칸을 안 차지하므로 뺀다.
    · **시김새를 음표 앞에 쓰면 여기 걸린다**(`{흘림표}황`). 앞에 쓰면 붙지 않고 제 칸을 열어
      좌우 두 칸이 되기 때문이다(2026-08-24 실측: `⌒ 黃`으로 나란히 그려진다). 종류와 무관하게
      **시김새는 음표 뒤**다 — `중{흘림표}`·`중{니레}`·`중{퇴성}` 다 한 칸으로 붙고, 홀로 선
      `{니레}`는 제 분박을 차지하는 독립 시김새라 걸리지 않는다. 국악원 OMR 변환기도
      `청황{미는표}`처럼 뒤에 쓴다.
    · 자리 셈이 두 가지라 조심할 것: `a`는 코드포인트 단위, `parseMelodyOffsets`의 start/end는
      UTF-16 단위다. BMP 밖 글자가 섞일 때만 자리표를 만들어 옮긴다(평소엔 안 만든다).
  - **곁줄·장단은 안 본다** — 글자를 적는 자리라 '틀렸다'고 말할 잣대가 없다.
- **한자 율명을 붙여넣으면 한글로 바뀐다**(app.js `HANJA_TO_YUL`·`fromHanja`·`wireHanjaPaste`).
  정간보 데이터의 정본은 **한글**이고 한자는 그리는 단계의 표기라, 다른 데서 한자로 된
  정간보를 복사해 붙이면 화면에는 그 한자가 그대로 그려져 **멀쩡해 보이는데 재생·오선보·
  MusicXML에서 통째로 빠진다**(2026-08-24 실측: 3음 중 0음이 울렸다). 눈으로는 알 수 없는
  어긋남이라 붙여넣는 자리에서 돌려놓는다.
  - 되돌림표는 `YUL`·`OCT_HANJA`를 뒤집어 만들고 **같은 한자가 두 뜻을 갖는 것은 뺀다** —
    지금은 `OCT_HANJA`의 `-1`층에 `侇`가 유·이 **둘 다에 적혀 있다**(둘 중 하나가 오기이고,
    그래서 앱은 지금 배유와 배이를 같은 글자로 그린다). 그 글자만 그대로 두고 문법 검사에
    걸리게 남긴다 — 짐작으로 남의 글자를 바꾸지 않는다.
  - `HANJA_TO_YUL`은 **`octPrefix` 뒤에** 있어야 한다. 그보다 앞에 두면 `OCT_ROWS`의 TDZ에 걸린다.
  - **선율 칸에만** 건다(에디터의 #melody, 직접 입력의 정간 카드). 곁줄·장단·텍스트는 글자를
    적는 자리라 한자가 제 뜻이다.
  - 넣는 것은 `document.execCommand("insertText")` — **⌘Z를 살려 두려는 것**이다. value를
    직접 갈아치우면 브라우저의 되돌리기 기록이 끊긴다(막힌 환경에서만 그쪽으로 물러난다).
- **게시 창은 올리기 전에 문법을 한 줄로 알려 준다**(`#pubSyntax` · cloud.js `showSyntaxNote`).
  문구는 `문법이 틀린 곳이 N군데 있습니다.` **딱 이것뿐이다** — 무엇이 어떻게 안 된다는
  설명은 여기서 할 말이 아니고(사용자 확정, 2026-08-24), **막지도 않는다**(무엇을 쓰려던
  것인지는 앱이 모르고 일부러 그렇게 둔 악보일 수도 있다).
  세는 자는 `window.jgbDoc.badCount()`이고 그 안은 선율 에디터가 빨간 바탕을 깔 때 쓰는
  `melodyBadFlags` 그대로다 — cloud.js에서 다시 세면 화면에 빨갛게 보이는 자리와 숫자가
  어긋난다. 글자 수가 아니라 **이어진 덩이 수**를 센다('3군데'로 읽히게).
- **정간 구간 복사·오려두기·붙여넣기**(⌘/Ctrl+C·X·V, `melClip`)는 드래그로 고른 구간이 대상이고,
  담는 것은 그 정간에 **보이는 것 전부** — 선율·곁줄·정간 서식(배경색·가로줄). 장단은 곡에 한
  줄뿐이라(gi가 늘 0) 정간 구간의 일부가 아니라 뺐다. 조작은 **단축키뿐**(버튼 없음).
  - 붙여넣기는 **고른 구간의 첫 칸부터** 덮어쓰고, 악보 끝을 넘는 몫은 조용히 버린다 — 각을
    새로 만들며 늘리면 붙여넣기가 '구조를 바꾸는 일'이 되어 페이지 배치까지 흔들린다.
  - 서식은 담을 때도 붙일 때도 `cloneCellStyle`로 **새로 떠야** 한다. border는 변마다 객체라
    그대로 두면 원본과 붙여넣은 칸이 같은 객체를 가리켜, 한쪽 모양을 고치면 다른 쪽도 바뀐다.
  - 글자를 치는 중(input/textarea)이거나 글을 고른 상태면 브라우저 기본 동작에 양보한다 —
    도움말·안내문은 user-select가 살아 있어 거기서 ⌘C가 먹어야 한다.
- **시김새는 소리로 풀린다**(app.js '시김새 소리내기' 절 + 사전의 `snd`). 핵심은 '니'가 반음
  위가 아니라 **그 곡 음계의 바로 윗음**이라는 것 — 그래서 음높이를 옮기기 전에 곡의 음계부터
  정해야 한다.
  - 음계는 **조 프리셋(#joPreset)이 정해져 있으면 그것**, '12율 전체'(기본값)면 **곡이 어느
    조인지 먼저 판단한다**(`scaleNotes`, 2026-08-14 사용자 확정) — 평조 꼴(0·2·5·7·9)을
    12율에 앉힌 12벌 중 곡의 음을 가장 많이 담는 집합을 고른다. 계면조는 따로 안 훑는다 —
    같은 5음 집합을 다른 궁에서 부른 이름이라 12벌이 이미 덮고, '한음 위'가 보는 것은 이름이
    아니라 집합이다. 예전엔 많이 나온 율명 다섯을 그냥 뽑았는데 시김새가 스치는 이웃 음이
    순위에 끼면 어느 조도 아닌 음계가 나왔다. 조 칸은 본래 팔레트를 추리는 자리지만 이 앱에서
    악조를 적어 두는 곳이 거기 하나뿐이라, 적혀 있으면 그게 곧 이 곡의 음계다. 율명이 넷도 안
    되는 악보는 셀 것이 없어 황종 평조로 둔다.
  - `makeScale(hwangMidi)`의 **한 칸**은 음계에 없는 음(예외음)에서 떠날 때 그 방향 첫 음계음까지다
    — 어디서 출발하든 한 칸이 늘 '바로 다음 음'이 되게.
  - 붙임(꾸밈)은 자리의 3할까지만 떼어 나눠 갖는다(`GRACE_MAX` 0.09초) — **박은 안 바뀐다.**
  - **끄고 민음으로 들을 수 있다**(재생 설정의 #playSigimsae '시김새대로 연주', 기본 켬 ·
    `sigimsaeSoundOn()` → `realizeMelody(..., {plain:true})`). 가락의 뼈대만 듣고 싶을 때가
    있어서 둔 길이다. 끄면 붙임은 꾸밈음도 '본음 자리를 가르는 꼴'도 안 내고, **독립 시김새는
    적힌 율명이 아니므로 앞 음이 이어진다**. 여기서도 박은 그대로다.
    - **오선보·MusicXML은 이 값과 무관하다** — 거기선 늘 시김새를 그린다. 그래서 옵션은
      `realizeMelody`의 **인자로만** 넘기고, 그 함수 안에서 체크박스를 읽지 않는다(읽으면
      악보까지 따라 바뀐다). 재생(`buildAudioEvents`)만 넘긴다.
  - 다음 시김새가 기준 삼는 `prevMidi`엔 **본음만** 들어간다(꾸밈음이 들어가면 가락이 밀린다).
    쉼표는 소리를 끊되 `prevMidi`는 그대로 둔다 — 쉼표 뒤의 '노'도 쉼표 앞 음 기준이라야 이어진다.
  - 값의 출처는 MALerLab/SejongMusic(ISMIR 2024)의 `jg_to_staff_converter.py`가 쓰는 음정
    관계다. **코드를 옮긴 게 아니라**(그 리포엔 LICENSE가 없다) 음악적 규칙만 우리 사전에 적고,
    `node tools/check-sigimsae-sound.mjs`가 되짚어 확인한다. 그쪽이 다루는 33개 중 32개가 값까지
    같고, 나머지 '로니로'는 우리에게 그림이 없는 기호다(2026-08-08 대조).
  - 검사는 app.js **소스 조각을 이름으로 떼어 와 그대로 돌린다** — 같은 셈을 검사에 한 벌 더
    적으면 app.js를 고쳐도 옛 답을 계속 맞다 하기 때문. `window.jgbAudioEvents`는 그 결과를
    브라우저에서 들여다보는 창구(jgbShareLink와 같은 성격).
- **무엇이 울리나는 `realizeMelody()` 한 곳에서 푼다.** 악보를 '자리(slot)' 목록으로 바꿔
  주는데(`{gak,cell,beat,dur,kind,seq,pre,post}`, 길이는 **정간 하나를 1로 센 박 단위**),
  재생과 오선보 내보내기가 **이것 하나를 나눠 쓴다** — 같은 셈을 두 벌 적으면 소리와 악보가
  어긋난다. 재생은 여기에 초(secPerBeat)와 사인파를, 내보내기는 음표 길이와 음이름을 입힌다.
  `kind`는 셋: `"note"` 새 소리 · `"rest"` 쉼표 · `"hold"` 앞 음을 잇는다.
  **지속(hold)은 제 각 안에서만** — 각(=한 장단)이 바뀌면 push가 rest로 바꿔 소리가 끊기고
  오선보에도 쉼표로 적힌다(2026-08-14 사용자 확정). 그래서 마디를 넘는 붙임줄은 이제 안
  나온다(마디 안에서 자르는 tie 기계는 남겨 뒀다). prevMidi는 쉼표 규칙 그대로 안 지운다 —
  다음 각의 '노'도 앞 각 마지막 음 기준이라야 가락이 이어진다.
- **오선보 내보내기**(#btnMusicXml → 파일 메뉴 · 오선보 칸의 [파일로], `js/musicxml.js`): MusicXML 4.0.
  - **각 = 한 마디**가 기본이고(고를 수 있다 — 아래 '각 하나가 늘 한 마디인 것은 아니다'),
    정간 하나를 **자동(기본)·점4분음표·4분음표·8분음표 중에 고른다**
    (#staffUnit, 오선보 칸 머리줄). 그래서 박자표의 **기본값**이 거기서 정해진다: 점4분음표면
    `3N/8`, 4분음표면 `N/4`, 8분음표면 `N/8` (12정간 각이면 36/8 ↔ 12/4 ↔ 12/8).
    아랫수는 따로 고를 수도 있다(#staffTime — 아래 '박자표도 사람이 고를 수 있다' 절).
    - **'자동'은 각의 정간 수가 정한다: 12정간이면 8분음표(12/8), 그 밖은 점4분음표**
      (2026-08-14 사용자 확정). 12정간을 점4분음표로 보면 36/8이라 한 마디가 서양 악보에서
      터무니없이 길고, 취타·여민락처럼 12정간 각이 흔해 기본값으로 둘 값어치가 있다.
      **고른 값이 있으면 그 값이 이긴다** — 옛 문서는 거기 적힌 값 그대로 열린다.
    - 이건 취향이 아니라 **그 곡이 정간을 몇으로 쪼개느냐**와 **각이 몇 정간인가**에 달렸다:
      3분박이면 점4분음표라야 분박이 8분음표로 딱 떨어지고, 2분박이면 4분음표·8분음표라야
      그렇다. 안 맞는 쪽을 고르면 그림은 (비례 간격이라) 견딜 만해도 MusicXML의 `<type>`이
      비어 악보 프로그램이 추측하게 된다. 정악은 3분박이 흔해 **점4분음표가 기본**이고,
      그건 MALerLab/SejongMusic 자료와 같은 환산이라 그쪽 악보와 나란히 견줄 수 있다.
    - **8분음표는 2분박이면서 각이 길 때** 쓴다 — 20정간 각을 4분음표로 보면 `20/4`이 되어 한
      마디가 서양 악보에서 터무니없이 길어지는데, 8분음표면 `20/8`이다. 민요·산조처럼 정간이
      잘게 흐르는 악보가 이쪽이다.
    - **문서에 딸린 값**이다(CTRL_IDS의 `staffUnit`) — 조 프리셋과 같은 성격이라 남에게 악보를
      주면 같이 간다. 배율·높이(`jgb_staff_v1`, 브라우저별 보기 설정)와는 사는 곳이 다르다.
    - 길이의 단위는 `js/staff-core.js`의 `JG = {dotted, plain, eighth}`이고, 고른 값이 score의
      `jg`·`unit`에 실려 그리기·내보내기로 함께 간다. **`SC.JG`를 숫자로 쓰지 말 것**(표다).
      **박자표 셈도 거기 `timeSig(unit, beats)` 한 곳에 있다** — 화면과 파일이 같은 답을 써야
      하므로 어느 한쪽에 `unit !== "plain"` 같은 갈래를 다시 적지 말 것. 단위를 늘릴 땐
      `JG`·`timeSig`·index.html의 #staffUnit 세 곳만 손대면 된다(app.js는 `SC.JG`에 있는
      이름인지만 본다).
  - `divisions`는 1680(4분음표당) → 정간 하나 = **2520**. 2520을 고른 건 1~10과 12로
    나눠떨어져 분박을 몇으로 쪼개든 길이가 정수로 떨어지기 때문. 그래도 남는 반올림 오차는
    **그 정간의 마지막 자리에서 걷는다** — 안 그러면 오차가 쌓여 뒤쪽 마디부터 어긋난다.
  - 마디를 넘는 음은 잘라 **붙임줄(tie)** 로 잇고, 마지막 마디의 빈자리는 쉼표로 메운다.
    각을 여러 마디로 나눈 경우 각 안의 마디 경계에서도 같은 규칙으로 이어진다(각 경계는 예전대로
    hold가 끊겨 쉼이 된다 — realizeMelody가 이미 잘라 놓는다).
  - 붙임 시김새 = 꾸밈음(`<grace>`, 길이를 안 먹음) · 독립 시김새 = 제 자리를 나눈 실음.
    그래서 **재생의 GRACE_MAX(0.09초)는 오선보와 무관하다** — 거기선 꾸밈음에 길이가 없다.
  - **조표는 음계의 '도'가 정한다**(`SC.fifthsFor`) — 잉크가 가장 적은 조표가 아니다. 5음
    음계는 **임시표 없이 적을 수 있는 조표가 셋**이고(황종 평조면 ♭5·♭4·♭3) 셋 다 음정은 안
    틀리므로, 고르는 일은 표기 경제가 아니라 **무엇을 도(do)로 선언하는가**의 문제다. 평조는
    솔라도레미, 계면조는 라도레미솔로 읽고 그 '도'를 으뜸음 삼는 장조 조표를 쓴다:
    황종 평조 ♭4(도=중려) · 중려 평조 ♭5(도=무역) · 황종 계면조 ♭6(황협중임무 = 라도레미솔
    이라 도=협종). **이 '도'는 그 악조의 궁(宮)이 아니다** — 악조 이름이 말하는 궁은 황종이고
    (그래서 '황종 평조'다) 도는 중려다. 정간보는 악조를 궁으로 이름 붙이는데 서양 조표는 장조
    으뜸음을 선언하므로 **중심음과 조표의 으뜸음이 어긋난다**(2026-08-13 사용자 정정 — 예전
    주석은 중려를 '궁'이라 잘못 불렀다). 셈은 **5도권에서 가장 낮은 음계음이 그 '도'** — 그
    음은 음계에 있는데 한 겹 더 내림쪽 음은 없는 자리다(평조·계면조가 이 한 규칙으로 함께
    맞는다). **2026-08-11 정정** — 예전엔 최소 임시표라 셋 다 한 겹씩 모자랐고(♭3·♭4·♭5),
    음이 하나도 안 틀려 **검사로는 안 잡히고 악보를 읽는 사람 눈에만 보이던** 오류였다.
    황=E♭ 평조면 세종 자료와 같은 ♭4가 나온다.
    음계가 5도권에서 안 이어지면(빈도로 추정한 음계·반음계적 선율) '도'를 못 짚는데,
    **그때는 조표 없음(다장조)으로 물러나 임시표로 적는다**(2026-08-13 사용자 확정) —
    예전엔 '임시표가 가장 적은 조표'로 물러났는데, 그건 방금 버린 표기 경제 기준을 뒷문으로
    되살리는 데다 **못 짚은 자리에서 아무 조나 선언해 버린다**(읽는 사람은 그 조라고 믿는다).
    조표 없음은 '조를 못 정했다'는 정직한 표시고, 대가는 임시표가 느는 것뿐이다.
  - **기준음은 정간보 쪽 황 음고**(#hwangPitch, app.js `staffHwang()`) — 황을 C로 두고 쓰는
    악보면 오선보도 C로 적히고 조표도 그 자리에서 다시 잡힌다(황=C 평조면 도가 중려라 ♭1).
    정간보와 오선보가 같은 곡을 가리키는데 기준음이 갈리면 그건 다른 곡이라서. 다만 **곡
    하나에 기준음은 하나**이므로 어디서 오는지를 그 함수 한 곳에 못 박아 둔다 — 조표·음이름·
    재생이 저마다 다른 데서 집어 오면 어긋난다.
  - **조표는 사람이 고를 수 있다**(#staffKey, 오선보 창 머리줄 · CTRL_IDS라 문서에 딸린 값).
    기본 '자동'이 위 '도' 규칙을 쓰고, 골라 두면 그 값(♭7~♯7)이 이긴다 — `staffFifths()`가
    자동보다 먼저 이 칸을 본다. 열어 둔 까닭은 임시표 없이 적을 수 있는 조표가 하나가 아닌
    데다 채보 관행도 갈리기 때문이다(국립국악원 교과서 표준악보집은 가야금 연주곡을 아예
    본청=사음으로 옮겨 ♯1개로 적는다 — `references/정악보/국악원교육표준악보.pdf`).
    **기준음이 고정이라 조표를 바꿔도 음높이는 안 변한다** — 적는 방식(음이름·임시표)만
    달라진다. 검사가 그 둘을 함께 지켜본다.
  - **박자표도 사람이 고를 수 있다**(#staffTime, 오선보 창 머리줄 · CTRL_IDS라 문서에 딸린 값 ·
    `staffTimeType()` → score의 `timeType` → `SC.timeSig(unit, beats, type)`). 조표와 같은
    성격의 칸이다 — 자동으로 정해 주되 잠그지 않는다. 20정간 각이 자동으로 20/8이 되는데
    그런 표기를 쓰는 악보는 없어서(관행은 10/4) 연 자리다.
    - **고르는 것은 아랫수뿐이고 윗수는 따라온다.** 마디 총 길이가 각의 정간 수로 이미
      정해져 있어, 윗수까지 자유로 두면 마디 길이가 어긋나 악보 프로그램이 마디를 다시
      짠다. 그래서 같은 길이를 다른 아랫수로 다시 세는 것만 연다(20/8 ↔ 10/4 ↔ 5/2) —
      **자유 입력 칸으로 바꾸지 말 것.** 윗수는 `SC.timeTop(unit, beats, type)`이 센다.
    - 안 나눠떨어지는 아랫수(5정간 각을 2분음표로 세는 등)는 **조용히 자동으로 물러난다** —
      각마다 정간 수가 다를 수 있어 어느 마디에서만 안 맞을 수 있기 때문이다. 팔레트에서는
      `updateStaffTimeOptions()`가 항목 글씨에 실제 박자표(3/2·6/4…)를 써 넣고 못 적는
      아랫수를 막는다(index.html의 `─/2` 따위는 자리 표시일 뿐이다).
    - **메트로놈(`beatUnit`·`dot`)은 안 따라간다** — 그건 '정간 하나'의 이름이라 박자표를
      바꿔도 빠르기가 달라지면 안 된다. 빔 묶음(`SC.beatGroups`)도 정간·대강이 정하므로 무관.
  - **각 하나가 늘 한 마디인 것은 아니다**(#staffBar, 오선보 창 머리줄 · CTRL_IDS라 문서에
    딸린 값 · `staffBarMode()`·`barsOfGak()`). 기본은 예전처럼 각 전체가 한 마디이고,
    '대강마다'로 두면 **각을 대강 분절대로 끊어** 여러 마디로 나눈다(2026-08-15 요청,
    2026-08-16 대강 기준으로 확정):
    3,3,3,3 → 3/8 넷 · 11·5 → 33/8 + 15/8 · 6,4,4,6 → 18/8 + 12/8 + 12/8 + 18/8.
    - **끊는 자리는 악보가 이미 답을 갖고 있다.** 예전엔 '2정간씩·5정간씩' 같은 임의 등분도
      열어 두었는데, 3,3,3,3처럼 고르게 나뉜 각에서만 우연히 맞았지 11·5나 6,4,4,6에서는
      뜻 없는 자리에서 잘렸다 — **되살리지 말 것.** 대강이 없거나 하나뿐인 악보에서는
      '대강마다'가 막히고 각 전체 그대로다.
    - **각의 경계는 그대로 지킨다** — 각을 걸치는 마디는 만들지 않는다. 각 = 장단 한 주기가
      이 기보의 뼈대이고 위 '각 = 한 마디'도 거기서 나온 말이라, 나뉘는 것은 각 **안**뿐이다.
      각마다 새로 끊으므로 그 꼴이 각마다 되풀이된다.
    - 마디를 세우는 일은 **`measBeats[]` 한 곳**이 다 몰고 간다 — 예전엔 '각마다의 정간 수'가
      들어가던 배열에 이제 '마디마다의 정간 수'가 들어간다. 채우는 루프(③)·박자표·마디 폭이
      전부 이 배열만 보므로 나머지는 손댈 것이 없었다. **새 코드에서도 각 수로 마디를 세지 말 것.**
    - 함께 실리는 두 표: `measDg[]`(그 마디의 대강 분절)와 `gakMeasStart[]`(각 번호 → 첫 마디
      번호, 끝 보초 포함).
      · `measDg`는 **빔 묶음**에 쓴다 — `SC.beatGroups(unit, beats, daegang)`이 마디마다 제
        분절을 받는다. 각 전체 마디면 그 각의 대강 분절이 그대로 들어가고(빔은 여전히 대강으로
        묶인다), 대강마다 끊었으면 그 마디가 곧 한 대강이라 `[마디 길이]` 한 덩이가 들어간다.
        score의 `daegang`(기본 각 것)은 옛 재료를 위한 대비값으로만 남는다.
      · `gakMeasStart`는 **나란히 인쇄**가 쪽의 각 범위를 마디 범위로 옮기는 데 쓴다.
        거기서 `measBeats`·`measDg`도 **함께 잘라야** 한다 — 안 자르면 쪽 안의 마디 번호로
        곡 전체의 표를 읽어 박자표·빔이 밀린다(각 길이가 다른 곡에서 이미 있던 버그).
  - **한 줄에 몇 마디를 놓을지도 고를 수 있다**(#staffPerLine, 오선보 창 머리줄 · CTRL_IDS라
    문서에 딸린 값 · `staffPerLine()` → score의 `perLine`). 기본 '자동'은 폭이 닿는 데까지
    채우는 것이라, 각 길이가 들쭉날쭉한 곡(가곡·수제천)에서는 줄마다 마디 수가 달라진다 —
    각이 곧 장단이므로 줄을 고르게 끊어 읽고 싶을 때가 있어 연 칸이다(정간보 '한 줄 각 수'와
    같은 마음).
    - **줄을 앱이 세지 않는다.** musicxml.js가 그 자리마다 `<print new-system="yes"/>`를
      악보에 적고 조판기가 접는다 — 그래서 화면(Verovio)·인쇄·MusicXML 파일이 저절로 같은
      줄 나눔을 갖고, 뮤즈스코어에서 열어도 그대로다. Verovio 쪽은 `breaks: "line"`이라야
      이 표시를 따르므로 vrvRender·vrvSheetPages **두 곳 다** 값이 있을 때 "line"으로 바꾼다.
    - 끊는 자리는 그 score가 품은 마디 안에서 센다(`mi % perLine`) — 인쇄가 쪽마다 마디를
      잘라 넘기므로(sideBySidePages의 `measures.slice`) 곡 전체 번호로 세면 쪽 첫 줄만
      토막 난다. 쪽이 바뀌면 줄 세기도 새로 시작하는 것이 맞다.
    - staff-view(대비책)도 같은 자리에서 끊는다 — 폭을 쌓는 루프에 `c < perLine` 조건이
      붙었다. **폭이 모자라면 정한 수보다 적게 놓는다**(넘겨 그리면 창 밖으로 나간다).
    - 검사는 check-musicxml('한 줄에 몇 마디' 절 — 줄바꿈 자리·마디 맨 앞인지·길이 불변)과
      check-staff-view(줄 수·좁은 창에서의 물러남).
  - **길이 하나를 어떻게 적을지는 `SC.writeAs(units, off, beat)` 한 곳이 정한다** — 내보내기가
    쓰는 창구이고, 차례가 곧 규칙이다: ① 음표 하나로 떨어지면 그대로 ② **한 박 안에 들면서**
    3/2배가 떨어지면 셋잇단(그 꼴 + 3:2 time-modification) ③ 아니면 박을 기준으로 갈라
    붙임줄로 잇는다. musicxml.js에 이 판단을 다시 적지 말 것.
    - **②의 '한 박 안에' 조건이 급소다** — 없으면 정간을 걸친 음이 통째로 셋잇단이 되어
      '점점2분음표 3:2' 같은 것이 나온다(취타 길타령, 2026-08-14 사용자 제보). 셋잇단은
      한 박 안의 일이다.
    - **박 한가운데서 시작해 박을 넘는 음은 한 음표로 떨어지더라도 가른다** — 안 그러면 그
      음이 걸치고 있던 잇단 묶음이 반 토막으로 남아 박이 안 보인다. 박 머리에서 시작하는
      음은 그대로 둔다(12/8의 점2분음표는 제 꼴이다).
    - 괄호(숫자 3)는 같은 박 안에서 **셋 이상 모였고 길이 합이 표준 음표값이 되는 자리마다**
      닫는다 — 박 통째로 한 묶음이면 3분박×3(16분 셋잇단 아홉)이 9개짜리 묶음이 되어 조판이
      깨지고, 반대로 '합이 떨어지면 무조건'으로 두면 길이가 섞인 박에서 **두 음만에 닫혀
      묶음이 한가운데서 갈린다**(560+280=840에서 끊겨 빔까지 따라 갈렸다). 셋을 채우기
      전에는 박 끝까지 간다.
    - **잇단도 보통 음표와 함께 빔으로 묶는다** — 조판기는 빔으로 묶인 잇단에는 숫자만
      얹고 **안 묶으면 각진 대괄호**를 그린다(길타령에서 잇단 32개 중 26개가 괄호였다,
      2026-08-14 사용자 제보). 예전엔 '꼴이 섞인 잇단은 안 잇는다'며 미뤄 뒀는데 겹이 다른
      자리를 잇는 몽당빔 셈이 생기며 갈래를 나눌 까닭이 없어졌다. 다만 잇단과 보통 음표를
      한 빔에 섞지는 않는다(빔 묶음의 열쇠가 '박 + 잇단 묶음 번호'다).
      **둥근 곡선 잇단은 Verovio가 못 그린다** — `line-shape="curved"`는 무시되고 옵션도
      없다(2026-08-14 실측). 그래서 남는 길은 '괄호' 아니면 `bracket="no"`(숫자만) 둘뿐인데,
      **표준 관행대로 두기로 확정했다**(2026-08-14 사용자 확정): 빔으로 묶인 잇단은 숫자만,
      빔을 못 잇는 잇단(안에 4분음표가 낀 자리)만 괄호. 괄호는 그 묶음이 어디까지인지
      알려주는 유일한 표시라 `bracket="no"`로 통째로 없애지 말 것.
  - **꼬리 있는 음표는 한 박 안에서 빔으로 잇는다**(2026-08-14 사용자 요청). 낱개 깃발로
    두면 꼬리 숲이 되어 못 읽는다. 한 박의 단위는 위 '오선보 보기'의 빔 규칙과 **같은
    `SC.beatGroups`** — 화면과 파일이 같은 빔을 본다. 끊는 자리 넷: **쉼표 · 꼬리 없는
    음표(4분음표 이상) · 박 경계 · 잇단**(잇단은 제 묶음이 따로 빔을 갖는다).
    첫 겹은 묶음 전체를 가로지르고 **둘째 겹부터는 그만큼 짧은 음표가 이어지는 자리에만**
    얹으며, 그 자리가 혼자면 몽당빔(`forward/backward hook`)으로 적는다 — `<beam>`을
    겹마다 따로 적어야 해서 noteEl이 `beams` **배열**을 받는다(한 값이 아니다).
    붙임줄로 가른 조각도 꼬리가 있으면 이웃과 묶이므로, **가르기와 빔을 한 목록(`items`)
    위에서 잇달아 셈한다** — 가르기를 emit 중에 하면 빔을 얹을 자리가 없다.
  - **음표꼴이 없는 길이는 붙임줄로 가른다**(위 `writeAs`의 ③, 2026-08-14 사용자 요청).
    한 음을 세 정간 끌면 7560인데 어떤 음표에 점을 둘까지 붙여도 그 값이 없어 `<type>`이
    비었고, 그러면 **조판기가 기둥도 꼬리도 없는 머리만 그린다**(길이는 맞아도 악보로는
    못 읽는 그림). 가르는 자리는 **모음박 = 정간 하나**가 정한다: ① 박으로 **들어가는**
    첫 조각은 경계까지 끊고 **짧은 것부터**(박 앞은 짧게) ② 경계에 올라선 뒤로는 **온전한
    박을 한 음표로 최대한 길게** ③ 남는 꼬리는 긴 것부터. 그냥 긴 것부터 집으면 7560이
    온음표+8분음표가 되어 박을 가로지른다 — 12/8의 세 박은 점2분음표 ⌒ 점4분음표라야
    박이 보인다. 임시표는 **첫 조각에만** 적는다(뒤는 같은 음이 이어지는 것뿐).
    셈을 staff-core에 둔 건 화면·파일이 같은 답을 봐야 해서다 — musicxml.js에 다시 적지 말 것.
    - **박 안에 드는 조각은 셋잇단으로도 갈라진다**(`fragment`) — 표준값 → 셋잇단 →
      표준값 여럿 → **셋잇단 여럿** 순으로 시도한다. 마지막 갈래가 급소인데, 3분박 박에서는
      값이 분박의 배수라(4분음표 정간이면 560) 2의 거듭제곱 값만으로는 1400(2.5분박) 같은
      길이를 통째로 못 적어 `<type>`이 비었다(길타령에서 실제로 그랬다). 3/2배 자리에서
      갈라 되돌리면 1400 = 1120(3잇단 4분) + 280(3잇단 16분)이 된다.
    - 그 밖(5·7분박)은 2·3의 어느 쪽으로도 안 나뉘어 붙임줄로도 못 적으므로 `writeAs`가
      null을 주고, 예전처럼 비워 두면 악보 프로그램이 `<duration>`을 보고 알아서 고른다
      (`<type>`은 없어도 되는 항목 — 억지 잇단보다 정직하다, 쓰지 않기로 확정).
  - **악기가 여럿이면 여러 part로 나간다**(MusicXML이 원래 그런 형식이다).
  - **가사는 아직 안 싣는다**(장단은 아래 '장구 오선보' 절 참고).
    검사는 `node tools/check-musicxml.mjs`.
- **장구 오선보**(#staffJanggu, app.js '장구 오선보' 절 · 표기 표는 `js/staff-core.js`의
  `JANGGU`·`PERC_POS`): 장단을 **한 줄짜리 보표(일선보)** 에 옮긴다. 음높이가 없으므로 자리는
  하나뿐이고, 무엇으로 쳤나는 **기둥 방향**이 말한다 — 북편(왼손, 궁)은 아래, 채편(오른손)은
  위, 양손(덩)은 둘 다. 그래서 이 파트만 성부가 둘이다(MusicXML `<voice>` 1·2 + `<backup>`).
  - **작은덩·다는 덩·덕과 같은 음표로 적는다**(2026-08-21 사용자 확정) — 세기만 다를 뿐 치는
    손도 자리도 같고, 크기로 가르면 작은 음표가 꾸밈음으로 읽힌다. 세기를 적고 싶어지면
    크기가 아니라 셈여림표로 붙일 것. 기덕은 앞꾸밈 하나, 더러러러는 트레몰로 사선 셋.
  - **갈림길은 '장단은 한 각짜리 패턴 하나'라는 것**이다. 정간보는 첫 각 옆에 한 번만 그리는데
    오선보의 파트는 곡 전체 길이를 가져야 한다. 그래서 칸이 셋이다(CTRL_IDS의 문서 값):
    · **첫 마디만**(기본) — 장단 한 각을 **따로 한 번 조판해** 악보 위에 얹는다(`vrvLegend` ·
      인쇄는 `vrvLegendPiece`). 악보의 성부가 아니라 **악보 앞에 붙는 안내**라 본 악보와 같은
      조판에 안 섞는다 — 섞으면 빈 장구 보표가 모든 줄에 따라붙어 긴 곡에서 종이를 통째로
      먹는다. 각 길이가 들쭉날쭉한 곡·'대강마다' 마디 나눔에도 안 휘둘린다.
    · **파트로 반복** — 진짜 성부로 **맨 위**에 얹고 각마다 되풀이한다(합주 총보를 뮤즈스코어로
      넘길 때). 맨 위인 것은 각(장단)이 이 기보의 뼈대이기도 하고, 빠르기 표시가 맨 위 보표에
      붙어야 해서이기도 하다(musicxml.js는 `pi === 0`에 적는다).
    · **안 그림**.
  - **인쇄 범례는 첫 쪽에만**(2026-08-21 사용자 확정). 자리는 **모든 쪽에서 똑같이 비운다** —
    쪽마다 상자가 다르면 조판기가 한 번에 접을 수 없어서다(둘째 쪽부터 위 여백이 그만큼 넓다).
    **나란히 인쇄에는 범례를 안 붙인다** — 왼쪽 반쪽의 정간보에 장단 줄이 이미 그려져 있다.
  - **MusicXML 파일에는 '첫 마디만'이어도 곡 전체로 실린다.** 범례는 종이에 몇 번 그릴까 하는
    조판의 문제지만 파일은 **곡 자체**를 담고 장단은 실제로 곡 내내 친다 — 한 마디만 넣으면
    뮤즈스코어에서 '장단이 첫 각에만 있는 곡'이 되어 사실과 달라진다. 화면과 다른 것이 아니라
    같은 말의 다른 적기다.
  - **총보든 파트보든 나온다**(2026-08-21 사용자 확정) — 장단은 곡에 하나뿐이라 정간보에도 늘
    그려지고 재생에서도 보기와 무관하게 울린다. 그 규칙을 오선보만 뒤집을 까닭이 없다.
  - **타점의 길이는 다음 타점까지, 길어도 한 박까지**다. 북은 끌리지 않아 뒤를 얼마나 잡아
    두느냐는 관행의 문제인데, 한 박을 넘겨 잡으면 음표꼴이 없는 길이가 나와 붙임줄로 갈린다
    (12정간 각의 궁 하나가 '온음표 ⌒ 8분음표'가 됐다) — 북 한 번을 붙임줄로 잇는 악보는 없다.
    **무엇이 한 박인가는 `SC.beatGroups` 한 곳**이 답한다(정간이 점4분·4분음표면 정간 하나,
    8분음표면 대강 하나). 그래서 12/8 장단이 대강마다 점4분음표로 적히고 남는 자리는 쉼표가
    된다 — 실제 장단 오선 표기의 모습이다. 이음(`-`)과 모르는 글자는 새로 치지 않는다.
  - 마디 계획은 선율과 **같은 `measurePlan()`** 을 본다 — 어느 한쪽에 다시 적으면 '대강마다'로
    끊었을 때 장단만 딴 자리에서 갈린다. 나란히 인쇄가 쪽마다 자를 때 `measures`뿐 아니라
    **`lanes`도 함께 잘라야** 북편이 곡 전체인 채로 남지 않는다.
  - `js/staff-view.js`(대비책)는 **장구를 그리지 않는다** — 다섯 줄 보표를 전제로 짜여 있고,
    조판기를 정말 못 쓸 때의 대비책이라 새 보표 꼴을 한 벌 더 들일 값어치가 없다.
  - Verovio 실측(2026-08-21): `<staff-lines>1` + `<clef><sign>percussion</sign>`으로 1선보가
    되고, `<unpitched>`의 **E4**가 자리표와 무관하게 그 한 줄 위다(한 칸 어긋나면 음표가 줄
    밖에 뜬다). 쉼표엔 `<stem>`을 적지 않는다(규격 위반이자 조판기가 헷갈린다).
  - 검사는 `node tools/check-janggu-staff.mjs`.
- **오선보 보기**(#btnStaff → 정간보 아래 #staffPane, app.js '오선보 보기' 절 + `js/staff-view.js`):
  바꾼 결과를 **화면에서 곧바로 본다** — 파일로만 나가면 맞게 바뀌었는지 확인할 길이
  뮤즈스코어뿐이라 연 자리다.
  - **재료는 내보내기와 같은 `buildStaffScores()`** — 화면에서 본 것과 파일로 나간 것이 다르면
    보는 뜻이 없다.
  - **화면 조판은 Verovio가 한다**(app.js 오선보 보기 절의 vrvTk·loadVerovio·vrvRender).
    재료(buildStaffScores)를 MusicXML(js/musicxml.js)로 적어 그대로 먹인다 — **MEI로 미리
    바꾸지 않는다**(Verovio가 안에서 스스로 바꿔 결과가 같음을 2026-08-14 좌표 단위까지 대조).
    6.7MB 조판기는 칸을 열 때만 지연 로드하고, 로드 전·실패 시엔 staff-view가 예전처럼
    그린다 — 조판기 하나 때문에 오선보가 통째로 막히면 안 된다(staff-view를 지우지 말 것).
    다크모드는 styles.css `.vrv-out` 규칙이 맡는다: 안쪽 svg의 `color="black"` **속성**을
    CSS(color:inherit)로 누르고 fill을 currentColor로 — 선택자에 **use가 꼭 있어야 한다**
    (음표머리는 <use>가 defs를 참조하는데 원본 path에 준 fill이 복제본까지 못 간다, 실측).
    **꾸밈음이 둘 이상이면 musicxml.js가 빔(begin/continue/end 두 겹)으로 꼬리를 잇는다**
    (2026-08-14 사용자 확정) — 화면(Verovio)과 내보낸 파일(뮤즈스코어)이 같은 빔을 본다.
    **인쇄·PNG(아래 절)도 같은 조판기가 접는다**(vrvSheetPages) — 화면과 종이가 같은
    그림이어야 하므로. 못 불러왔을 때만 staff-view 쪽 나눔(staffSheetPages)으로 물러난다.
  - **무엇을 보여주나는 정간보의 '총보' 체크(#scoreView)를 그대로 따른다** — 켜져 있으면 모든
    악기를 오선 여러 줄로, 아니면 지금 편집 중인 악기만. 둘이 늘 같은 것을 보여야 나란히 놓고
    견줄 수 있다(그러라고 아래에 붙여 둔 창이다). 왼쪽 세로줄이 한 벌임을 보이고, 각 번호는
    **맨 위 악기에만** 단다.
  - **악기 이름은 총보만이 아니라 파트보에도** 왼쪽에 붙는다 — 위에서 고른 악기가 오선보에도
    보여야 같은 것을 가리키게 된다. 이름을 안 적고 **악기만 고른 경우에도** 그 악기 이름이
    적히고(`buildStaffScores`가 이름→악기 순으로 골라 넘긴다), 이름도 악기도 없는 파트보는
    빈 채로 둔다('파트 1'이라 적으면 알려 주는 것 없이 자리만 먹는다). 첫 줄은 온이름,
    둘째 줄부터 약어이고 약어가 없으면 안 되풀이한다. 이름칸 폭은 **재서** 잡는다.
    악기 select의 change가 `scheduleStaff()`를 부르는 것을 빼지 말 것 — 악기는 정간보를
    안 바꾸므로 render()를 안 타서, 그게 없으면 오선보만 옛 이름으로 남는다.
  - **음장은 비례 간격**이다. 정간보의 분박은 5·7등분처럼 서양 음표값으로 안 떨어지는 것이
    흔한데, 억지로 맞추면 없는 쉼표·잇단음표를 지어내야 한다. 그래서 **음표꼴은 가장 가까운
    것으로 그리되 가로 자리는 실제 길이에 비례**해 놓는다 — 길이의 진실은 자리가 말한다.
    (정확한 길이는 MusicXML이 지므로 거기서 본다.)
  - 정간 폭은 **가장 짧은 음이 1.75칸을 갖도록** 잡는다(오선 칸 단위). 예전엔 '한 정간에
    음표가 몇 개인가'로 셌는데 그 셈은 음이 정간에 고르게 퍼져 있다고 본 것이라, 시김새가
    **앞 분박만** 다섯으로 가르면(`{느나르나니}황`) 실제 간격이 절반이 되어 머리와 덧줄이
    겹쳤다. 위아래 여백은 **곡의 음역을 재서** 잡는다 — 고정값이었을 땐 하배 음역이 잘렸다.
  - **자리표는 악기가 정해 놓았으면 그것, 아니면 덧줄이 적은 쪽**(`SC.pickClef(dias, inst)`).
    - **악기별 표는 `SC.CLEF_INST` 한 곳**이고 지금은 `거문고 → F` 하나다(2026-08-20 사용자
      요청). 거문고 채보 관행이 낮은음자리표로 굳어 있어, **곡이 높아 덧줄 셈이 뒤집히더라도
      악기가 바뀌지 않는 한 자리표도 바뀌지 않아야** 한다. 악기를 늘릴 땐 이 표에만 더할 것.
      악기는 파트의 `instrument`가 `buildStaffScores` → `staffScoreOf`의 meta로 실려 온다
      (안 고른 파트는 `"all"`이라 표에 안 걸리고 아래 덧줄 셈으로 간다).
    - 표에 없는 악기는 **적힌 음을 본다** — 낮은 음만으로 된 곡을 높은음자리표에 얹으면
      덧줄만 잔뜩 생겨 못 읽는다. 꾸밈음까지 함께 센다. 예전엔 '평균 음높이 < 57'이었는데
      대개 같은 답이 나오긴 해도 그건 대리 지표라, 정말로 재려는 것(덧줄이 몇 줄 생기나)을
      직접 세게 바꿨다.
    - 자리표 표(dia·기준줄·글리프·MusicXML sign/line)는 **staff-core의 `CLEF` 한 곳**에
      있다 — 고르는 쪽·화면·파일이 같은 것을 봐야 하므로 어느 한쪽에 다시 적지 말 것.
  - 덧줄은 **이웃까지의 거리에 맞춰 줄인다** — 촘촘한 자리에서 여러 줄이 이어 붙어 검은
    판으로 뭉쳤다.
  - 빔은 **한 박 안에서만** 묶고, 둘째 빔부터는 그만큼 짧은 음표가 이어지는 자리에만
    얹는다. **무엇이 한 박인지는 정간 단위가 정한다**: 점4분음표·4분음표면 정간 하나가 곧
    한 박이지만, **8분음표면 정간 하나는 한 박이 아니라서 대강으로 묶는다**(안 그러면 정간마다
    8분음표 하나씩 따로 꼬리가 붙어 꼬리 숲이 된다 — 12/8을 그렇게 적는 악보는 없다).
    대강은 정간보가 이미 갖고 있으므로 `staffScoreOf`가 `daegang`으로 실어 보낸다.
    안 적힌 악보는 셋씩, 셋으로 안 나뉘면 둘씩 묶는다.
    그 '정간 번호 → 박 묶음 번호' 표는 **`SC.beatGroups` 한 곳**에 있고 화면(staff-view)과
    파일(musicxml)이 같은 것을 본다 — 어느 한쪽에 다시 적지 말 것.
    **마디를 두 줄에 걸쳐 쪼개지 않는다** — 마디보다 창이 좁으면 가로로
    스크롤한다(그게 대가다). 묶음의 기둥 방향은 머릿수가 아니라 **가운뎃줄에서 가장 먼 음**이
    정하고, 묶음의 음역이 아홉 자리(옥타브 남짓)를 넘으면 **거기서 끊는다** — 둘 다 안 지키면
    빔이 한 음 너머로 밀려나 나머지 기둥이 오선을 통째로 가로지른다(9칸이 나왔다).
  - **굵기·크기는 예외 없이 '오선 한 칸(SP)'의 비율**이다(staff-view의 `metrics()`). 고정 px로
    그리면 크게 볼수록 실처럼 가늘어진다 — **새 요소도 px를 직접 쓰지 말 것.**
  - **자리표·임시표·쉼표·꼬리·박자표는 Bravura에서 떠 온 패스**다(`js/staff-glyphs.js`,
    생성 파일 — 직접 고치지 말 것). 예전엔 유니코드 글자로 찍었는데, 그러면 시스템 폰트에서
    빌려오게 되어 악보 글꼴이 없는 컴퓨터에서는 𝄞가 Apple Symbols·♭이 Times에서 나왔다.
    악보용으로 그려진 글자가 아니라 획 굵기가 음표와 안 맞고 모양도 조판 관행과 달라,
    **악보가 '손글씨처럼' 보이던 까닭이 이것이었다**(2026-08-10 사용자 지적).
    - 패스는 **오선 한 칸을 1로 본 좌표**이고 기준점이 SMuFL 규약대로 박혀 있다(자리표는 제가
      가리키는 줄, 임시표·쉼표·박자표는 붙는 줄, 꼬리는 기둥 끝). 그래서 놓을 자리에
      translate하고 SP로 scale하면 끝이고, **폰트마다 다른 잉크 상자를 재던 일이 통째로
      없어졌다**(옛 `ink()`·`INK` 실측표는 그래서 지웠다 — 되살리지 말 것).
    - 다시 뜨려면 `python3 tools/gen-staff-glyphs.py [Bravura.otf 경로]`.
      Bravura는 SIL OFL 1.1(Steinberg)이라 임베딩·파생물 배포가 허용되고, 여기서 만드는 것은
      윤곽선을 그림으로 구운 것이라 폰트 재배포가 아니다. **폰트 파일은 리포에 두지 않는다** —
      생성기의 입력일 뿐이다(받는 곳: github.com/steinbergmedia/bravura).
      워드마크(`tools/gen-wordmark.py`)와 같은 수법이다.
    - 꼬리는 **개수마다 제 글리프가 따로 있다**(8분·16분·32분·64분) — 8분 꼬리를 겹쳐 쌓는 게
      아니다. 쉼표도 꼴마다 따로다.
    - **음표머리만 아직 회전한 타원**이다. 실물과 거의 같은 데다 기둥 붙는 자리·점·덧줄 길이가
      전부 `headRx` 하나를 기준으로 짜여 있어 패스로 바꾸면 그 셈이 통째로 흔들린다.
    - 글리프가 통째로 빠지면 **조용히 아무것도 안 그려진다** — `check-staff-view.mjs`의
      '악보 글리프' 절이 개수와 자리표 기준점을 지켜본다.
  - **재생 중에는 지금 울리는 음표가 강조색으로 바뀐다**(app.js '재생 위치 짚기', 2026-08-14
    사용자 요청). 정간보가 현재 정간을 상자로 짚는 그 순간과 같은 것을 가리킨다.
    - 자리는 **Verovio가 조판과 함께 내주는 timemap**(시각 → 음표 id)을 그대로 쓴다
      (`grabTimemap`·`vrvNotesAt`). 우리 시간축과 어긋날 수가 없는 것은 MusicXML에 적어 보낸
      `<sound tempo>`가 재생과 같은 #tempoBpm에서 나오기 때문이다(bpm 60·점4분음표면
      tempo=90이 적히고 정간 하나가 딱 1000ms로 떨어짐을 실측). **자리를 다시 세지 말 것.**
    - **새 타이머를 두지 않는다** — 재생의 `tick()`(60ms)에 `staffPlayMark`를 얹었다. 시계가
      둘이면 둘이 어긋난다. 그림(staff-view)으로 물러난 경우엔 음표에 id가 없어 조용히 빠진다.
    - 색은 `--play-now`(차분한 벽돌빛 빨강, 다크는 한 단계 밝게). **강조색(--accent)을 쓰지
      말 것** — 갈색이라 검은 잉크 위에서 거의 안 보였다(2026-08-14 사용자 제보). 칠하는
      규칙을 새로 적지 않고 `.staff-now`의 **color 하나만** 갈아 끼운다(잉크가 이미
      fill: currentColor라 머리·기둥·꼬리·점이 한꺼번에 따라온다).
    - **세로 스크롤은 줄(system)이 바뀔 때만.** 음표마다 세로를 맞추면 음높이를 따라 악보가
      위아래로 출렁여 읽을 수가 없다(2026-08-14 사용자 제보) — 한 줄을 지나는 동안 종이는
      가만히 있어야 한다. `scrollStaffTo`의 `staffNowSys` 빗장이 그것이니 없애지 말 것.
    - 검증 창구는 `window.jgbStaffNow(ms)`(jgbAudioEvents·jgbShareLink와 같은 성격).
  - **창 폭에 우겨넣지 않는다 — 넘치면 가로로 스크롤한다**(2026-08-16 사용자 요청).
    조판을 **두 번** 한다: 첫 번째(loadData)에 `adjustPageWidth + noJustification`을 얹어
    **자연 폭**을 재고, 두 번째가 실제 그림이다. 자연 폭이 창보다 넓으면 그 폭으로 앉히고,
    좁으면 예전처럼 창 폭에 맞춰 고르게 늘린다(짧은 곡이 왼쪽에 쪼그라들지 않게).
    - **자연 폭을 잴 때 pageWidth를 크게 주지 말 것** — 줄이 아예 안 나뉘어 곡 전체 길이가
      나온다(14각이 한 줄). 창 폭 그대로 두고 두 옵션만 켜야 '줄마다의 자연 폭'이 나온다.
    - 조판 횟수는 **예전과 같은 두 번**이다(재는 것을 loadData에 얹었다). 관악영산회상 상령산
      (4파트·14각·음표 544) 실측 2.5초 → 2.75초.
    - staff-view(대비책)는 예전부터 이렇게 그려 왔다 — Verovio 경로만 창 폭에 묶여 있었다.
  - **조판기를 불러오는 동안엔 대비책 그림을 그리지 않는다**(2026-08-16 사용자 제보).
    예전엔 staff-view로 먼저 그렸는데, 6.7MB wasm이 받아지는 3초쯤 뒤 Verovio로 다시 그려지면서
    '다른 악보가 보였다가 바뀌는' 것처럼 보였다 — 두 그림은 생김새가 사뭇 달라서다.
    staff-view는 조판기를 **정말 못 쓸 때**(vrvState "failed")의 대비책이지 로딩 자리표시가
    아니다. 불러오는 동안엔 한 줄 안내를 띄운다.
    더불어 **#btnStaff에 마우스가 얹히면 미리 불러 둔다**(mouseenter·touchstart) — 얹었다 말면
    그만이라 손해가 없고, 누를 때쯤엔 대개 준비가 끝나 있다. 오선보를 안 쓰는 사람은 이 버튼에
    손이 안 가므로 지연 로드의 뜻도 그대로다.
  - **꾸밈음은 `graceFactor` 0.6**(Verovio 기본 0.75, `VRV_GRACE`). 시김새가 꾸밈음으로
    풀리는 이 악보에서는 **꾸밈음이 본음보다 많다**(상령산 실측: 본음 226 · 꾸밈 318).
    꾸밈음은 길이가 0이라 간격 옵션(spacingLinear·NonLinear)이 안 걸리고 제 글자 폭만큼만
    자리를 받으므로, 무리 지어 붙는 것을 푸는 길은 크기를 줄이는 것뿐이다.
  - **붙박이라 정간보를 고치면 곧바로 따라 바뀐다.** 다만 render()는 글자 한 자에도 불리므로
    `scheduleStaff()`가 **열려 있을 때만, 120ms 뒤에** 그린다 — 이 절 밖에서 오선보를 직접
    그리지 말 것.
  - 마크업은 `#sheetCol`이 #sheetArea와 #staffPane을 감싼다. 평소엔 `display:contents`라
    없는 셈이고, 기능바 왼쪽 도킹(#main이 가로 flex)일 때만 진짜 세로 열이 된다 —
    **#leftDock이 쓰는 것과 같은 수법**이니 함께 고칠 것.
  - **배율·높이는 앱 설정으로 기억하고(`jgb_staff_v1`) 열림은 안 기억한다** — 칸이 화면을 꽤
    차지해서 켜 둔 채 잊으면 다음에 열 때 종이가 좁아 보인다(2026-08-08 사용자 확정).
    다크·테마와 같은 자리(localStorage)인 건 그것이 '이 악보가 어떤 악보인가'가 아니라
    '내가 이 브라우저에서 어떻게 보는가'라서다 — 문서 상태에 넣으면 남이 내 악보를 열었을
    때 내 보기 방식이 따라간다. `?first=1`도 이 키는 안 지운다(온보딩과 무관).
  - `.no-print` — 화면 칸 자체는 인쇄에 안 나간다(종이로 뽑는 길은 아래 '오선보 인쇄' 절).
    검사는 `node tools/check-staff-view.mjs`.
- **오선보 인쇄·PNG**(오른쪽 레일의 [출력] 메뉴 = #outToggle → #outPop, app.js '오선보
  인쇄·PNG' 절): 오선보 인쇄/PNG · MusicXML 파일 · 정간보와 나란히 인쇄/PNG.
  - **예전엔 오선보 창 머리줄의 [출력 ▾]였다**(#staffOutToggle → #staffOutPop). 2026-08-26에
    출력을 한 자리로 모으며 레일로 옮겼다 — 그 자리에만 버튼이 있어 '오선보를 뽑으려면 창부터
    열어야 한다'는 숨은 규칙이 있었는데, 기능적으로는 그럴 이유가 없다(`vrvReady()`가 조판기를
    알아서 불러오고 `buildStaffScores()`도 창과 무관하다). 배선은 그대로 오선보 절에 있다 —
    그 블록은 창 열림과 무관하게 시작할 때 한 번 돈다.
  - **쪽은 Verovio가 접는다**(`vrvSheetPages`) — 종이 상자(mm)를 픽셀로 셈해 pageWidth/
    pageHeight로 주면 저들이 줄 경계에서 쪽을 가른다(오선 한 줄을 반으로 자르지 않는 규칙도
    저들 것). 나온 쪽 SVG의 루트를 mm 상자로 갈아입혀 종이에 앉힌다. 조판기 로드를 기다려야
    해서 staffOnlyPages·sideBySidePages는 **프로미스**를 주고, [출력] 핸들러가 .then으로 받는다.
    화면 배율과 무관한 크기(STAFF_PRINT_SCALE×40)는 화면 칸과 같은 원칙.
  - **staffSheetPages(staff-view 쪽 나눔)는 대비책으로 남는다** — 조판기를 못 불러왔을 때
    같은 계약({svg})으로 갈아끼워진다. 줄 높이·줄 수와 **줄 경계 표시(`<!--sys-->` 주석)** 를
    staff-view가 그림에 적어 보내고(`data-sys-h`·`data-sys-count`·`data-top`), 그 자리에서 갈라
    쪽마다 **제 줄만** 싣는 방식이다(통째로 싣고 viewBox로 오려 내면 쪽마다 곡 전체를 품어
    긴 곡에서 파일이 터진다). **그리기 셈을 인쇄 쪽에 옮겨 적지 말 것**은 두 길 다 같다 —
    자를 자리는 조판기가 말해 주는 것이지 여기서 다시 세는 것이 아니다.
  - **UI 표기는 '출력'** — 예전 이름 '뽑기'를 고쳤다(2026-08-15 사용자 요청). 인쇄·PNG·파일을
    함께 덮는 말이라야 하고, 앱의 다른 자리(상단바 [인쇄])와도 결이 맞는다. 파일 이름·식별자
    (staffOut*)는 그대로 — 코드 이름과 표시 이름을 분리하는 이 리포의 관례다(browse↔'모아보기',
    cellStyle*↔'정간 서식'과 같다).
  - 종이 크기·방향·여백은 **정간보와 같은 값**(#paperSize·#orientation·페이지 채움). 오선 크기만
    따로 잡는다 — 화면 배율은 '내가 어떻게 보나'라 종이에 따라오면 안 된다.
  - **인쇄 오선 크기는 '오선 한 칸 몇 mm'로 정한다**(#staffPrintSize, 출력 메뉴 안 ·
    `STAFF_PRINT_SP_MM` = 작게 1.4 · 보통 1.75 · 크게 2.2 · CTRL_IDS라 문서 값).
    조판에서 악보 크기를 말하는 단위가 그것이라서다(표준 보표는 한 칸 1.75mm). 예전엔
    `STAFF_PRINT_SCALE = 1.15`라는 뜻 없는 수였고 그 값이 한 칸 **2.19mm** — 표준보다 25%
    컸다(2026-08-16 실측 후 기본을 1.75mm로 내렸다).
    - 칸을 머리줄이 아니라 **출력 메뉴 안**에 둔 것은 뽑을 때만 쓰는 값이라서다. 그래서
      `wireTopMenu`가 메뉴 안 클릭에 닫히던 것을 **select·label·input은 빼도록** 고쳤다 —
      안 그러면 열자마자 닫혀 값을 고를 수가 없다.
    - **종이에는 가로 스크롤이 없다.** 화면은 자연 폭으로 넓히면 그만이지만 인쇄는 그럴 수가
      없으므로, **가로 쪽맞춤**을 한다 — 자연 폭이 종이보다 넓으면 그만큼 오선을 줄여 넣는다
      (2026-08-16 사용자 요청: "설정대로 다 들어가게, 음표 크기는 많이 줄여도 좋다").
      고른 크기는 **위 끝**이고(그보다 키우지 않는다) 아래 끝은 `STAFF_PRINT_MIN_SP_MM`(0.8mm)다.
    - **마디를 대신 나누지 않는다**(2026-08-16 사용자 확정). 마디를 어디서 끊을지는 사용자가
      정한 것이고 **인쇄는 그 설정대로 나와야 한다** — 안 들어간다고 앱이 #staffBar를 바꿔
      뽑으면 화면과 종이가 다른 악보가 된다. 종이가 모자라면 오선을 줄일 뿐이다.
    - 20정간 각(관악영산회상 상령산 4파트)을 A4 세로로 뽑은 실측 — 쪽맞춤 전 → 후
      (본음 사이 최소 간격, 오선칸 단위):
      **0.01 → 1.47**(오선칸이 1.75mm에서 1.38mm로 저절로 줄고 쪽수는 5 → 4).
      쪽맞춤이 있어도 [마디: 대강마다]로 줄을 늘리면 오선이 덜 줄어든다(1.62mm·간격 1.23).
  - **나란히는 좌우·위아래 둘이고, 놓는 방향이 종이 방향까지 정한다**(2026-08-16 사용자 요청):
    좌우면 **가로로 긴 종이**, 위아래면 **세로로 긴 종이**라야 반쪽이 안 찌그러진다. 사용자의
    종이 **크기**는 그대로 쓰고 **방향만** `sideBySideBox(dir)`가 돌린다 — 세로로 작업하다
    좌우 나란히를 눌러도 알아서 가로로 나간다.
    - **@page도 함께 돌려야 한다.** #pageStyle은 render()가 사용자 방향으로 써 두므로, 그대로
      두면 브라우저가 세로 용지에 가로 그림을 축소해 앉힌다. `printPages(pages, what, box)`가
      #pageStyle **뒤에** 놓이는 `#printPageStyle`로 덮고 인쇄가 끝나면 비운다 — 사용자 문서
      설정(#paperSize·#orientation)은 한 번도 안 건드린다.
    - **어느 쪽이든 정간보가 먼저다** — 좌우 = 왼쪽 정간보·오른쪽 오선보, 위아래 = 위 정간보·
      아래 오선보(2026-08-16 사용자 확정). 이 앱이 만드는 것은 정간보이고 오선보는 그것을 옮겨
      적은 것이므로, 왼→오른쪽·위→아래로 읽는 종이에서 원본이 앞에 온다. 예전 좌우는 반대였는데
      (정간보가 제 안에서 오른쪽→왼쪽으로 읽으니 종이 오른쪽 반에 두자는 셈이었다) 그건 정간보
      **안**의 방향일 뿐, 두 악보를 나란히 놓는 차례를 정하는 근거가 못 된다.
    정간보는 **반쪽 크기 종이로 다시 앉힌다**(좌우면 반 폭·위아래면 반 높이) — 전체 폭 배치를
    그대로 줄이면 아래 반이 통째로 빈다. 그래서 종이 칸(#paperSize·#paperW·#paperH·#orientation)을
    잠깐 반 폭 세로로 바꿔 `render()`를 한 번 돌리고, 그림과 `pageGakRanges`를 걷은 뒤 원래
    값으로 되돌려 다시 그린다(render는 컨트롤을 읽어 그릴 뿐 저장·되돌리기를 안 건드린다).
    되돌리기는 `finally`에 둘 것 — 중간에 터지면 사용자 종이 설정이 반 폭인 채로 남는다.
  - **대응은 쪽 단위**다: 그 쪽의 정간보에 담긴 각 = 그 쪽 오선보의 마디(`pageGakRanges`로 자른다).
    줄 단위까지 맞추려면 각마다 오선 한 줄을 강제해야 하는데 그러면 짧은 각에서 오선보가 뭉텅뭉텅
    빈다(사용자 확정, 2026-08-13).
  - 인쇄용 종이는 **#staffSheet**에 깔고 `body.print-staff`가 인쇄에서만 #sheet 대신 그것을 내보낸다
    (끝나면 `afterprint`로 걷는다). #sheet를 갈아 끼우지 않는 까닭은 인쇄 뒤 화면을 되돌리는 사이
    편집 상태가 흔들리기 때문. #pageStyle의 인쇄 규칙이 `.page > svg`인 것도 이 때문이다 —
    직계가 아니면 오선보 종이가 품은 안쪽 `<svg>`(줄을 오려 내는 상자)까지 종이 크기로 부풀어
    그림이 터진다.
- **재생은 선율(사인파) + 장단(장구 녹음 음원) 두 갈래**다(app.js '재생' 절).
  `buildAudioEvents`가 한 시간축에서 둘을 함께 짜는데, 장단은 음높이가 없어 `janggu:[{t,name}]`로
  따로 담는다. 장단 줄은 **한 각짜리 패턴 하나**뿐이므로(악보에도 맨 처음 각 옆에 한 번만 그린다)
  각마다 같은 칸을 그 각의 시각에 얹는다 — 선율이 비어 있는 각도 시간은 흐르므로 거기서도 울린다.
  소리 조건은 `jangguSoundOn()` 한 곳: 장단 줄 켬 + 적힌 구음 있음 + 재생 설정의 #playJanggu.
  - 음원은 **재생용 컨텍스트에서 풀고**(`jangguBuffers`) 표본율이 같을 때만 캐시를 다시 쓴다 —
    재생용 ctx는 정지할 때마다 close 되고 다음 재생에 새로 만들어지기 때문.
  - 음원을 불러 푸는 동안 시작이 늦어 `playPending` 빗장이 필요하다. 그래서 옛 `playMelody`가
    **playMelody(준비) + startPlayback(실제 재생)** 둘로 갈렸다 — 재생 시작 코드를 고칠 땐 뒤쪽.
  - **모든 소리는 출력 버스 하나로 모여 나간다**(app.js '출력 버스' 절, `makeOutBus`) —
    선율 master도 장구도 이 버스를 지나고 `ctx.destination`에 닿는 것은 버스뿐이다.
    **새 소리를 destination에 직접 연결하지 말 것**: 우회하는 소리가 있으면 아무리 나눠도
    그만큼 새어 나가 최대치를 넘긴다.
    - **마스터는 소리 나는 파트 수로 나눈다**(`MELODY_PEAK / built.voices`). 예전엔 0.25
      고정이었는데, 음 하나가 진폭 1까지 오르고 정악은 헤테로포니라 악기들이 **같은 가락을
      같은 시각에** 낸다. 게다가 다 순수 사인파에 `osc.start(on)`이라 **시작 위상까지 같아**
      √N이 아니라 그대로 N배로 더해진다 — 봉우리가 정확히 0.25 × 파트 수였고(5파트
      영산회상 군악 1.249), destination이 [-1,1]로 하드 클립하며 사인 꼭대기가 평평해진 것이
      총보에서 나던 '지지직'이었다(2026-08-15 사용자 제보). **1/√N으로 바꾸지 말 것** —
      제창이라 봉우리가 파트 수에 정비례한다(실측).
    - 나눈 뒤에도 넘치는 자리(꾸밈음이 겹치거나 장구 겹타)는 **소프트 클립**(`softClipCurve`,
      WaveShaper + 4x 오버샘플)으로 누른다. 무릎(`SOFT_KNEE` 0.7) **아래는 정확히 통과**라
      평소 소리엔 왜곡이 0이다. WaveShaper의 curve는 입력 -1~1로 색인되고 그 밖은 끝값에
      물리므로, 버스 앞 `pre.gain = 0.5`로 줄여 넣고 곡선이 그 절반을 되돌린다(그래서 원래
      진폭 2.0까지 다룬다) — **이 짝을 따로 떼어 고치지 말 것.**
    - 장구는 master가 아니라 **버스에 나란히** 붙는다(`JANGGU_GAIN`) — 녹음 음원이라 선율
      master에 같이 태우면 묻힌다(파트 수로 나뉘므로 더욱). 선율 합의 봉우리가 파트 수와
      무관하게 `MELODY_PEAK`로 고정이라 이 비율은 독주·합주에서 똑같이 유지된다.
      마지막 박의 울림이 남으므로 총 길이도 그만큼 늘린다.
    - 검사는 `node tools/check-play-mix.mjs` — voices 세기 · 표본으로 잰 봉우리가 파트 수와
      무관하게 한계 안인지 · 소프트 클립이 평소 소리에 손을 안 대는지를 함께 본다.
  - **겹타는 음원을 박보다 먼저 시작한다**(`JANGGU_LEAD`, 지금은 기덕 0.15초). 기덕 음원은
    맨 앞이 본 타가 아니라 앞꾸밈('기')이고 본 타('덕')가 0.150초에 있어(실측), 파일 시작을
    박에 맞추면 귀에 박으로 들리는 소리가 그만큼 늦는다. 그래서 `startAt`에 **가장 큰
    당김만큼 여유를 더 둔다** — 안 그러면 첫 박의 겹타만 시작 시각이 과거가 되어 앞꾸밈이
    잘린 채 울린다. 음원을 다시 뽑거나 겹타를 늘릴 땐 이 표에 잰 값을 적을 것.
  - 못 불러와도(`loadJangguAudio` 실패) 선율 재생은 그대로 간다 — 소리 하나 때문에 재생이
    통째로 막히면 안 된다.
- 되돌리기는 전역 하나뿐(⌘/Ctrl+Z, render()마다 스냅샷·600ms 디바운스). 장단/가사
  전용 되돌리기는 제거했음 — 다시 만들지 말 것.
- 한글 IME keydown 핸들러엔 `e.isComposing || e.keyCode === 229` 가드 필수.
- 선율/가사 데이터에서 빈 정간은 반드시 `" | "` 꼴 (`||`는 옛 각-구분으로 파싱됨).
  가사는 각·줄 단위로 선율과 1:1 — 줄 넣고 뺄 때 같이 밀고 당겨야 함(insertGakBelow 참고).
- 도움말/둘러보기/환영 카드: 상단바 #btnHelp → #helpModal(4탭, 전용 .help-tab/.help-pane —
  전역 .tab/.tabpanel은 사이드바가 문서 전체를 토글하므로 절대 혼용 금지), 투어는 #tourLayer
  (TOUR_STEPS, app.js "도움말 센터" 섹션). 진짜 첫 방문(`jgb_welcome_v1`·`jgb_guide_seen_v1`·
  상태 전부 없음)에만 환영 카드 → 어떤 선택이든 새 문서 마법사로 수렴.
  - 투어 본문(body)은 줄글 금지 — `\n`으로 나눈 `•` 글머리표 한 줄씩. showTourStep이 줄마다
    div로 넣고 CSS(#tourBody div)가 내어쓰기 + word-break:keep-all을 건다(통짜 p + pre-line로는
    접힌 둘째 줄이 • 밑에 붙는다 — text-indent는 블록 첫 줄에만 걸려서).
  - **`?first=1`**(consumeFirstVisitParam)로 온보딩을 첫 방문처럼 다시 볼 수 있다. 강력
    새로고침(Cmd+Shift+R)은 HTTP 캐시만 비우고 localStorage는 그대로라 아무리 눌러도 첫 방문이
    안 되고, 브라우저가 '강력 새로고침인지'를 JS에 알려주지 않아 그 동작을 훅으로 잡을 수도 없어서 둔 것.
    100% 같은 조건은 시크릿 창이고 이건 '지금 창에서 빠르게' 확인용.
    지우는 건 온보딩 관문 + 현재 곡뿐 — **보관(jgb_snapshots_v1)·다크(jgb_dark_v1)는 일부러 남긴다**
    (온보딩과 무관한데 지우면 남의 자료가 날아간다. localStorage.clear()를 쓰지 말 것).
    현재 곡은 지울 수밖에 없어(남으면 restored라 카드가 안 뜸) confirm()으로 한 번 묻고,
    파라미터는 쓰자마자 replaceState로 주소에서 뗀다 — 이 주소를 북마크해두고 새로고침할 때마다
    작업이 날아가는 걸 막는 것이니 그 순서를 바꾸지 말 것.

## 프리뷰 검증 (.claude/launch.json의 "jgb")

- iCloud 폴더라 프리뷰 서버 프로세스가 직접 못 읽음 — launch.json은 /tmp/jgb-mirror-wt를
  서빙하고, 파일 수정 후엔 Bash에서 rsync로 미러 갱신 + 리로드해야 반영됨:
  `rsync -a --delete --exclude .git --exclude .claude --exclude paper --exclude references --exclude _보관 "<워크트리>/" /tmp/jgb-mirror-wt/`
  (paper/ 안 일부 한글 파일명이 rsync에서 Illegal byte sequence를 내므로 반드시 제외. 앱은 index.html·
  css/·js/ 만 있으면 돌아가므로 references/·_보관/·assets/·tools/는 제외해도 무방)
- 첫 로드에 새 문서 모달이 뜸 → `document.getElementById('ndCancel').click()`.
  localStorage가 완전히 비어 있으면 대신 환영 카드(#welcomeModal)가 뜸 → `#wcSkip` 클릭.
- 직접 입력 전환: `document.getElementById('modeDirect').click()` (에디터는 `modeEditor`).
  예전 `melInputSelect` <select>는 없어졌다 — 지금은 #modeToggle ⋯ #modePop 메뉴.
- preview_eval에서 DOMRect는 `{}`로 직렬화됨 — `[left,top,right,bottom]` 배열로 손수 변환.
- 뷰포트가 0×0으로 측정되면 preview_resize 후 다시 측정.
- **치수를 재기 전엔 스크린샷을 한 번 찍을 것** — 탭·모드를 바꾼 직후 eval에서 재면
  getBoundingClientRect가 낡은 레이아웃(버튼이 죄다 22px 따위)을 돌려준다. 스크린샷이 리플로를
  강제하므로 그 뒤에 재야 참값이 나온다. 값이 수상하면 눈으로 본 스크린샷을 믿을 것.
- 악보의 선 하나를 눈으로 확인할 땐 **SVG를 cloneNode해 viewBox를 그 자리로 좁힌 오버레이**를
  띄우는 게 제일 낫다(앱 배율을 올리는 것보다 정확·간단). Browser 도구의 zoom(region crop)은 안 먹는다.
- requestAnimationFrame·scrollIntoView(smooth)는 실행 안 됨 — setTimeout 사용.
- 실제 클릭(preview_click)은 뷰포트 리사이즈 후 좌표가 어긋나 `<html>`에 떨어질 수 있음 —
  preview_eval에서 `.click()`으로 대신할 것.
- **포인터 상호작용(드래그 등) 검증은 반드시 실제 히트테스트 경로로** — 대상 요소에
  dispatchEvent를 직접 쏘면 위에 덮인 요소(투명 클릭존 등)를 우회해 가짜 통과가 난다.
  `document.elementFromPoint(x,y)`로 그 좌표의 최상단 요소를 얻어 **거기에** 이벤트를 쏠 것
  (각/장 이름 드래그가 이걸로 한 번 가짜 통과했다 — bdfed51에서 수정).
- 프리뷰 브라우저가 js/css를 강하게 캐시해 rsync 후에도 옛 코드가 돌 수 있다
  (performance 리소스의 transferSize 0 = 캐시). 미러의 index.html에만 `?v=타임스탬프`를
  sed로 찍는 게 확실: rsync 후
  `sed -i '' -e 's|src="js/app.js"|src="js/app.js?v=STAMP|' …` (원본엔 안 찍음).
- 테스트 후 `localStorage.clear()`로 원상복구(키: jgb_state_v1, jgb_guide_seen_v1, jgb_welcome_v1).

## 관례

- **악보 SVG는 off-DOM으로 조립한 뒤 붙인다** — 그리는 중 `getBBox()`는 0을 준다(상자가 원점에
  5×5로 찍힘). 제목·부제·자유텍스트의 하이라이트 겸 드래그 히트(.hit-fit)는 SVG 삽입 뒤
  `fitDragHits()`가 실제 글자 크기에 맞춰 다시 잡는다. getBBox로 크기를 잡아야 하면 이 패턴을 쓸 것
  (각/장 라벨은 대신 좌표로 계산). 제목·부제·자유텍스트 드래그·하이라이트는 텍스트 창(文)이
  열려 있을 때만(textWinOpen) — 章 이름이 章 창 열림에만 반응하는 것과 같은 규칙.
- **글자와 이미지는 '크기'의 기준이 다르다** — 한자는 잉크가 글자 크기의 0.86배만 채우는데
  (측정: 林 0.857·黃 0.872), 잉크 경계로 크롭된 PNG·SVG는 상자를 0.96배까지 꽉 채운다. 같은
  값을 주면 이미지가 더 커 보인다. 특수 율명(하하배임)은 `SPECIAL_NOTE_SCALE`(0.9)로 그 차이를
  걷어낸다. 이미지 기호를 글자 옆에 나란히 놓을 땐 늘 이 보정을 생각할 것.
- 세로쓰기(`verticalText`)는 괄호를 **시계방향 90° 눕혀** 그린다(VERT_ROTATE 집합, 전통 세로
  조판). `(`→︵, `)`→︶ 꼴이 되어 글줄을 위/아래에서 감싼다. 세로 자리는 그대로 한 글자만큼
  차지한다(세로 격자와 줄 간격을 맞추려고). 제목·부제·자유텍스트·각/장 이름·빠르기가 이 함수를
  같이 쓰므로 한 곳만 고치면 다 걸린다. ASCII `<`·`>`는 일부러 제외 — 이 앱에서 `<`는 숨표
  토큰이다. **가로쓰기는 눕히지 않는다**(가로에선 괄호가 서 있어야 맞다). 가사는
  `drawLyricCell`이 한 분박 행을 통짜 `<text>` 하나로 그려 글자가 가로로 흐르므로 대상이 아니다.
- 둘러보기 문구는 `js/tour-text.js`(사람이 직접 고침), 뼈대(위치·그림·장)는 app.js TOUR_STEPS.
  - **하이라이트 대상(`sel`·`also`)은 세 꼴을 받는다**(`rectOfSpec`): `"선택자"`(첫 요소) ·
    `{union:"선택자"}`(잡히는 것 **전부를 감싸는** 한 상자) · `[스펙,…]`(앞에서부터 되는 것).
    악보의 줄은 칸이 여럿으로 쪼개져 그려지므로 union이라야 '줄 하나'로 가리켜진다.
  - `also` 항목에 `label`을 주면 상자에 **이름표**가, `tone`을 주면 **다른 색**이 붙는다
    (`labelPos`: 기본 위 · `"side"`=오른쪽 바깥). **첫 장(정간보)** 이 정간·대강·각을 이렇게 짚는다 —
    무엇을 가리키는 말인지 모른 채 넘어가면 뒤가 다 헛돌아서 첫 장에 둔다.
  - 그 셋은 **각기 다른 각**에 있고 색도 다르다. 한 각에 포개면 큰 상자 안에 작은 상자가 들어가
    어느 게 무엇인지 헷갈린다 — 도로 한 각에 몰지 말 것.
  - 말풍선은 `also` 상자를 덮지 않게 옆으로 비킨다(positionTour). 구멍이 화면만큼 크면
    (악보 전체 등) 말풍선이 대상 위에 앉을 수밖에 없는데, 정작 가리키는 상자를 덮으면
    그 단계가 통째로 무의미해진다.
  - 그 표적은 render가 히트 사각형에 다는 클래스다: `.tour-lane-mel`(정간) · `.tour-lane-ly`
    (곁줄 — 곁줄이 아직 없을 때의 '진입로'에도 붙는다) · `.tour-lane-jd`(장단) ·
    `.tour-lane-cell`(첫 정간 하나) · `.tour-lane-dg`(**두 번째** 대강 — 첫 대강은 각과 같은
    자리에서 시작해 상자가 겹친다).
    **정간·곁줄은 첫 각(melIdx 0)만** 단다 — 전부 달면 하이라이트가 격자 전체가 되어 어느
    줄을 말하는지 되레 안 보인다. 장단은 곡에 한 줄뿐이라 전부 단다.
    대상이 없으면(장단이 꺼져 있는 등) rectOfSpec이 null을 주고 그 강조만 조용히 빠진다.
  글 꼴은 **`**항목 이름**: 내용` 한 줄씩**, 맺음은 **"~할 수 있습니다"** — 둘러보기는 '무엇을 할
  수 있는지' 보여주는 자리라 "~합니다"로 끊으면 설명서처럼 읽힌다. 사람이 하는 일이 아닌 것
  (읽는 방향, 저절로 그려지는 것)까지 억지로 맞추지는 말 것.
  **항목 이름엔 대상을 붙일 것** — "켜기"·"적기"·"위치"처럼 대상 없는 말은 카드를 닫으면 무엇을
  켜라는 건지 남지 않는다. "장단 켜기"·"장단 입력"·"시김새 위치·크기 편집" 꼴로 쓴다.
  한 가지 일을 여러 줄로 쪼개지도 말 것(켜는 법과 쓰는 법이 한 흐름이면 한 줄에).
  규칙은 파일 머리 주석에도 적어 뒀다.
  어둠은 #tourSpot SVG 마스크(여러 곳을 밝게 뚫음, box-shadow 컷아웃 아님).
- **숫자 칸은 성격에 따라 배선이 갈린다.**
  - `wireLive` — **눈으로 보며 맞추는 값**(크기·간격·자간·위치): 치는 대로 바로 반영.
    min/max 밖의 '치는 도중 값'은 렌더에 안 넘기고(11을 치려고 1을 친 순간 정간이 1mm가 되어
    페이지가 수백 장으로 불어나는 걸 막는다) 90ms 디바운스. 포커스가 떠날 땐 범위 안으로 당겨 맞춘다.
  - `wireConfirm` — **개수를 정하는 값**(정간 수·각별 정간 수·총 각 수·한 줄 각 수·페이지당 줄 수·대강 분절):
    [확인]/Enter로만 적용. 중간값도 범위 안이라 걸러지지 않고, 배치가 통째로 뒤집혀 눈이 못 따라간다.
    대강은 '숫자의 합이 정간 수와 같아야' 해서 치는 도중엔 늘 어긋난다.
  - 새 숫자 칸을 만들 땐 이 둘 중 어디인지 먼저 정할 것. 헷갈리면 '이 값을 바꾸면 각의 **개수**가
    바뀌나?'로 가른다. 빠르기 BPM 칸들은 `wireLive` — min이 20이라 한 자릿수가 저절로 걸러져
    '一分・一井' 같은 중간 표기가 안 나온다.
- UI 문구는 -니다체, 여러 문장이면 `•` 글머리표.
  - **예외: 입력 팔레트 위 한 줄 안내(`.sym-hint`)와 검색칸 안내는 개조식** —
    `<무엇을 어떻게> ex) <보기> · <보기>` 꼴로 서술어를 뺀다(예: `✎ 글자 그대로/괄호 안에
    넣어 입력 가능 ex) 덩 · {덩}`). 늘 떠 있는 한 줄이라 짧아야 읽힌다. 긴 설명은 머리줄
    `?` 안내(melody-guide)가 지고, 그쪽은 -니다체 그대로.
- 커밋은 요청 시에만, 한국어 conventional(feat:/fix:/chore:).
- 레이아웃 기준은 `악보 예시/`의 취타·군악 이미지(전통 방식: 오른쪽→왼쪽).
